// @generated by `cargo xtask codegen` from the vendored OCPP JSON schemas.
// DO NOT EDIT — re-run the generator instead. CI fails if this file is stale.

//! Request and response payloads for every OCPP 1.6 action.

#![allow(unused_imports, clippy::doc_markdown, clippy::struct_excessive_bools)]
#![allow(
    clippy::too_many_lines,
    clippy::too_many_arguments,
    clippy::wildcard_imports
)]
#![allow(clippy::match_same_arms, clippy::fn_params_excessive_bools)]

use alloc::string::String;
use alloc::vec::Vec;

use serde::{Deserialize, Serialize};

use super::action::Action;
use super::enums::*;
use super::types::*;
use crate::message::{Confirmed, MessageKind, Origin, Request, Response, Sealed, Unconfirmed};
use crate::validate::{self, Validate, ValidationPath, Violations};

/// Spec: OCPP 1.6 · Core · Charging Station → CSMS · CALL.
#[derive(Clone, Debug, PartialEq, Serialize, Deserialize)]
pub struct AuthorizeRequest {
    /// The `idTag` member.
    #[serde(rename = "idTag")]
    pub id_tag: String,
}

impl AuthorizeRequest {
    /// Creates a `AuthorizeRequest` from its required fields.
    #[must_use]
    pub fn new(id_tag: impl Into<String>) -> Self {
        Self {
            id_tag: id_tag.into(),
        }
    }
}

impl Validate for AuthorizeRequest {
    fn validate_at(&self, path: &mut ValidationPath, out: &mut Violations) {
        path.push_key("idTag");
        validate::string(self.id_tag.as_str(), None, Some(20), path, out);
        path.pop();
    }
}

impl Sealed for AuthorizeRequest {}
impl Request for AuthorizeRequest {
    type Action = Action;
    const ACTION: Action = Action::Authorize;
    const KIND: MessageKind = MessageKind::Call;
    const ORIGIN: Origin = Origin::ChargingStation;
    type Response = AuthorizeResponse;
}

impl Confirmed for AuthorizeRequest {}

/// Spec: OCPP 1.6 · Core · Charging Station → CSMS · CALL.
#[derive(Clone, Debug, PartialEq, Serialize, Deserialize)]
pub struct AuthorizeResponse {
    /// The `idTagInfo` member.
    #[serde(rename = "idTagInfo")]
    pub id_tag_info: IdTagInfo,
}

impl AuthorizeResponse {
    /// Creates a `AuthorizeResponse` from its required fields.
    #[must_use]
    pub fn new(id_tag_info: IdTagInfo) -> Self {
        Self { id_tag_info }
    }
}

impl Validate for AuthorizeResponse {
    fn validate_at(&self, path: &mut ValidationPath, out: &mut Violations) {
        path.push_key("idTagInfo");
        Validate::validate_at(&self.id_tag_info, path, out);
        path.pop();
    }
}

impl Sealed for AuthorizeResponse {}
impl Response for AuthorizeResponse {}

/// Spec: OCPP 1.6 · Core · Charging Station → CSMS · CALL.
#[derive(Clone, Debug, PartialEq, Serialize, Deserialize)]
pub struct BootNotificationRequest {
    /// The `chargePointVendor` member.
    #[serde(rename = "chargePointVendor")]
    pub charge_point_vendor: String,
    /// The `chargePointModel` member.
    #[serde(rename = "chargePointModel")]
    pub charge_point_model: String,
    /// The `chargePointSerialNumber` member.
    #[serde(rename = "chargePointSerialNumber")]
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub charge_point_serial_number: Option<String>,
    /// The `chargeBoxSerialNumber` member.
    #[serde(rename = "chargeBoxSerialNumber")]
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub charge_box_serial_number: Option<String>,
    /// The `firmwareVersion` member.
    #[serde(rename = "firmwareVersion")]
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub firmware_version: Option<String>,
    /// The `iccid` member.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub iccid: Option<String>,
    /// The `imsi` member.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub imsi: Option<String>,
    /// The `meterType` member.
    #[serde(rename = "meterType")]
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub meter_type: Option<String>,
    /// The `meterSerialNumber` member.
    #[serde(rename = "meterSerialNumber")]
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub meter_serial_number: Option<String>,
}

impl BootNotificationRequest {
    /// Creates a `BootNotificationRequest` from its required fields.
    #[must_use]
    pub fn new(
        charge_point_vendor: impl Into<String>,
        charge_point_model: impl Into<String>,
    ) -> Self {
        Self {
            charge_point_vendor: charge_point_vendor.into(),
            charge_point_model: charge_point_model.into(),
            charge_point_serial_number: None,
            charge_box_serial_number: None,
            firmware_version: None,
            iccid: None,
            imsi: None,
            meter_type: None,
            meter_serial_number: None,
        }
    }

    /// Sets the optional `chargePointSerialNumber` field.
    #[must_use]
    pub fn with_charge_point_serial_number(mut self, value: impl Into<String>) -> Self {
        self.charge_point_serial_number = Some(value.into());
        self
    }

    /// Sets the optional `chargeBoxSerialNumber` field.
    #[must_use]
    pub fn with_charge_box_serial_number(mut self, value: impl Into<String>) -> Self {
        self.charge_box_serial_number = Some(value.into());
        self
    }

    /// Sets the optional `firmwareVersion` field.
    #[must_use]
    pub fn with_firmware_version(mut self, value: impl Into<String>) -> Self {
        self.firmware_version = Some(value.into());
        self
    }

    /// Sets the optional `iccid` field.
    #[must_use]
    pub fn with_iccid(mut self, value: impl Into<String>) -> Self {
        self.iccid = Some(value.into());
        self
    }

    /// Sets the optional `imsi` field.
    #[must_use]
    pub fn with_imsi(mut self, value: impl Into<String>) -> Self {
        self.imsi = Some(value.into());
        self
    }

    /// Sets the optional `meterType` field.
    #[must_use]
    pub fn with_meter_type(mut self, value: impl Into<String>) -> Self {
        self.meter_type = Some(value.into());
        self
    }

    /// Sets the optional `meterSerialNumber` field.
    #[must_use]
    pub fn with_meter_serial_number(mut self, value: impl Into<String>) -> Self {
        self.meter_serial_number = Some(value.into());
        self
    }
}

impl Validate for BootNotificationRequest {
    fn validate_at(&self, path: &mut ValidationPath, out: &mut Violations) {
        path.push_key("chargePointVendor");
        validate::string(self.charge_point_vendor.as_str(), None, Some(20), path, out);
        path.pop();
        path.push_key("chargePointModel");
        validate::string(self.charge_point_model.as_str(), None, Some(20), path, out);
        path.pop();
        path.push_key("chargePointSerialNumber");
        if let Some(value) = &self.charge_point_serial_number {
            validate::string(value.as_str(), None, Some(25), path, out);
        }
        path.pop();
        path.push_key("chargeBoxSerialNumber");
        if let Some(value) = &self.charge_box_serial_number {
            validate::string(value.as_str(), None, Some(25), path, out);
        }
        path.pop();
        path.push_key("firmwareVersion");
        if let Some(value) = &self.firmware_version {
            validate::string(value.as_str(), None, Some(50), path, out);
        }
        path.pop();
        path.push_key("iccid");
        if let Some(value) = &self.iccid {
            validate::string(value.as_str(), None, Some(20), path, out);
        }
        path.pop();
        path.push_key("imsi");
        if let Some(value) = &self.imsi {
            validate::string(value.as_str(), None, Some(20), path, out);
        }
        path.pop();
        path.push_key("meterType");
        if let Some(value) = &self.meter_type {
            validate::string(value.as_str(), None, Some(25), path, out);
        }
        path.pop();
        path.push_key("meterSerialNumber");
        if let Some(value) = &self.meter_serial_number {
            validate::string(value.as_str(), None, Some(25), path, out);
        }
        path.pop();
    }
}

impl Sealed for BootNotificationRequest {}
impl Request for BootNotificationRequest {
    type Action = Action;
    const ACTION: Action = Action::BootNotification;
    const KIND: MessageKind = MessageKind::Call;
    const ORIGIN: Origin = Origin::ChargingStation;
    type Response = BootNotificationResponse;
}

impl Confirmed for BootNotificationRequest {}

/// Spec: OCPP 1.6 · Core · Charging Station → CSMS · CALL.
#[derive(Clone, Debug, PartialEq, Serialize, Deserialize)]
pub struct BootNotificationResponse {
    /// The `status` member.
    pub status: RegistrationStatus,
    /// The `currentTime` member.
    #[serde(rename = "currentTime")]
    pub current_time: crate::types::DateTime,
    /// The `interval` member.
    pub interval: i32,
}

impl BootNotificationResponse {
    /// Creates a `BootNotificationResponse` from its required fields.
    #[must_use]
    pub fn new(
        status: RegistrationStatus,
        current_time: impl Into<crate::types::DateTime>,
        interval: i32,
    ) -> Self {
        Self {
            status,
            current_time: current_time.into(),
            interval,
        }
    }
}

impl Validate for BootNotificationResponse {
    fn validate_at(&self, path: &mut ValidationPath, out: &mut Violations) {
        path.push_key("status");
        Validate::validate_at(&self.status, path, out);
        path.pop();
    }
}

impl Sealed for BootNotificationResponse {}
impl Response for BootNotificationResponse {}

/// Spec: OCPP 1.6 · Reservation · CSMS → Charging Station · CALL.
#[derive(Clone, Debug, PartialEq, Serialize, Deserialize)]
pub struct CancelReservationRequest {
    /// The `reservationId` member.
    #[serde(rename = "reservationId")]
    pub reservation_id: i32,
}

impl CancelReservationRequest {
    /// Creates a `CancelReservationRequest` from its required fields.
    #[must_use]
    pub fn new(reservation_id: i32) -> Self {
        Self { reservation_id }
    }
}

impl Validate for CancelReservationRequest {
    fn validate_at(&self, _path: &mut ValidationPath, _out: &mut Violations) {}
}

impl Sealed for CancelReservationRequest {}
impl Request for CancelReservationRequest {
    type Action = Action;
    const ACTION: Action = Action::CancelReservation;
    const KIND: MessageKind = MessageKind::Call;
    const ORIGIN: Origin = Origin::Csms;
    type Response = CancelReservationResponse;
}

impl Confirmed for CancelReservationRequest {}

/// Spec: OCPP 1.6 · Reservation · CSMS → Charging Station · CALL.
#[derive(Clone, Debug, PartialEq, Serialize, Deserialize)]
pub struct CancelReservationResponse {
    /// The `status` member.
    pub status: CancelReservationStatus,
}

impl CancelReservationResponse {
    /// Creates a `CancelReservationResponse` from its required fields.
    #[must_use]
    pub fn new(status: CancelReservationStatus) -> Self {
        Self { status }
    }
}

impl Validate for CancelReservationResponse {
    fn validate_at(&self, path: &mut ValidationPath, out: &mut Violations) {
        path.push_key("status");
        Validate::validate_at(&self.status, path, out);
        path.pop();
    }
}

impl Sealed for CancelReservationResponse {}
impl Response for CancelReservationResponse {}

/// Spec: OCPP 1.6 · Security (Whitepaper ed. 2) · CSMS → Charging Station · CALL.
#[derive(Clone, Debug, PartialEq, Serialize, Deserialize)]
pub struct CertificateSignedRequest {
    /// The `certificateChain` member.
    #[serde(rename = "certificateChain")]
    pub certificate_chain: String,
}

impl CertificateSignedRequest {
    /// Creates a `CertificateSignedRequest` from its required fields.
    #[must_use]
    pub fn new(certificate_chain: impl Into<String>) -> Self {
        Self {
            certificate_chain: certificate_chain.into(),
        }
    }
}

impl Validate for CertificateSignedRequest {
    fn validate_at(&self, path: &mut ValidationPath, out: &mut Violations) {
        path.push_key("certificateChain");
        validate::string(
            self.certificate_chain.as_str(),
            None,
            Some(10000),
            path,
            out,
        );
        path.pop();
    }
}

impl Sealed for CertificateSignedRequest {}
impl Request for CertificateSignedRequest {
    type Action = Action;
    const ACTION: Action = Action::CertificateSigned;
    const KIND: MessageKind = MessageKind::Call;
    const ORIGIN: Origin = Origin::Csms;
    type Response = CertificateSignedResponse;
}

impl Confirmed for CertificateSignedRequest {}

/// Spec: OCPP 1.6 · Security (Whitepaper ed. 2) · CSMS → Charging Station · CALL.
#[derive(Clone, Debug, PartialEq, Serialize, Deserialize)]
pub struct CertificateSignedResponse {
    /// The `status` member.
    pub status: CertificateSignedStatus,
}

impl CertificateSignedResponse {
    /// Creates a `CertificateSignedResponse` from its required fields.
    #[must_use]
    pub fn new(status: CertificateSignedStatus) -> Self {
        Self { status }
    }
}

impl Validate for CertificateSignedResponse {
    fn validate_at(&self, path: &mut ValidationPath, out: &mut Violations) {
        path.push_key("status");
        Validate::validate_at(&self.status, path, out);
        path.pop();
    }
}

impl Sealed for CertificateSignedResponse {}
impl Response for CertificateSignedResponse {}

/// Spec: OCPP 1.6 · Core · CSMS → Charging Station · CALL.
#[derive(Clone, Debug, PartialEq, Serialize, Deserialize)]
pub struct ChangeAvailabilityRequest {
    /// The `connectorId` member.
    #[serde(rename = "connectorId")]
    pub connector_id: i32,
    /// The `type` member.
    pub r#type: AvailabilityType,
}

impl ChangeAvailabilityRequest {
    /// Creates a `ChangeAvailabilityRequest` from its required fields.
    #[must_use]
    pub fn new(connector_id: i32, r#type: AvailabilityType) -> Self {
        Self {
            connector_id,
            r#type,
        }
    }
}

impl Validate for ChangeAvailabilityRequest {
    fn validate_at(&self, path: &mut ValidationPath, out: &mut Violations) {
        path.push_key("type");
        Validate::validate_at(&self.r#type, path, out);
        path.pop();
    }
}

impl Sealed for ChangeAvailabilityRequest {}
impl Request for ChangeAvailabilityRequest {
    type Action = Action;
    const ACTION: Action = Action::ChangeAvailability;
    const KIND: MessageKind = MessageKind::Call;
    const ORIGIN: Origin = Origin::Csms;
    type Response = ChangeAvailabilityResponse;
}

impl Confirmed for ChangeAvailabilityRequest {}

/// Spec: OCPP 1.6 · Core · CSMS → Charging Station · CALL.
#[derive(Clone, Debug, PartialEq, Serialize, Deserialize)]
pub struct ChangeAvailabilityResponse {
    /// The `status` member.
    pub status: AvailabilityStatus,
}

impl ChangeAvailabilityResponse {
    /// Creates a `ChangeAvailabilityResponse` from its required fields.
    #[must_use]
    pub fn new(status: AvailabilityStatus) -> Self {
        Self { status }
    }
}

impl Validate for ChangeAvailabilityResponse {
    fn validate_at(&self, path: &mut ValidationPath, out: &mut Violations) {
        path.push_key("status");
        Validate::validate_at(&self.status, path, out);
        path.pop();
    }
}

impl Sealed for ChangeAvailabilityResponse {}
impl Response for ChangeAvailabilityResponse {}

/// Spec: OCPP 1.6 · Core · CSMS → Charging Station · CALL.
#[derive(Clone, Debug, PartialEq, Serialize, Deserialize)]
pub struct ChangeConfigurationRequest {
    /// The `key` member.
    pub key: String,
    /// The `value` member.
    pub value: String,
}

impl ChangeConfigurationRequest {
    /// Creates a `ChangeConfigurationRequest` from its required fields.
    #[must_use]
    pub fn new(key: impl Into<String>, value: impl Into<String>) -> Self {
        Self {
            key: key.into(),
            value: value.into(),
        }
    }
}

impl Validate for ChangeConfigurationRequest {
    fn validate_at(&self, path: &mut ValidationPath, out: &mut Violations) {
        path.push_key("key");
        validate::string(self.key.as_str(), None, Some(50), path, out);
        path.pop();
        path.push_key("value");
        validate::string(self.value.as_str(), None, Some(500), path, out);
        path.pop();
    }
}

impl Sealed for ChangeConfigurationRequest {}
impl Request for ChangeConfigurationRequest {
    type Action = Action;
    const ACTION: Action = Action::ChangeConfiguration;
    const KIND: MessageKind = MessageKind::Call;
    const ORIGIN: Origin = Origin::Csms;
    type Response = ChangeConfigurationResponse;
}

impl Confirmed for ChangeConfigurationRequest {}

/// Spec: OCPP 1.6 · Core · CSMS → Charging Station · CALL.
#[derive(Clone, Debug, PartialEq, Serialize, Deserialize)]
pub struct ChangeConfigurationResponse {
    /// The `status` member.
    pub status: ConfigurationStatus,
}

impl ChangeConfigurationResponse {
    /// Creates a `ChangeConfigurationResponse` from its required fields.
    #[must_use]
    pub fn new(status: ConfigurationStatus) -> Self {
        Self { status }
    }
}

impl Validate for ChangeConfigurationResponse {
    fn validate_at(&self, path: &mut ValidationPath, out: &mut Violations) {
        path.push_key("status");
        Validate::validate_at(&self.status, path, out);
        path.pop();
    }
}

impl Sealed for ChangeConfigurationResponse {}
impl Response for ChangeConfigurationResponse {}

/// Spec: OCPP 1.6 · Core · CSMS → Charging Station · CALL.
#[derive(Clone, Debug, Default, PartialEq, Serialize, Deserialize)]
pub struct ClearCacheRequest {}

impl ClearCacheRequest {
    /// Creates an empty `ClearCacheRequest`. Every field of this type is optional.
    #[must_use]
    pub fn new() -> Self {
        Self::default()
    }
}

impl Validate for ClearCacheRequest {
    fn validate_at(&self, _path: &mut ValidationPath, _out: &mut Violations) {}
}

impl Sealed for ClearCacheRequest {}
impl Request for ClearCacheRequest {
    type Action = Action;
    const ACTION: Action = Action::ClearCache;
    const KIND: MessageKind = MessageKind::Call;
    const ORIGIN: Origin = Origin::Csms;
    type Response = ClearCacheResponse;
}

impl Confirmed for ClearCacheRequest {}

/// Spec: OCPP 1.6 · Core · CSMS → Charging Station · CALL.
#[derive(Clone, Debug, PartialEq, Serialize, Deserialize)]
pub struct ClearCacheResponse {
    /// The `status` member.
    pub status: ClearCacheStatus,
}

impl ClearCacheResponse {
    /// Creates a `ClearCacheResponse` from its required fields.
    #[must_use]
    pub fn new(status: ClearCacheStatus) -> Self {
        Self { status }
    }
}

impl Validate for ClearCacheResponse {
    fn validate_at(&self, path: &mut ValidationPath, out: &mut Violations) {
        path.push_key("status");
        Validate::validate_at(&self.status, path, out);
        path.pop();
    }
}

impl Sealed for ClearCacheResponse {}
impl Response for ClearCacheResponse {}

/// Spec: OCPP 1.6 · Smart Charging · CSMS → Charging Station · CALL.
#[derive(Clone, Debug, Default, PartialEq, Serialize, Deserialize)]
pub struct ClearChargingProfileRequest {
    /// The `id` member.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub id: Option<i32>,
    /// The `connectorId` member.
    #[serde(rename = "connectorId")]
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub connector_id: Option<i32>,
    /// The `chargingProfilePurpose` member.
    #[serde(rename = "chargingProfilePurpose")]
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub charging_profile_purpose: Option<ChargingProfilePurpose>,
    /// The `stackLevel` member.
    #[serde(rename = "stackLevel")]
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub stack_level: Option<i32>,
}

impl ClearChargingProfileRequest {
    /// Creates an empty `ClearChargingProfileRequest`. Every field of this type is optional.
    #[must_use]
    pub fn new() -> Self {
        Self::default()
    }

    /// Sets the optional `id` field.
    #[must_use]
    pub fn with_id(mut self, value: i32) -> Self {
        self.id = Some(value);
        self
    }

    /// Sets the optional `connectorId` field.
    #[must_use]
    pub fn with_connector_id(mut self, value: i32) -> Self {
        self.connector_id = Some(value);
        self
    }

    /// Sets the optional `chargingProfilePurpose` field.
    #[must_use]
    pub fn with_charging_profile_purpose(mut self, value: ChargingProfilePurpose) -> Self {
        self.charging_profile_purpose = Some(value);
        self
    }

    /// Sets the optional `stackLevel` field.
    #[must_use]
    pub fn with_stack_level(mut self, value: i32) -> Self {
        self.stack_level = Some(value);
        self
    }
}

impl Validate for ClearChargingProfileRequest {
    fn validate_at(&self, path: &mut ValidationPath, out: &mut Violations) {
        path.push_key("chargingProfilePurpose");
        if let Some(value) = &self.charging_profile_purpose {
            Validate::validate_at(value, path, out);
        }
        path.pop();
    }
}

impl Sealed for ClearChargingProfileRequest {}
impl Request for ClearChargingProfileRequest {
    type Action = Action;
    const ACTION: Action = Action::ClearChargingProfile;
    const KIND: MessageKind = MessageKind::Call;
    const ORIGIN: Origin = Origin::Csms;
    type Response = ClearChargingProfileResponse;
}

impl Confirmed for ClearChargingProfileRequest {}

/// Spec: OCPP 1.6 · Smart Charging · CSMS → Charging Station · CALL.
#[derive(Clone, Debug, PartialEq, Serialize, Deserialize)]
pub struct ClearChargingProfileResponse {
    /// The `status` member.
    pub status: ClearChargingProfileStatus,
}

impl ClearChargingProfileResponse {
    /// Creates a `ClearChargingProfileResponse` from its required fields.
    #[must_use]
    pub fn new(status: ClearChargingProfileStatus) -> Self {
        Self { status }
    }
}

impl Validate for ClearChargingProfileResponse {
    fn validate_at(&self, path: &mut ValidationPath, out: &mut Violations) {
        path.push_key("status");
        Validate::validate_at(&self.status, path, out);
        path.pop();
    }
}

impl Sealed for ClearChargingProfileResponse {}
impl Response for ClearChargingProfileResponse {}

/// Spec: OCPP 1.6 · Core · either direction · CALL.
#[derive(Clone, Debug, PartialEq, Serialize, Deserialize)]
pub struct DataTransferRequest {
    /// The `vendorId` member.
    #[serde(rename = "vendorId")]
    pub vendor_id: String,
    /// The `messageId` member.
    #[serde(rename = "messageId")]
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub message_id: Option<String>,
    /// The `data` member.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub data: Option<String>,
}

impl DataTransferRequest {
    /// Creates a `DataTransferRequest` from its required fields.
    #[must_use]
    pub fn new(vendor_id: impl Into<String>) -> Self {
        Self {
            vendor_id: vendor_id.into(),
            message_id: None,
            data: None,
        }
    }

    /// Sets the optional `messageId` field.
    #[must_use]
    pub fn with_message_id(mut self, value: impl Into<String>) -> Self {
        self.message_id = Some(value.into());
        self
    }

    /// Sets the optional `data` field.
    #[must_use]
    pub fn with_data(mut self, value: impl Into<String>) -> Self {
        self.data = Some(value.into());
        self
    }
}

impl Validate for DataTransferRequest {
    fn validate_at(&self, path: &mut ValidationPath, out: &mut Violations) {
        path.push_key("vendorId");
        validate::string(self.vendor_id.as_str(), None, Some(255), path, out);
        path.pop();
        path.push_key("messageId");
        if let Some(value) = &self.message_id {
            validate::string(value.as_str(), None, Some(50), path, out);
        }
        path.pop();
    }
}

impl Sealed for DataTransferRequest {}
impl Request for DataTransferRequest {
    type Action = Action;
    const ACTION: Action = Action::DataTransfer;
    const KIND: MessageKind = MessageKind::Call;
    const ORIGIN: Origin = Origin::Both;
    type Response = DataTransferResponse;
}

impl Confirmed for DataTransferRequest {}

/// Spec: OCPP 1.6 · Core · either direction · CALL.
#[derive(Clone, Debug, PartialEq, Serialize, Deserialize)]
pub struct DataTransferResponse {
    /// The `status` member.
    pub status: DataTransferStatus,
    /// The `data` member.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub data: Option<String>,
}

impl DataTransferResponse {
    /// Creates a `DataTransferResponse` from its required fields.
    #[must_use]
    pub fn new(status: DataTransferStatus) -> Self {
        Self { status, data: None }
    }

    /// Sets the optional `data` field.
    #[must_use]
    pub fn with_data(mut self, value: impl Into<String>) -> Self {
        self.data = Some(value.into());
        self
    }
}

impl Validate for DataTransferResponse {
    fn validate_at(&self, path: &mut ValidationPath, out: &mut Violations) {
        path.push_key("status");
        Validate::validate_at(&self.status, path, out);
        path.pop();
    }
}

impl Sealed for DataTransferResponse {}
impl Response for DataTransferResponse {}

/// Spec: OCPP 1.6 · Security (Whitepaper ed. 2) · CSMS → Charging Station · CALL.
#[derive(Clone, Debug, PartialEq, Serialize, Deserialize)]
pub struct DeleteCertificateRequest {
    /// The `certificateHashData` member.
    #[serde(rename = "certificateHashData")]
    pub certificate_hash_data: CertificateHashData,
}

impl DeleteCertificateRequest {
    /// Creates a `DeleteCertificateRequest` from its required fields.
    #[must_use]
    pub fn new(certificate_hash_data: CertificateHashData) -> Self {
        Self {
            certificate_hash_data,
        }
    }
}

impl Validate for DeleteCertificateRequest {
    fn validate_at(&self, path: &mut ValidationPath, out: &mut Violations) {
        path.push_key("certificateHashData");
        Validate::validate_at(&self.certificate_hash_data, path, out);
        path.pop();
    }
}

impl Sealed for DeleteCertificateRequest {}
impl Request for DeleteCertificateRequest {
    type Action = Action;
    const ACTION: Action = Action::DeleteCertificate;
    const KIND: MessageKind = MessageKind::Call;
    const ORIGIN: Origin = Origin::Csms;
    type Response = DeleteCertificateResponse;
}

impl Confirmed for DeleteCertificateRequest {}

/// Spec: OCPP 1.6 · Security (Whitepaper ed. 2) · CSMS → Charging Station · CALL.
#[derive(Clone, Debug, PartialEq, Serialize, Deserialize)]
pub struct DeleteCertificateResponse {
    /// The `status` member.
    pub status: DeleteCertificateStatus,
}

impl DeleteCertificateResponse {
    /// Creates a `DeleteCertificateResponse` from its required fields.
    #[must_use]
    pub fn new(status: DeleteCertificateStatus) -> Self {
        Self { status }
    }
}

impl Validate for DeleteCertificateResponse {
    fn validate_at(&self, path: &mut ValidationPath, out: &mut Violations) {
        path.push_key("status");
        Validate::validate_at(&self.status, path, out);
        path.pop();
    }
}

impl Sealed for DeleteCertificateResponse {}
impl Response for DeleteCertificateResponse {}

/// Spec: OCPP 1.6 · Firmware Management · Charging Station → CSMS · CALL.
#[derive(Clone, Debug, PartialEq, Serialize, Deserialize)]
pub struct DiagnosticsStatusNotificationRequest {
    /// The `status` member.
    pub status: DiagnosticsStatus,
}

impl DiagnosticsStatusNotificationRequest {
    /// Creates a `DiagnosticsStatusNotificationRequest` from its required fields.
    #[must_use]
    pub fn new(status: DiagnosticsStatus) -> Self {
        Self { status }
    }
}

impl Validate for DiagnosticsStatusNotificationRequest {
    fn validate_at(&self, path: &mut ValidationPath, out: &mut Violations) {
        path.push_key("status");
        Validate::validate_at(&self.status, path, out);
        path.pop();
    }
}

impl Sealed for DiagnosticsStatusNotificationRequest {}
impl Request for DiagnosticsStatusNotificationRequest {
    type Action = Action;
    const ACTION: Action = Action::DiagnosticsStatusNotification;
    const KIND: MessageKind = MessageKind::Call;
    const ORIGIN: Origin = Origin::ChargingStation;
    type Response = DiagnosticsStatusNotificationResponse;
}

impl Confirmed for DiagnosticsStatusNotificationRequest {}

/// Spec: OCPP 1.6 · Firmware Management · Charging Station → CSMS · CALL.
#[derive(Clone, Debug, Default, PartialEq, Serialize, Deserialize)]
pub struct DiagnosticsStatusNotificationResponse {}

impl DiagnosticsStatusNotificationResponse {
    /// Creates an empty `DiagnosticsStatusNotificationResponse`. Every field of this type is optional.
    #[must_use]
    pub fn new() -> Self {
        Self::default()
    }
}

impl Validate for DiagnosticsStatusNotificationResponse {
    fn validate_at(&self, _path: &mut ValidationPath, _out: &mut Violations) {}
}

impl Sealed for DiagnosticsStatusNotificationResponse {}
impl Response for DiagnosticsStatusNotificationResponse {}

/// Spec: OCPP 1.6 · Security (Whitepaper ed. 2) · CSMS → Charging Station · CALL.
#[derive(Clone, Debug, PartialEq, Serialize, Deserialize)]
pub struct ExtendedTriggerMessageRequest {
    /// The `requestedMessage` member.
    #[serde(rename = "requestedMessage")]
    pub requested_message: ExtendedMessageTrigger,
    /// The `connectorId` member.
    #[serde(rename = "connectorId")]
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub connector_id: Option<i32>,
}

impl ExtendedTriggerMessageRequest {
    /// Creates a `ExtendedTriggerMessageRequest` from its required fields.
    #[must_use]
    pub fn new(requested_message: ExtendedMessageTrigger) -> Self {
        Self {
            requested_message,
            connector_id: None,
        }
    }

    /// Sets the optional `connectorId` field.
    #[must_use]
    pub fn with_connector_id(mut self, value: i32) -> Self {
        self.connector_id = Some(value);
        self
    }
}

impl Validate for ExtendedTriggerMessageRequest {
    fn validate_at(&self, path: &mut ValidationPath, out: &mut Violations) {
        path.push_key("requestedMessage");
        Validate::validate_at(&self.requested_message, path, out);
        path.pop();
    }
}

impl Sealed for ExtendedTriggerMessageRequest {}
impl Request for ExtendedTriggerMessageRequest {
    type Action = Action;
    const ACTION: Action = Action::ExtendedTriggerMessage;
    const KIND: MessageKind = MessageKind::Call;
    const ORIGIN: Origin = Origin::Csms;
    type Response = ExtendedTriggerMessageResponse;
}

impl Confirmed for ExtendedTriggerMessageRequest {}

/// Spec: OCPP 1.6 · Security (Whitepaper ed. 2) · CSMS → Charging Station · CALL.
#[derive(Clone, Debug, PartialEq, Serialize, Deserialize)]
pub struct ExtendedTriggerMessageResponse {
    /// The `status` member.
    pub status: TriggerMessageStatus,
}

impl ExtendedTriggerMessageResponse {
    /// Creates a `ExtendedTriggerMessageResponse` from its required fields.
    #[must_use]
    pub fn new(status: TriggerMessageStatus) -> Self {
        Self { status }
    }
}

impl Validate for ExtendedTriggerMessageResponse {
    fn validate_at(&self, path: &mut ValidationPath, out: &mut Violations) {
        path.push_key("status");
        Validate::validate_at(&self.status, path, out);
        path.pop();
    }
}

impl Sealed for ExtendedTriggerMessageResponse {}
impl Response for ExtendedTriggerMessageResponse {}

/// Spec: OCPP 1.6 · Firmware Management · Charging Station → CSMS · CALL.
#[derive(Clone, Debug, PartialEq, Serialize, Deserialize)]
pub struct FirmwareStatusNotificationRequest {
    /// The `status` member.
    pub status: FirmwareStatus,
}

impl FirmwareStatusNotificationRequest {
    /// Creates a `FirmwareStatusNotificationRequest` from its required fields.
    #[must_use]
    pub fn new(status: FirmwareStatus) -> Self {
        Self { status }
    }
}

impl Validate for FirmwareStatusNotificationRequest {
    fn validate_at(&self, path: &mut ValidationPath, out: &mut Violations) {
        path.push_key("status");
        Validate::validate_at(&self.status, path, out);
        path.pop();
    }
}

impl Sealed for FirmwareStatusNotificationRequest {}
impl Request for FirmwareStatusNotificationRequest {
    type Action = Action;
    const ACTION: Action = Action::FirmwareStatusNotification;
    const KIND: MessageKind = MessageKind::Call;
    const ORIGIN: Origin = Origin::ChargingStation;
    type Response = FirmwareStatusNotificationResponse;
}

impl Confirmed for FirmwareStatusNotificationRequest {}

/// Spec: OCPP 1.6 · Firmware Management · Charging Station → CSMS · CALL.
#[derive(Clone, Debug, Default, PartialEq, Serialize, Deserialize)]
pub struct FirmwareStatusNotificationResponse {}

impl FirmwareStatusNotificationResponse {
    /// Creates an empty `FirmwareStatusNotificationResponse`. Every field of this type is optional.
    #[must_use]
    pub fn new() -> Self {
        Self::default()
    }
}

impl Validate for FirmwareStatusNotificationResponse {
    fn validate_at(&self, _path: &mut ValidationPath, _out: &mut Violations) {}
}

impl Sealed for FirmwareStatusNotificationResponse {}
impl Response for FirmwareStatusNotificationResponse {}

/// Spec: OCPP 1.6 · Smart Charging · CSMS → Charging Station · CALL.
#[derive(Clone, Debug, PartialEq, Serialize, Deserialize)]
pub struct GetCompositeScheduleRequest {
    /// The `connectorId` member.
    #[serde(rename = "connectorId")]
    pub connector_id: i32,
    /// The `duration` member.
    pub duration: i32,
    /// The `chargingRateUnit` member.
    #[serde(rename = "chargingRateUnit")]
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub charging_rate_unit: Option<ChargingRateUnit>,
}

impl GetCompositeScheduleRequest {
    /// Creates a `GetCompositeScheduleRequest` from its required fields.
    #[must_use]
    pub fn new(connector_id: i32, duration: i32) -> Self {
        Self {
            connector_id,
            duration,
            charging_rate_unit: None,
        }
    }

    /// Sets the optional `chargingRateUnit` field.
    #[must_use]
    pub fn with_charging_rate_unit(mut self, value: ChargingRateUnit) -> Self {
        self.charging_rate_unit = Some(value);
        self
    }
}

impl Validate for GetCompositeScheduleRequest {
    fn validate_at(&self, path: &mut ValidationPath, out: &mut Violations) {
        path.push_key("chargingRateUnit");
        if let Some(value) = &self.charging_rate_unit {
            Validate::validate_at(value, path, out);
        }
        path.pop();
    }
}

impl Sealed for GetCompositeScheduleRequest {}
impl Request for GetCompositeScheduleRequest {
    type Action = Action;
    const ACTION: Action = Action::GetCompositeSchedule;
    const KIND: MessageKind = MessageKind::Call;
    const ORIGIN: Origin = Origin::Csms;
    type Response = GetCompositeScheduleResponse;
}

impl Confirmed for GetCompositeScheduleRequest {}

/// Spec: OCPP 1.6 · Smart Charging · CSMS → Charging Station · CALL.
#[derive(Clone, Debug, PartialEq, Serialize, Deserialize)]
pub struct GetCompositeScheduleResponse {
    /// The `status` member.
    pub status: GetCompositeScheduleStatus,
    /// The `connectorId` member.
    #[serde(rename = "connectorId")]
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub connector_id: Option<i32>,
    /// The `scheduleStart` member.
    #[serde(rename = "scheduleStart")]
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub schedule_start: Option<crate::types::DateTime>,
    /// The `chargingSchedule` member.
    #[serde(rename = "chargingSchedule")]
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub charging_schedule: Option<ChargingSchedule>,
}

impl GetCompositeScheduleResponse {
    /// Creates a `GetCompositeScheduleResponse` from its required fields.
    #[must_use]
    pub fn new(status: GetCompositeScheduleStatus) -> Self {
        Self {
            status,
            connector_id: None,
            schedule_start: None,
            charging_schedule: None,
        }
    }

    /// Sets the optional `connectorId` field.
    #[must_use]
    pub fn with_connector_id(mut self, value: i32) -> Self {
        self.connector_id = Some(value);
        self
    }

    /// Sets the optional `scheduleStart` field.
    #[must_use]
    pub fn with_schedule_start(mut self, value: impl Into<crate::types::DateTime>) -> Self {
        self.schedule_start = Some(value.into());
        self
    }

    /// Sets the optional `chargingSchedule` field.
    #[must_use]
    pub fn with_charging_schedule(mut self, value: ChargingSchedule) -> Self {
        self.charging_schedule = Some(value);
        self
    }
}

impl Validate for GetCompositeScheduleResponse {
    fn validate_at(&self, path: &mut ValidationPath, out: &mut Violations) {
        path.push_key("status");
        Validate::validate_at(&self.status, path, out);
        path.pop();
        path.push_key("chargingSchedule");
        if let Some(value) = &self.charging_schedule {
            Validate::validate_at(value, path, out);
        }
        path.pop();
    }
}

impl Sealed for GetCompositeScheduleResponse {}
impl Response for GetCompositeScheduleResponse {}

/// Spec: OCPP 1.6 · Core · CSMS → Charging Station · CALL.
#[derive(Clone, Debug, Default, PartialEq, Serialize, Deserialize)]
pub struct GetConfigurationRequest {
    /// The `key` member.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub key: Option<Vec<String>>,
}

impl GetConfigurationRequest {
    /// Creates an empty `GetConfigurationRequest`. Every field of this type is optional.
    #[must_use]
    pub fn new() -> Self {
        Self::default()
    }

    /// Sets the optional `key` field.
    #[must_use]
    pub fn with_key(mut self, value: impl IntoIterator<Item = impl Into<String>>) -> Self {
        self.key = Some(value.into_iter().map(Into::into).collect());
        self
    }
}

impl Validate for GetConfigurationRequest {
    fn validate_at(&self, path: &mut ValidationPath, out: &mut Violations) {
        path.push_key("key");
        if let Some(value) = &self.key {
            for (index, item) in value.iter().enumerate() {
                path.push_index(index);
                validate::string(item.as_str(), None, Some(50), path, out);
                path.pop();
            }
        }
        path.pop();
    }
}

impl Sealed for GetConfigurationRequest {}
impl Request for GetConfigurationRequest {
    type Action = Action;
    const ACTION: Action = Action::GetConfiguration;
    const KIND: MessageKind = MessageKind::Call;
    const ORIGIN: Origin = Origin::Csms;
    type Response = GetConfigurationResponse;
}

impl Confirmed for GetConfigurationRequest {}

/// Spec: OCPP 1.6 · Core · CSMS → Charging Station · CALL.
#[derive(Clone, Debug, Default, PartialEq, Serialize, Deserialize)]
pub struct GetConfigurationResponse {
    /// The `configurationKey` member.
    #[serde(rename = "configurationKey")]
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub configuration_key: Option<Vec<KeyValue>>,
    /// The `unknownKey` member.
    #[serde(rename = "unknownKey")]
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub unknown_key: Option<Vec<String>>,
}

impl GetConfigurationResponse {
    /// Creates an empty `GetConfigurationResponse`. Every field of this type is optional.
    #[must_use]
    pub fn new() -> Self {
        Self::default()
    }

    /// Sets the optional `configurationKey` field.
    #[must_use]
    pub fn with_configuration_key(mut self, value: Vec<KeyValue>) -> Self {
        self.configuration_key = Some(value);
        self
    }

    /// Sets the optional `unknownKey` field.
    #[must_use]
    pub fn with_unknown_key(mut self, value: impl IntoIterator<Item = impl Into<String>>) -> Self {
        self.unknown_key = Some(value.into_iter().map(Into::into).collect());
        self
    }
}

impl Validate for GetConfigurationResponse {
    fn validate_at(&self, path: &mut ValidationPath, out: &mut Violations) {
        path.push_key("configurationKey");
        if let Some(value) = &self.configuration_key {
            for (index, item) in value.iter().enumerate() {
                path.push_index(index);
                Validate::validate_at(item, path, out);
                path.pop();
            }
        }
        path.pop();
        path.push_key("unknownKey");
        if let Some(value) = &self.unknown_key {
            for (index, item) in value.iter().enumerate() {
                path.push_index(index);
                validate::string(item.as_str(), None, Some(50), path, out);
                path.pop();
            }
        }
        path.pop();
    }
}

impl Sealed for GetConfigurationResponse {}
impl Response for GetConfigurationResponse {}

/// Spec: OCPP 1.6 · Firmware Management · CSMS → Charging Station · CALL.
#[derive(Clone, Debug, PartialEq, Serialize, Deserialize)]
pub struct GetDiagnosticsRequest {
    /// The `location` member.
    pub location: String,
    /// The `retries` member.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub retries: Option<i32>,
    /// The `retryInterval` member.
    #[serde(rename = "retryInterval")]
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub retry_interval: Option<i32>,
    /// The `startTime` member.
    #[serde(rename = "startTime")]
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub start_time: Option<crate::types::DateTime>,
    /// The `stopTime` member.
    #[serde(rename = "stopTime")]
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub stop_time: Option<crate::types::DateTime>,
}

impl GetDiagnosticsRequest {
    /// Creates a `GetDiagnosticsRequest` from its required fields.
    #[must_use]
    pub fn new(location: impl Into<String>) -> Self {
        Self {
            location: location.into(),
            retries: None,
            retry_interval: None,
            start_time: None,
            stop_time: None,
        }
    }

    /// Sets the optional `retries` field.
    #[must_use]
    pub fn with_retries(mut self, value: i32) -> Self {
        self.retries = Some(value);
        self
    }

    /// Sets the optional `retryInterval` field.
    #[must_use]
    pub fn with_retry_interval(mut self, value: i32) -> Self {
        self.retry_interval = Some(value);
        self
    }

    /// Sets the optional `startTime` field.
    #[must_use]
    pub fn with_start_time(mut self, value: impl Into<crate::types::DateTime>) -> Self {
        self.start_time = Some(value.into());
        self
    }

    /// Sets the optional `stopTime` field.
    #[must_use]
    pub fn with_stop_time(mut self, value: impl Into<crate::types::DateTime>) -> Self {
        self.stop_time = Some(value.into());
        self
    }
}

impl Validate for GetDiagnosticsRequest {
    fn validate_at(&self, _path: &mut ValidationPath, _out: &mut Violations) {}
}

impl Sealed for GetDiagnosticsRequest {}
impl Request for GetDiagnosticsRequest {
    type Action = Action;
    const ACTION: Action = Action::GetDiagnostics;
    const KIND: MessageKind = MessageKind::Call;
    const ORIGIN: Origin = Origin::Csms;
    type Response = GetDiagnosticsResponse;
}

impl Confirmed for GetDiagnosticsRequest {}

/// Spec: OCPP 1.6 · Firmware Management · CSMS → Charging Station · CALL.
#[derive(Clone, Debug, Default, PartialEq, Serialize, Deserialize)]
pub struct GetDiagnosticsResponse {
    /// The `fileName` member.
    #[serde(rename = "fileName")]
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub file_name: Option<String>,
}

impl GetDiagnosticsResponse {
    /// Creates an empty `GetDiagnosticsResponse`. Every field of this type is optional.
    #[must_use]
    pub fn new() -> Self {
        Self::default()
    }

    /// Sets the optional `fileName` field.
    #[must_use]
    pub fn with_file_name(mut self, value: impl Into<String>) -> Self {
        self.file_name = Some(value.into());
        self
    }
}

impl Validate for GetDiagnosticsResponse {
    fn validate_at(&self, path: &mut ValidationPath, out: &mut Violations) {
        path.push_key("fileName");
        if let Some(value) = &self.file_name {
            validate::string(value.as_str(), None, Some(255), path, out);
        }
        path.pop();
    }
}

impl Sealed for GetDiagnosticsResponse {}
impl Response for GetDiagnosticsResponse {}

/// Spec: OCPP 1.6 · Security (Whitepaper ed. 2) · CSMS → Charging Station · CALL.
#[derive(Clone, Debug, PartialEq, Serialize, Deserialize)]
pub struct GetInstalledCertificateIdsRequest {
    /// The `certificateType` member.
    #[serde(rename = "certificateType")]
    pub certificate_type: CertificateUse,
}

impl GetInstalledCertificateIdsRequest {
    /// Creates a `GetInstalledCertificateIdsRequest` from its required fields.
    #[must_use]
    pub fn new(certificate_type: CertificateUse) -> Self {
        Self { certificate_type }
    }
}

impl Validate for GetInstalledCertificateIdsRequest {
    fn validate_at(&self, path: &mut ValidationPath, out: &mut Violations) {
        path.push_key("certificateType");
        Validate::validate_at(&self.certificate_type, path, out);
        path.pop();
    }
}

impl Sealed for GetInstalledCertificateIdsRequest {}
impl Request for GetInstalledCertificateIdsRequest {
    type Action = Action;
    const ACTION: Action = Action::GetInstalledCertificateIds;
    const KIND: MessageKind = MessageKind::Call;
    const ORIGIN: Origin = Origin::Csms;
    type Response = GetInstalledCertificateIdsResponse;
}

impl Confirmed for GetInstalledCertificateIdsRequest {}

/// Spec: OCPP 1.6 · Security (Whitepaper ed. 2) · CSMS → Charging Station · CALL.
#[derive(Clone, Debug, PartialEq, Serialize, Deserialize)]
pub struct GetInstalledCertificateIdsResponse {
    /// The `status` member.
    pub status: GetInstalledCertificateStatus,
    /// The `certificateHashData` member.
    #[serde(rename = "certificateHashData")]
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub certificate_hash_data: Option<Vec<CertificateHashData>>,
}

impl GetInstalledCertificateIdsResponse {
    /// Creates a `GetInstalledCertificateIdsResponse` from its required fields.
    #[must_use]
    pub fn new(status: GetInstalledCertificateStatus) -> Self {
        Self {
            status,
            certificate_hash_data: None,
        }
    }

    /// Sets the optional `certificateHashData` field.
    #[must_use]
    pub fn with_certificate_hash_data(mut self, value: Vec<CertificateHashData>) -> Self {
        self.certificate_hash_data = Some(value);
        self
    }
}

impl Validate for GetInstalledCertificateIdsResponse {
    fn validate_at(&self, path: &mut ValidationPath, out: &mut Violations) {
        path.push_key("status");
        Validate::validate_at(&self.status, path, out);
        path.pop();
        path.push_key("certificateHashData");
        if let Some(value) = &self.certificate_hash_data {
            validate::list_len(value.len(), Some(1), None, path, out);
            for (index, item) in value.iter().enumerate() {
                path.push_index(index);
                Validate::validate_at(item, path, out);
                path.pop();
            }
        }
        path.pop();
    }
}

impl Sealed for GetInstalledCertificateIdsResponse {}
impl Response for GetInstalledCertificateIdsResponse {}

/// Spec: OCPP 1.6 · Local Auth List Management · CSMS → Charging Station · CALL.
#[derive(Clone, Debug, Default, PartialEq, Serialize, Deserialize)]
pub struct GetLocalListVersionRequest {}

impl GetLocalListVersionRequest {
    /// Creates an empty `GetLocalListVersionRequest`. Every field of this type is optional.
    #[must_use]
    pub fn new() -> Self {
        Self::default()
    }
}

impl Validate for GetLocalListVersionRequest {
    fn validate_at(&self, _path: &mut ValidationPath, _out: &mut Violations) {}
}

impl Sealed for GetLocalListVersionRequest {}
impl Request for GetLocalListVersionRequest {
    type Action = Action;
    const ACTION: Action = Action::GetLocalListVersion;
    const KIND: MessageKind = MessageKind::Call;
    const ORIGIN: Origin = Origin::Csms;
    type Response = GetLocalListVersionResponse;
}

impl Confirmed for GetLocalListVersionRequest {}

/// Spec: OCPP 1.6 · Local Auth List Management · CSMS → Charging Station · CALL.
#[derive(Clone, Debug, PartialEq, Serialize, Deserialize)]
pub struct GetLocalListVersionResponse {
    /// The `listVersion` member.
    #[serde(rename = "listVersion")]
    pub list_version: i32,
}

impl GetLocalListVersionResponse {
    /// Creates a `GetLocalListVersionResponse` from its required fields.
    #[must_use]
    pub fn new(list_version: i32) -> Self {
        Self { list_version }
    }
}

impl Validate for GetLocalListVersionResponse {
    fn validate_at(&self, _path: &mut ValidationPath, _out: &mut Violations) {}
}

impl Sealed for GetLocalListVersionResponse {}
impl Response for GetLocalListVersionResponse {}

/// Spec: OCPP 1.6 · Security (Whitepaper ed. 2) · CSMS → Charging Station · CALL.
#[derive(Clone, Debug, PartialEq, Serialize, Deserialize)]
pub struct GetLogRequest {
    /// The `log` member.
    pub log: LogParameters,
    /// The `logType` member.
    #[serde(rename = "logType")]
    pub log_type: LogType,
    /// The `requestId` member.
    #[serde(rename = "requestId")]
    pub request_id: i32,
    /// The `retries` member.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub retries: Option<i32>,
    /// The `retryInterval` member.
    #[serde(rename = "retryInterval")]
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub retry_interval: Option<i32>,
}

impl GetLogRequest {
    /// Creates a `GetLogRequest` from its required fields.
    #[must_use]
    pub fn new(log: LogParameters, log_type: LogType, request_id: i32) -> Self {
        Self {
            log,
            log_type,
            request_id,
            retries: None,
            retry_interval: None,
        }
    }

    /// Sets the optional `retries` field.
    #[must_use]
    pub fn with_retries(mut self, value: i32) -> Self {
        self.retries = Some(value);
        self
    }

    /// Sets the optional `retryInterval` field.
    #[must_use]
    pub fn with_retry_interval(mut self, value: i32) -> Self {
        self.retry_interval = Some(value);
        self
    }
}

impl Validate for GetLogRequest {
    fn validate_at(&self, path: &mut ValidationPath, out: &mut Violations) {
        path.push_key("log");
        Validate::validate_at(&self.log, path, out);
        path.pop();
        path.push_key("logType");
        Validate::validate_at(&self.log_type, path, out);
        path.pop();
    }
}

impl Sealed for GetLogRequest {}
impl Request for GetLogRequest {
    type Action = Action;
    const ACTION: Action = Action::GetLog;
    const KIND: MessageKind = MessageKind::Call;
    const ORIGIN: Origin = Origin::Csms;
    type Response = GetLogResponse;
}

impl Confirmed for GetLogRequest {}

/// Spec: OCPP 1.6 · Security (Whitepaper ed. 2) · CSMS → Charging Station · CALL.
#[derive(Clone, Debug, PartialEq, Serialize, Deserialize)]
pub struct GetLogResponse {
    /// The `status` member.
    pub status: LogStatus,
    /// The `filename` member.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub filename: Option<String>,
}

impl GetLogResponse {
    /// Creates a `GetLogResponse` from its required fields.
    #[must_use]
    pub fn new(status: LogStatus) -> Self {
        Self {
            status,
            filename: None,
        }
    }

    /// Sets the optional `filename` field.
    #[must_use]
    pub fn with_filename(mut self, value: impl Into<String>) -> Self {
        self.filename = Some(value.into());
        self
    }
}

impl Validate for GetLogResponse {
    fn validate_at(&self, path: &mut ValidationPath, out: &mut Violations) {
        path.push_key("status");
        Validate::validate_at(&self.status, path, out);
        path.pop();
        path.push_key("filename");
        if let Some(value) = &self.filename {
            validate::string(value.as_str(), None, Some(255), path, out);
        }
        path.pop();
    }
}

impl Sealed for GetLogResponse {}
impl Response for GetLogResponse {}

/// Spec: OCPP 1.6 · Core · Charging Station → CSMS · CALL.
#[derive(Clone, Debug, Default, PartialEq, Serialize, Deserialize)]
pub struct HeartbeatRequest {}

impl HeartbeatRequest {
    /// Creates an empty `HeartbeatRequest`. Every field of this type is optional.
    #[must_use]
    pub fn new() -> Self {
        Self::default()
    }
}

impl Validate for HeartbeatRequest {
    fn validate_at(&self, _path: &mut ValidationPath, _out: &mut Violations) {}
}

impl Sealed for HeartbeatRequest {}
impl Request for HeartbeatRequest {
    type Action = Action;
    const ACTION: Action = Action::Heartbeat;
    const KIND: MessageKind = MessageKind::Call;
    const ORIGIN: Origin = Origin::ChargingStation;
    type Response = HeartbeatResponse;
}

impl Confirmed for HeartbeatRequest {}

/// Spec: OCPP 1.6 · Core · Charging Station → CSMS · CALL.
#[derive(Clone, Debug, PartialEq, Serialize, Deserialize)]
pub struct HeartbeatResponse {
    /// The `currentTime` member.
    #[serde(rename = "currentTime")]
    pub current_time: crate::types::DateTime,
}

impl HeartbeatResponse {
    /// Creates a `HeartbeatResponse` from its required fields.
    #[must_use]
    pub fn new(current_time: impl Into<crate::types::DateTime>) -> Self {
        Self {
            current_time: current_time.into(),
        }
    }
}

impl Validate for HeartbeatResponse {
    fn validate_at(&self, _path: &mut ValidationPath, _out: &mut Violations) {}
}

impl Sealed for HeartbeatResponse {}
impl Response for HeartbeatResponse {}

/// Spec: OCPP 1.6 · Security (Whitepaper ed. 2) · CSMS → Charging Station · CALL.
#[derive(Clone, Debug, PartialEq, Serialize, Deserialize)]
pub struct InstallCertificateRequest {
    /// The `certificateType` member.
    #[serde(rename = "certificateType")]
    pub certificate_type: CertificateUse,
    /// The `certificate` member.
    pub certificate: String,
}

impl InstallCertificateRequest {
    /// Creates a `InstallCertificateRequest` from its required fields.
    #[must_use]
    pub fn new(certificate_type: CertificateUse, certificate: impl Into<String>) -> Self {
        Self {
            certificate_type,
            certificate: certificate.into(),
        }
    }
}

impl Validate for InstallCertificateRequest {
    fn validate_at(&self, path: &mut ValidationPath, out: &mut Violations) {
        path.push_key("certificateType");
        Validate::validate_at(&self.certificate_type, path, out);
        path.pop();
        path.push_key("certificate");
        validate::string(self.certificate.as_str(), None, Some(5500), path, out);
        path.pop();
    }
}

impl Sealed for InstallCertificateRequest {}
impl Request for InstallCertificateRequest {
    type Action = Action;
    const ACTION: Action = Action::InstallCertificate;
    const KIND: MessageKind = MessageKind::Call;
    const ORIGIN: Origin = Origin::Csms;
    type Response = InstallCertificateResponse;
}

impl Confirmed for InstallCertificateRequest {}

/// Spec: OCPP 1.6 · Security (Whitepaper ed. 2) · CSMS → Charging Station · CALL.
#[derive(Clone, Debug, PartialEq, Serialize, Deserialize)]
pub struct InstallCertificateResponse {
    /// The `status` member.
    pub status: InstallCertificateStatus,
}

impl InstallCertificateResponse {
    /// Creates a `InstallCertificateResponse` from its required fields.
    #[must_use]
    pub fn new(status: InstallCertificateStatus) -> Self {
        Self { status }
    }
}

impl Validate for InstallCertificateResponse {
    fn validate_at(&self, path: &mut ValidationPath, out: &mut Violations) {
        path.push_key("status");
        Validate::validate_at(&self.status, path, out);
        path.pop();
    }
}

impl Sealed for InstallCertificateResponse {}
impl Response for InstallCertificateResponse {}

/// Spec: OCPP 1.6 · Security (Whitepaper ed. 2) · Charging Station → CSMS · CALL.
#[derive(Clone, Debug, PartialEq, Serialize, Deserialize)]
pub struct LogStatusNotificationRequest {
    /// The `status` member.
    pub status: UploadLogStatus,
    /// The `requestId` member.
    #[serde(rename = "requestId")]
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub request_id: Option<i32>,
}

impl LogStatusNotificationRequest {
    /// Creates a `LogStatusNotificationRequest` from its required fields.
    #[must_use]
    pub fn new(status: UploadLogStatus) -> Self {
        Self {
            status,
            request_id: None,
        }
    }

    /// Sets the optional `requestId` field.
    #[must_use]
    pub fn with_request_id(mut self, value: i32) -> Self {
        self.request_id = Some(value);
        self
    }
}

impl Validate for LogStatusNotificationRequest {
    fn validate_at(&self, path: &mut ValidationPath, out: &mut Violations) {
        path.push_key("status");
        Validate::validate_at(&self.status, path, out);
        path.pop();
    }
}

impl Sealed for LogStatusNotificationRequest {}
impl Request for LogStatusNotificationRequest {
    type Action = Action;
    const ACTION: Action = Action::LogStatusNotification;
    const KIND: MessageKind = MessageKind::Call;
    const ORIGIN: Origin = Origin::ChargingStation;
    type Response = LogStatusNotificationResponse;
}

impl Confirmed for LogStatusNotificationRequest {}

/// Spec: OCPP 1.6 · Security (Whitepaper ed. 2) · Charging Station → CSMS · CALL.
#[derive(Clone, Debug, Default, PartialEq, Serialize, Deserialize)]
pub struct LogStatusNotificationResponse {}

impl LogStatusNotificationResponse {
    /// Creates an empty `LogStatusNotificationResponse`. Every field of this type is optional.
    #[must_use]
    pub fn new() -> Self {
        Self::default()
    }
}

impl Validate for LogStatusNotificationResponse {
    fn validate_at(&self, _path: &mut ValidationPath, _out: &mut Violations) {}
}

impl Sealed for LogStatusNotificationResponse {}
impl Response for LogStatusNotificationResponse {}

/// Spec: OCPP 1.6 · Core · Charging Station → CSMS · CALL.
#[derive(Clone, Debug, PartialEq, Serialize, Deserialize)]
pub struct MeterValuesRequest {
    /// The `connectorId` member.
    #[serde(rename = "connectorId")]
    pub connector_id: i32,
    /// The `meterValue` member.
    #[serde(rename = "meterValue")]
    pub meter_value: Vec<MeterValue>,
    /// The `transactionId` member.
    #[serde(rename = "transactionId")]
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub transaction_id: Option<i32>,
}

impl MeterValuesRequest {
    /// Creates a `MeterValuesRequest` from its required fields.
    #[must_use]
    pub fn new(connector_id: i32, meter_value: Vec<MeterValue>) -> Self {
        Self {
            connector_id,
            meter_value,
            transaction_id: None,
        }
    }

    /// Sets the optional `transactionId` field.
    #[must_use]
    pub fn with_transaction_id(mut self, value: i32) -> Self {
        self.transaction_id = Some(value);
        self
    }
}

impl Validate for MeterValuesRequest {
    fn validate_at(&self, path: &mut ValidationPath, out: &mut Violations) {
        path.push_key("meterValue");
        validate::list_len(self.meter_value.len(), Some(1), None, path, out);
        for (index, item) in self.meter_value.iter().enumerate() {
            path.push_index(index);
            Validate::validate_at(item, path, out);
            path.pop();
        }
        path.pop();
    }
}

impl Sealed for MeterValuesRequest {}
impl Request for MeterValuesRequest {
    type Action = Action;
    const ACTION: Action = Action::MeterValues;
    const KIND: MessageKind = MessageKind::Call;
    const ORIGIN: Origin = Origin::ChargingStation;
    type Response = MeterValuesResponse;
}

impl Confirmed for MeterValuesRequest {}

/// Spec: OCPP 1.6 · Core · Charging Station → CSMS · CALL.
#[derive(Clone, Debug, Default, PartialEq, Serialize, Deserialize)]
pub struct MeterValuesResponse {}

impl MeterValuesResponse {
    /// Creates an empty `MeterValuesResponse`. Every field of this type is optional.
    #[must_use]
    pub fn new() -> Self {
        Self::default()
    }
}

impl Validate for MeterValuesResponse {
    fn validate_at(&self, _path: &mut ValidationPath, _out: &mut Violations) {}
}

impl Sealed for MeterValuesResponse {}
impl Response for MeterValuesResponse {}

/// Spec: OCPP 1.6 · Core · CSMS → Charging Station · CALL.
#[derive(Clone, Debug, PartialEq, Serialize, Deserialize)]
pub struct RemoteStartTransactionRequest {
    /// The `idTag` member.
    #[serde(rename = "idTag")]
    pub id_tag: String,
    /// The `connectorId` member.
    #[serde(rename = "connectorId")]
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub connector_id: Option<i32>,
    /// The `chargingProfile` member.
    #[serde(rename = "chargingProfile")]
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub charging_profile: Option<ChargingProfile>,
}

impl RemoteStartTransactionRequest {
    /// Creates a `RemoteStartTransactionRequest` from its required fields.
    #[must_use]
    pub fn new(id_tag: impl Into<String>) -> Self {
        Self {
            id_tag: id_tag.into(),
            connector_id: None,
            charging_profile: None,
        }
    }

    /// Sets the optional `connectorId` field.
    #[must_use]
    pub fn with_connector_id(mut self, value: i32) -> Self {
        self.connector_id = Some(value);
        self
    }

    /// Sets the optional `chargingProfile` field.
    #[must_use]
    pub fn with_charging_profile(mut self, value: ChargingProfile) -> Self {
        self.charging_profile = Some(value);
        self
    }
}

impl Validate for RemoteStartTransactionRequest {
    fn validate_at(&self, path: &mut ValidationPath, out: &mut Violations) {
        path.push_key("idTag");
        validate::string(self.id_tag.as_str(), None, Some(20), path, out);
        path.pop();
        path.push_key("chargingProfile");
        if let Some(value) = &self.charging_profile {
            Validate::validate_at(value, path, out);
        }
        path.pop();
    }
}

impl Sealed for RemoteStartTransactionRequest {}
impl Request for RemoteStartTransactionRequest {
    type Action = Action;
    const ACTION: Action = Action::RemoteStartTransaction;
    const KIND: MessageKind = MessageKind::Call;
    const ORIGIN: Origin = Origin::Csms;
    type Response = RemoteStartTransactionResponse;
}

impl Confirmed for RemoteStartTransactionRequest {}

/// Spec: OCPP 1.6 · Core · CSMS → Charging Station · CALL.
#[derive(Clone, Debug, PartialEq, Serialize, Deserialize)]
pub struct RemoteStartTransactionResponse {
    /// The `status` member.
    pub status: RemoteStartStopStatus,
}

impl RemoteStartTransactionResponse {
    /// Creates a `RemoteStartTransactionResponse` from its required fields.
    #[must_use]
    pub fn new(status: RemoteStartStopStatus) -> Self {
        Self { status }
    }
}

impl Validate for RemoteStartTransactionResponse {
    fn validate_at(&self, path: &mut ValidationPath, out: &mut Violations) {
        path.push_key("status");
        Validate::validate_at(&self.status, path, out);
        path.pop();
    }
}

impl Sealed for RemoteStartTransactionResponse {}
impl Response for RemoteStartTransactionResponse {}

/// Spec: OCPP 1.6 · Core · CSMS → Charging Station · CALL.
#[derive(Clone, Debug, PartialEq, Serialize, Deserialize)]
pub struct RemoteStopTransactionRequest {
    /// The `transactionId` member.
    #[serde(rename = "transactionId")]
    pub transaction_id: i32,
}

impl RemoteStopTransactionRequest {
    /// Creates a `RemoteStopTransactionRequest` from its required fields.
    #[must_use]
    pub fn new(transaction_id: i32) -> Self {
        Self { transaction_id }
    }
}

impl Validate for RemoteStopTransactionRequest {
    fn validate_at(&self, _path: &mut ValidationPath, _out: &mut Violations) {}
}

impl Sealed for RemoteStopTransactionRequest {}
impl Request for RemoteStopTransactionRequest {
    type Action = Action;
    const ACTION: Action = Action::RemoteStopTransaction;
    const KIND: MessageKind = MessageKind::Call;
    const ORIGIN: Origin = Origin::Csms;
    type Response = RemoteStopTransactionResponse;
}

impl Confirmed for RemoteStopTransactionRequest {}

/// Spec: OCPP 1.6 · Core · CSMS → Charging Station · CALL.
#[derive(Clone, Debug, PartialEq, Serialize, Deserialize)]
pub struct RemoteStopTransactionResponse {
    /// The `status` member.
    pub status: RemoteStartStopStatus,
}

impl RemoteStopTransactionResponse {
    /// Creates a `RemoteStopTransactionResponse` from its required fields.
    #[must_use]
    pub fn new(status: RemoteStartStopStatus) -> Self {
        Self { status }
    }
}

impl Validate for RemoteStopTransactionResponse {
    fn validate_at(&self, path: &mut ValidationPath, out: &mut Violations) {
        path.push_key("status");
        Validate::validate_at(&self.status, path, out);
        path.pop();
    }
}

impl Sealed for RemoteStopTransactionResponse {}
impl Response for RemoteStopTransactionResponse {}

/// Spec: OCPP 1.6 · Reservation · CSMS → Charging Station · CALL.
#[derive(Clone, Debug, PartialEq, Serialize, Deserialize)]
pub struct ReserveNowRequest {
    /// The `connectorId` member.
    #[serde(rename = "connectorId")]
    pub connector_id: i32,
    /// The `expiryDate` member.
    #[serde(rename = "expiryDate")]
    pub expiry_date: crate::types::DateTime,
    /// The `idTag` member.
    #[serde(rename = "idTag")]
    pub id_tag: String,
    /// The `reservationId` member.
    #[serde(rename = "reservationId")]
    pub reservation_id: i32,
    /// The `parentIdTag` member.
    #[serde(rename = "parentIdTag")]
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub parent_id_tag: Option<String>,
}

impl ReserveNowRequest {
    /// Creates a `ReserveNowRequest` from its required fields.
    #[must_use]
    pub fn new(
        connector_id: i32,
        expiry_date: impl Into<crate::types::DateTime>,
        id_tag: impl Into<String>,
        reservation_id: i32,
    ) -> Self {
        Self {
            connector_id,
            expiry_date: expiry_date.into(),
            id_tag: id_tag.into(),
            reservation_id,
            parent_id_tag: None,
        }
    }

    /// Sets the optional `parentIdTag` field.
    #[must_use]
    pub fn with_parent_id_tag(mut self, value: impl Into<String>) -> Self {
        self.parent_id_tag = Some(value.into());
        self
    }
}

impl Validate for ReserveNowRequest {
    fn validate_at(&self, path: &mut ValidationPath, out: &mut Violations) {
        path.push_key("idTag");
        validate::string(self.id_tag.as_str(), None, Some(20), path, out);
        path.pop();
        path.push_key("parentIdTag");
        if let Some(value) = &self.parent_id_tag {
            validate::string(value.as_str(), None, Some(20), path, out);
        }
        path.pop();
    }
}

impl Sealed for ReserveNowRequest {}
impl Request for ReserveNowRequest {
    type Action = Action;
    const ACTION: Action = Action::ReserveNow;
    const KIND: MessageKind = MessageKind::Call;
    const ORIGIN: Origin = Origin::Csms;
    type Response = ReserveNowResponse;
}

impl Confirmed for ReserveNowRequest {}

/// Spec: OCPP 1.6 · Reservation · CSMS → Charging Station · CALL.
#[derive(Clone, Debug, PartialEq, Serialize, Deserialize)]
pub struct ReserveNowResponse {
    /// The `status` member.
    pub status: ReservationStatus,
}

impl ReserveNowResponse {
    /// Creates a `ReserveNowResponse` from its required fields.
    #[must_use]
    pub fn new(status: ReservationStatus) -> Self {
        Self { status }
    }
}

impl Validate for ReserveNowResponse {
    fn validate_at(&self, path: &mut ValidationPath, out: &mut Violations) {
        path.push_key("status");
        Validate::validate_at(&self.status, path, out);
        path.pop();
    }
}

impl Sealed for ReserveNowResponse {}
impl Response for ReserveNowResponse {}

/// Spec: OCPP 1.6 · Core · CSMS → Charging Station · CALL.
#[derive(Clone, Debug, PartialEq, Serialize, Deserialize)]
pub struct ResetRequest {
    /// The `type` member.
    pub r#type: ResetType,
}

impl ResetRequest {
    /// Creates a `ResetRequest` from its required fields.
    #[must_use]
    pub fn new(r#type: ResetType) -> Self {
        Self { r#type }
    }
}

impl Validate for ResetRequest {
    fn validate_at(&self, path: &mut ValidationPath, out: &mut Violations) {
        path.push_key("type");
        Validate::validate_at(&self.r#type, path, out);
        path.pop();
    }
}

impl Sealed for ResetRequest {}
impl Request for ResetRequest {
    type Action = Action;
    const ACTION: Action = Action::Reset;
    const KIND: MessageKind = MessageKind::Call;
    const ORIGIN: Origin = Origin::Csms;
    type Response = ResetResponse;
}

impl Confirmed for ResetRequest {}

/// Spec: OCPP 1.6 · Core · CSMS → Charging Station · CALL.
#[derive(Clone, Debug, PartialEq, Serialize, Deserialize)]
pub struct ResetResponse {
    /// The `status` member.
    pub status: ResetStatus,
}

impl ResetResponse {
    /// Creates a `ResetResponse` from its required fields.
    #[must_use]
    pub fn new(status: ResetStatus) -> Self {
        Self { status }
    }
}

impl Validate for ResetResponse {
    fn validate_at(&self, path: &mut ValidationPath, out: &mut Violations) {
        path.push_key("status");
        Validate::validate_at(&self.status, path, out);
        path.pop();
    }
}

impl Sealed for ResetResponse {}
impl Response for ResetResponse {}

/// Spec: OCPP 1.6 · Security (Whitepaper ed. 2) · Charging Station → CSMS · CALL.
#[derive(Clone, Debug, PartialEq, Serialize, Deserialize)]
pub struct SecurityEventNotificationRequest {
    /// The `type` member.
    pub r#type: String,
    /// The `timestamp` member.
    pub timestamp: crate::types::DateTime,
    /// The `techInfo` member.
    #[serde(rename = "techInfo")]
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub tech_info: Option<String>,
}

impl SecurityEventNotificationRequest {
    /// Creates a `SecurityEventNotificationRequest` from its required fields.
    #[must_use]
    pub fn new(r#type: impl Into<String>, timestamp: impl Into<crate::types::DateTime>) -> Self {
        Self {
            r#type: r#type.into(),
            timestamp: timestamp.into(),
            tech_info: None,
        }
    }

    /// Sets the optional `techInfo` field.
    #[must_use]
    pub fn with_tech_info(mut self, value: impl Into<String>) -> Self {
        self.tech_info = Some(value.into());
        self
    }
}

impl Validate for SecurityEventNotificationRequest {
    fn validate_at(&self, path: &mut ValidationPath, out: &mut Violations) {
        path.push_key("type");
        validate::string(self.r#type.as_str(), None, Some(50), path, out);
        path.pop();
        path.push_key("techInfo");
        if let Some(value) = &self.tech_info {
            validate::string(value.as_str(), None, Some(255), path, out);
        }
        path.pop();
    }
}

impl Sealed for SecurityEventNotificationRequest {}
impl Request for SecurityEventNotificationRequest {
    type Action = Action;
    const ACTION: Action = Action::SecurityEventNotification;
    const KIND: MessageKind = MessageKind::Call;
    const ORIGIN: Origin = Origin::ChargingStation;
    type Response = SecurityEventNotificationResponse;
}

impl Confirmed for SecurityEventNotificationRequest {}

/// Spec: OCPP 1.6 · Security (Whitepaper ed. 2) · Charging Station → CSMS · CALL.
#[derive(Clone, Debug, Default, PartialEq, Serialize, Deserialize)]
pub struct SecurityEventNotificationResponse {}

impl SecurityEventNotificationResponse {
    /// Creates an empty `SecurityEventNotificationResponse`. Every field of this type is optional.
    #[must_use]
    pub fn new() -> Self {
        Self::default()
    }
}

impl Validate for SecurityEventNotificationResponse {
    fn validate_at(&self, _path: &mut ValidationPath, _out: &mut Violations) {}
}

impl Sealed for SecurityEventNotificationResponse {}
impl Response for SecurityEventNotificationResponse {}

/// Spec: OCPP 1.6 · Local Auth List Management · CSMS → Charging Station · CALL.
#[derive(Clone, Debug, PartialEq, Serialize, Deserialize)]
pub struct SendLocalListRequest {
    /// The `listVersion` member.
    #[serde(rename = "listVersion")]
    pub list_version: i32,
    /// The `updateType` member.
    #[serde(rename = "updateType")]
    pub update_type: UpdateType,
    /// The `localAuthorizationList` member.
    #[serde(rename = "localAuthorizationList")]
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub local_authorization_list: Option<Vec<AuthorizationData>>,
}

impl SendLocalListRequest {
    /// Creates a `SendLocalListRequest` from its required fields.
    #[must_use]
    pub fn new(list_version: i32, update_type: UpdateType) -> Self {
        Self {
            list_version,
            update_type,
            local_authorization_list: None,
        }
    }

    /// Sets the optional `localAuthorizationList` field.
    #[must_use]
    pub fn with_local_authorization_list(mut self, value: Vec<AuthorizationData>) -> Self {
        self.local_authorization_list = Some(value);
        self
    }
}

impl Validate for SendLocalListRequest {
    fn validate_at(&self, path: &mut ValidationPath, out: &mut Violations) {
        path.push_key("updateType");
        Validate::validate_at(&self.update_type, path, out);
        path.pop();
        path.push_key("localAuthorizationList");
        if let Some(value) = &self.local_authorization_list {
            for (index, item) in value.iter().enumerate() {
                path.push_index(index);
                Validate::validate_at(item, path, out);
                path.pop();
            }
        }
        path.pop();
    }
}

impl Sealed for SendLocalListRequest {}
impl Request for SendLocalListRequest {
    type Action = Action;
    const ACTION: Action = Action::SendLocalList;
    const KIND: MessageKind = MessageKind::Call;
    const ORIGIN: Origin = Origin::Csms;
    type Response = SendLocalListResponse;
}

impl Confirmed for SendLocalListRequest {}

/// Spec: OCPP 1.6 · Local Auth List Management · CSMS → Charging Station · CALL.
#[derive(Clone, Debug, PartialEq, Serialize, Deserialize)]
pub struct SendLocalListResponse {
    /// The `status` member.
    pub status: UpdateStatus,
}

impl SendLocalListResponse {
    /// Creates a `SendLocalListResponse` from its required fields.
    #[must_use]
    pub fn new(status: UpdateStatus) -> Self {
        Self { status }
    }
}

impl Validate for SendLocalListResponse {
    fn validate_at(&self, path: &mut ValidationPath, out: &mut Violations) {
        path.push_key("status");
        Validate::validate_at(&self.status, path, out);
        path.pop();
    }
}

impl Sealed for SendLocalListResponse {}
impl Response for SendLocalListResponse {}

/// Spec: OCPP 1.6 · Smart Charging · CSMS → Charging Station · CALL.
#[derive(Clone, Debug, PartialEq, Serialize, Deserialize)]
pub struct SetChargingProfileRequest {
    /// The `connectorId` member.
    #[serde(rename = "connectorId")]
    pub connector_id: i32,
    /// The `csChargingProfiles` member.
    #[serde(rename = "csChargingProfiles")]
    pub cs_charging_profiles: ChargingProfile,
}

impl SetChargingProfileRequest {
    /// Creates a `SetChargingProfileRequest` from its required fields.
    #[must_use]
    pub fn new(connector_id: i32, cs_charging_profiles: ChargingProfile) -> Self {
        Self {
            connector_id,
            cs_charging_profiles,
        }
    }
}

impl Validate for SetChargingProfileRequest {
    fn validate_at(&self, path: &mut ValidationPath, out: &mut Violations) {
        path.push_key("csChargingProfiles");
        Validate::validate_at(&self.cs_charging_profiles, path, out);
        path.pop();
    }
}

impl Sealed for SetChargingProfileRequest {}
impl Request for SetChargingProfileRequest {
    type Action = Action;
    const ACTION: Action = Action::SetChargingProfile;
    const KIND: MessageKind = MessageKind::Call;
    const ORIGIN: Origin = Origin::Csms;
    type Response = SetChargingProfileResponse;
}

impl Confirmed for SetChargingProfileRequest {}

/// Spec: OCPP 1.6 · Smart Charging · CSMS → Charging Station · CALL.
#[derive(Clone, Debug, PartialEq, Serialize, Deserialize)]
pub struct SetChargingProfileResponse {
    /// The `status` member.
    pub status: ChargingProfileStatus,
}

impl SetChargingProfileResponse {
    /// Creates a `SetChargingProfileResponse` from its required fields.
    #[must_use]
    pub fn new(status: ChargingProfileStatus) -> Self {
        Self { status }
    }
}

impl Validate for SetChargingProfileResponse {
    fn validate_at(&self, path: &mut ValidationPath, out: &mut Violations) {
        path.push_key("status");
        Validate::validate_at(&self.status, path, out);
        path.pop();
    }
}

impl Sealed for SetChargingProfileResponse {}
impl Response for SetChargingProfileResponse {}

/// Spec: OCPP 1.6 · Security (Whitepaper ed. 2) · Charging Station → CSMS · CALL.
#[derive(Clone, Debug, PartialEq, Serialize, Deserialize)]
pub struct SignCertificateRequest {
    /// The `csr` member.
    pub csr: String,
}

impl SignCertificateRequest {
    /// Creates a `SignCertificateRequest` from its required fields.
    #[must_use]
    pub fn new(csr: impl Into<String>) -> Self {
        Self { csr: csr.into() }
    }
}

impl Validate for SignCertificateRequest {
    fn validate_at(&self, path: &mut ValidationPath, out: &mut Violations) {
        path.push_key("csr");
        validate::string(self.csr.as_str(), None, Some(5500), path, out);
        path.pop();
    }
}

impl Sealed for SignCertificateRequest {}
impl Request for SignCertificateRequest {
    type Action = Action;
    const ACTION: Action = Action::SignCertificate;
    const KIND: MessageKind = MessageKind::Call;
    const ORIGIN: Origin = Origin::ChargingStation;
    type Response = SignCertificateResponse;
}

impl Confirmed for SignCertificateRequest {}

/// Spec: OCPP 1.6 · Security (Whitepaper ed. 2) · Charging Station → CSMS · CALL.
#[derive(Clone, Debug, PartialEq, Serialize, Deserialize)]
pub struct SignCertificateResponse {
    /// The `status` member.
    pub status: GenericStatus,
}

impl SignCertificateResponse {
    /// Creates a `SignCertificateResponse` from its required fields.
    #[must_use]
    pub fn new(status: GenericStatus) -> Self {
        Self { status }
    }
}

impl Validate for SignCertificateResponse {
    fn validate_at(&self, path: &mut ValidationPath, out: &mut Violations) {
        path.push_key("status");
        Validate::validate_at(&self.status, path, out);
        path.pop();
    }
}

impl Sealed for SignCertificateResponse {}
impl Response for SignCertificateResponse {}

/// Spec: OCPP 1.6 · Security (Whitepaper ed. 2) · Charging Station → CSMS · CALL.
#[derive(Clone, Debug, PartialEq, Serialize, Deserialize)]
pub struct SignedFirmwareStatusNotificationRequest {
    /// The `status` member.
    pub status: SignedFirmwareStatus,
    /// The `requestId` member.
    #[serde(rename = "requestId")]
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub request_id: Option<i32>,
}

impl SignedFirmwareStatusNotificationRequest {
    /// Creates a `SignedFirmwareStatusNotificationRequest` from its required fields.
    #[must_use]
    pub fn new(status: SignedFirmwareStatus) -> Self {
        Self {
            status,
            request_id: None,
        }
    }

    /// Sets the optional `requestId` field.
    #[must_use]
    pub fn with_request_id(mut self, value: i32) -> Self {
        self.request_id = Some(value);
        self
    }
}

impl Validate for SignedFirmwareStatusNotificationRequest {
    fn validate_at(&self, path: &mut ValidationPath, out: &mut Violations) {
        path.push_key("status");
        Validate::validate_at(&self.status, path, out);
        path.pop();
    }
}

impl Sealed for SignedFirmwareStatusNotificationRequest {}
impl Request for SignedFirmwareStatusNotificationRequest {
    type Action = Action;
    const ACTION: Action = Action::SignedFirmwareStatusNotification;
    const KIND: MessageKind = MessageKind::Call;
    const ORIGIN: Origin = Origin::ChargingStation;
    type Response = SignedFirmwareStatusNotificationResponse;
}

impl Confirmed for SignedFirmwareStatusNotificationRequest {}

/// Spec: OCPP 1.6 · Security (Whitepaper ed. 2) · Charging Station → CSMS · CALL.
#[derive(Clone, Debug, Default, PartialEq, Serialize, Deserialize)]
pub struct SignedFirmwareStatusNotificationResponse {}

impl SignedFirmwareStatusNotificationResponse {
    /// Creates an empty `SignedFirmwareStatusNotificationResponse`. Every field of this type is optional.
    #[must_use]
    pub fn new() -> Self {
        Self::default()
    }
}

impl Validate for SignedFirmwareStatusNotificationResponse {
    fn validate_at(&self, _path: &mut ValidationPath, _out: &mut Violations) {}
}

impl Sealed for SignedFirmwareStatusNotificationResponse {}
impl Response for SignedFirmwareStatusNotificationResponse {}

/// Spec: OCPP 1.6 · Security (Whitepaper ed. 2) · CSMS → Charging Station · CALL.
#[derive(Clone, Debug, PartialEq, Serialize, Deserialize)]
pub struct SignedUpdateFirmwareRequest {
    /// The `requestId` member.
    #[serde(rename = "requestId")]
    pub request_id: i32,
    /// The `firmware` member.
    pub firmware: Firmware,
    /// The `retries` member.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub retries: Option<i32>,
    /// The `retryInterval` member.
    #[serde(rename = "retryInterval")]
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub retry_interval: Option<i32>,
}

impl SignedUpdateFirmwareRequest {
    /// Creates a `SignedUpdateFirmwareRequest` from its required fields.
    #[must_use]
    pub fn new(request_id: i32, firmware: Firmware) -> Self {
        Self {
            request_id,
            firmware,
            retries: None,
            retry_interval: None,
        }
    }

    /// Sets the optional `retries` field.
    #[must_use]
    pub fn with_retries(mut self, value: i32) -> Self {
        self.retries = Some(value);
        self
    }

    /// Sets the optional `retryInterval` field.
    #[must_use]
    pub fn with_retry_interval(mut self, value: i32) -> Self {
        self.retry_interval = Some(value);
        self
    }
}

impl Validate for SignedUpdateFirmwareRequest {
    fn validate_at(&self, path: &mut ValidationPath, out: &mut Violations) {
        path.push_key("firmware");
        Validate::validate_at(&self.firmware, path, out);
        path.pop();
    }
}

impl Sealed for SignedUpdateFirmwareRequest {}
impl Request for SignedUpdateFirmwareRequest {
    type Action = Action;
    const ACTION: Action = Action::SignedUpdateFirmware;
    const KIND: MessageKind = MessageKind::Call;
    const ORIGIN: Origin = Origin::Csms;
    type Response = SignedUpdateFirmwareResponse;
}

impl Confirmed for SignedUpdateFirmwareRequest {}

/// Spec: OCPP 1.6 · Security (Whitepaper ed. 2) · CSMS → Charging Station · CALL.
#[derive(Clone, Debug, PartialEq, Serialize, Deserialize)]
pub struct SignedUpdateFirmwareResponse {
    /// The `status` member.
    pub status: UpdateFirmwareStatus,
}

impl SignedUpdateFirmwareResponse {
    /// Creates a `SignedUpdateFirmwareResponse` from its required fields.
    #[must_use]
    pub fn new(status: UpdateFirmwareStatus) -> Self {
        Self { status }
    }
}

impl Validate for SignedUpdateFirmwareResponse {
    fn validate_at(&self, path: &mut ValidationPath, out: &mut Violations) {
        path.push_key("status");
        Validate::validate_at(&self.status, path, out);
        path.pop();
    }
}

impl Sealed for SignedUpdateFirmwareResponse {}
impl Response for SignedUpdateFirmwareResponse {}

/// Spec: OCPP 1.6 · Core · Charging Station → CSMS · CALL.
#[derive(Clone, Debug, PartialEq, Serialize, Deserialize)]
pub struct StartTransactionRequest {
    /// The `connectorId` member.
    #[serde(rename = "connectorId")]
    pub connector_id: i32,
    /// The `idTag` member.
    #[serde(rename = "idTag")]
    pub id_tag: String,
    /// The `meterStart` member.
    #[serde(rename = "meterStart")]
    pub meter_start: i32,
    /// The `timestamp` member.
    pub timestamp: crate::types::DateTime,
    /// The `reservationId` member.
    #[serde(rename = "reservationId")]
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub reservation_id: Option<i32>,
}

impl StartTransactionRequest {
    /// Creates a `StartTransactionRequest` from its required fields.
    #[must_use]
    pub fn new(
        connector_id: i32,
        id_tag: impl Into<String>,
        meter_start: i32,
        timestamp: impl Into<crate::types::DateTime>,
    ) -> Self {
        Self {
            connector_id,
            id_tag: id_tag.into(),
            meter_start,
            timestamp: timestamp.into(),
            reservation_id: None,
        }
    }

    /// Sets the optional `reservationId` field.
    #[must_use]
    pub fn with_reservation_id(mut self, value: i32) -> Self {
        self.reservation_id = Some(value);
        self
    }
}

impl Validate for StartTransactionRequest {
    fn validate_at(&self, path: &mut ValidationPath, out: &mut Violations) {
        path.push_key("idTag");
        validate::string(self.id_tag.as_str(), None, Some(20), path, out);
        path.pop();
    }
}

impl Sealed for StartTransactionRequest {}
impl Request for StartTransactionRequest {
    type Action = Action;
    const ACTION: Action = Action::StartTransaction;
    const KIND: MessageKind = MessageKind::Call;
    const ORIGIN: Origin = Origin::ChargingStation;
    type Response = StartTransactionResponse;
}

impl Confirmed for StartTransactionRequest {}

/// Spec: OCPP 1.6 · Core · Charging Station → CSMS · CALL.
#[derive(Clone, Debug, PartialEq, Serialize, Deserialize)]
pub struct StartTransactionResponse {
    /// The `idTagInfo` member.
    #[serde(rename = "idTagInfo")]
    pub id_tag_info: IdTagInfo,
    /// The `transactionId` member.
    #[serde(rename = "transactionId")]
    pub transaction_id: i32,
}

impl StartTransactionResponse {
    /// Creates a `StartTransactionResponse` from its required fields.
    #[must_use]
    pub fn new(id_tag_info: IdTagInfo, transaction_id: i32) -> Self {
        Self {
            id_tag_info,
            transaction_id,
        }
    }
}

impl Validate for StartTransactionResponse {
    fn validate_at(&self, path: &mut ValidationPath, out: &mut Violations) {
        path.push_key("idTagInfo");
        Validate::validate_at(&self.id_tag_info, path, out);
        path.pop();
    }
}

impl Sealed for StartTransactionResponse {}
impl Response for StartTransactionResponse {}

/// Spec: OCPP 1.6 · Core · Charging Station → CSMS · CALL.
#[derive(Clone, Debug, PartialEq, Serialize, Deserialize)]
pub struct StatusNotificationRequest {
    /// The `connectorId` member.
    #[serde(rename = "connectorId")]
    pub connector_id: i32,
    /// The `errorCode` member.
    #[serde(rename = "errorCode")]
    pub error_code: ChargePointErrorCode,
    /// The `status` member.
    pub status: ChargePointStatus,
    /// The `info` member.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub info: Option<String>,
    /// The `timestamp` member.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub timestamp: Option<crate::types::DateTime>,
    /// The `vendorId` member.
    #[serde(rename = "vendorId")]
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub vendor_id: Option<String>,
    /// The `vendorErrorCode` member.
    #[serde(rename = "vendorErrorCode")]
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub vendor_error_code: Option<String>,
}

impl StatusNotificationRequest {
    /// Creates a `StatusNotificationRequest` from its required fields.
    #[must_use]
    pub fn new(
        connector_id: i32,
        error_code: ChargePointErrorCode,
        status: ChargePointStatus,
    ) -> Self {
        Self {
            connector_id,
            error_code,
            status,
            info: None,
            timestamp: None,
            vendor_id: None,
            vendor_error_code: None,
        }
    }

    /// Sets the optional `info` field.
    #[must_use]
    pub fn with_info(mut self, value: impl Into<String>) -> Self {
        self.info = Some(value.into());
        self
    }

    /// Sets the optional `timestamp` field.
    #[must_use]
    pub fn with_timestamp(mut self, value: impl Into<crate::types::DateTime>) -> Self {
        self.timestamp = Some(value.into());
        self
    }

    /// Sets the optional `vendorId` field.
    #[must_use]
    pub fn with_vendor_id(mut self, value: impl Into<String>) -> Self {
        self.vendor_id = Some(value.into());
        self
    }

    /// Sets the optional `vendorErrorCode` field.
    #[must_use]
    pub fn with_vendor_error_code(mut self, value: impl Into<String>) -> Self {
        self.vendor_error_code = Some(value.into());
        self
    }
}

impl Validate for StatusNotificationRequest {
    fn validate_at(&self, path: &mut ValidationPath, out: &mut Violations) {
        path.push_key("errorCode");
        Validate::validate_at(&self.error_code, path, out);
        path.pop();
        path.push_key("status");
        Validate::validate_at(&self.status, path, out);
        path.pop();
        path.push_key("info");
        if let Some(value) = &self.info {
            validate::string(value.as_str(), None, Some(50), path, out);
        }
        path.pop();
        path.push_key("vendorId");
        if let Some(value) = &self.vendor_id {
            validate::string(value.as_str(), None, Some(255), path, out);
        }
        path.pop();
        path.push_key("vendorErrorCode");
        if let Some(value) = &self.vendor_error_code {
            validate::string(value.as_str(), None, Some(50), path, out);
        }
        path.pop();
    }
}

impl Sealed for StatusNotificationRequest {}
impl Request for StatusNotificationRequest {
    type Action = Action;
    const ACTION: Action = Action::StatusNotification;
    const KIND: MessageKind = MessageKind::Call;
    const ORIGIN: Origin = Origin::ChargingStation;
    type Response = StatusNotificationResponse;
}

impl Confirmed for StatusNotificationRequest {}

/// Spec: OCPP 1.6 · Core · Charging Station → CSMS · CALL.
#[derive(Clone, Debug, Default, PartialEq, Serialize, Deserialize)]
pub struct StatusNotificationResponse {}

impl StatusNotificationResponse {
    /// Creates an empty `StatusNotificationResponse`. Every field of this type is optional.
    #[must_use]
    pub fn new() -> Self {
        Self::default()
    }
}

impl Validate for StatusNotificationResponse {
    fn validate_at(&self, _path: &mut ValidationPath, _out: &mut Violations) {}
}

impl Sealed for StatusNotificationResponse {}
impl Response for StatusNotificationResponse {}

/// Spec: OCPP 1.6 · Core · Charging Station → CSMS · CALL.
#[derive(Clone, Debug, PartialEq, Serialize, Deserialize)]
pub struct StopTransactionRequest {
    /// The `meterStop` member.
    #[serde(rename = "meterStop")]
    pub meter_stop: i32,
    /// The `timestamp` member.
    pub timestamp: crate::types::DateTime,
    /// The `transactionId` member.
    #[serde(rename = "transactionId")]
    pub transaction_id: i32,
    /// The `idTag` member.
    #[serde(rename = "idTag")]
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub id_tag: Option<String>,
    /// The `reason` member.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub reason: Option<Reason>,
    /// The `transactionData` member.
    #[serde(rename = "transactionData")]
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub transaction_data: Option<Vec<MeterValue>>,
}

impl StopTransactionRequest {
    /// Creates a `StopTransactionRequest` from its required fields.
    #[must_use]
    pub fn new(
        meter_stop: i32,
        timestamp: impl Into<crate::types::DateTime>,
        transaction_id: i32,
    ) -> Self {
        Self {
            meter_stop,
            timestamp: timestamp.into(),
            transaction_id,
            id_tag: None,
            reason: None,
            transaction_data: None,
        }
    }

    /// Sets the optional `idTag` field.
    #[must_use]
    pub fn with_id_tag(mut self, value: impl Into<String>) -> Self {
        self.id_tag = Some(value.into());
        self
    }

    /// Sets the optional `reason` field.
    #[must_use]
    pub fn with_reason(mut self, value: Reason) -> Self {
        self.reason = Some(value);
        self
    }

    /// Sets the optional `transactionData` field.
    #[must_use]
    pub fn with_transaction_data(mut self, value: Vec<MeterValue>) -> Self {
        self.transaction_data = Some(value);
        self
    }
}

impl Validate for StopTransactionRequest {
    fn validate_at(&self, path: &mut ValidationPath, out: &mut Violations) {
        path.push_key("idTag");
        if let Some(value) = &self.id_tag {
            validate::string(value.as_str(), None, Some(20), path, out);
        }
        path.pop();
        path.push_key("reason");
        if let Some(value) = &self.reason {
            Validate::validate_at(value, path, out);
        }
        path.pop();
        path.push_key("transactionData");
        if let Some(value) = &self.transaction_data {
            for (index, item) in value.iter().enumerate() {
                path.push_index(index);
                Validate::validate_at(item, path, out);
                path.pop();
            }
        }
        path.pop();
    }
}

impl Sealed for StopTransactionRequest {}
impl Request for StopTransactionRequest {
    type Action = Action;
    const ACTION: Action = Action::StopTransaction;
    const KIND: MessageKind = MessageKind::Call;
    const ORIGIN: Origin = Origin::ChargingStation;
    type Response = StopTransactionResponse;
}

impl Confirmed for StopTransactionRequest {}

/// Spec: OCPP 1.6 · Core · Charging Station → CSMS · CALL.
#[derive(Clone, Debug, Default, PartialEq, Serialize, Deserialize)]
pub struct StopTransactionResponse {
    /// The `idTagInfo` member.
    #[serde(rename = "idTagInfo")]
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub id_tag_info: Option<IdTagInfo>,
}

impl StopTransactionResponse {
    /// Creates an empty `StopTransactionResponse`. Every field of this type is optional.
    #[must_use]
    pub fn new() -> Self {
        Self::default()
    }

    /// Sets the optional `idTagInfo` field.
    #[must_use]
    pub fn with_id_tag_info(mut self, value: IdTagInfo) -> Self {
        self.id_tag_info = Some(value);
        self
    }
}

impl Validate for StopTransactionResponse {
    fn validate_at(&self, path: &mut ValidationPath, out: &mut Violations) {
        path.push_key("idTagInfo");
        if let Some(value) = &self.id_tag_info {
            Validate::validate_at(value, path, out);
        }
        path.pop();
    }
}

impl Sealed for StopTransactionResponse {}
impl Response for StopTransactionResponse {}

/// Spec: OCPP 1.6 · Remote Trigger · CSMS → Charging Station · CALL.
#[derive(Clone, Debug, PartialEq, Serialize, Deserialize)]
pub struct TriggerMessageRequest {
    /// The `requestedMessage` member.
    #[serde(rename = "requestedMessage")]
    pub requested_message: MessageTrigger,
    /// The `connectorId` member.
    #[serde(rename = "connectorId")]
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub connector_id: Option<i32>,
}

impl TriggerMessageRequest {
    /// Creates a `TriggerMessageRequest` from its required fields.
    #[must_use]
    pub fn new(requested_message: MessageTrigger) -> Self {
        Self {
            requested_message,
            connector_id: None,
        }
    }

    /// Sets the optional `connectorId` field.
    #[must_use]
    pub fn with_connector_id(mut self, value: i32) -> Self {
        self.connector_id = Some(value);
        self
    }
}

impl Validate for TriggerMessageRequest {
    fn validate_at(&self, path: &mut ValidationPath, out: &mut Violations) {
        path.push_key("requestedMessage");
        Validate::validate_at(&self.requested_message, path, out);
        path.pop();
    }
}

impl Sealed for TriggerMessageRequest {}
impl Request for TriggerMessageRequest {
    type Action = Action;
    const ACTION: Action = Action::TriggerMessage;
    const KIND: MessageKind = MessageKind::Call;
    const ORIGIN: Origin = Origin::Csms;
    type Response = TriggerMessageResponse;
}

impl Confirmed for TriggerMessageRequest {}

/// Spec: OCPP 1.6 · Remote Trigger · CSMS → Charging Station · CALL.
#[derive(Clone, Debug, PartialEq, Serialize, Deserialize)]
pub struct TriggerMessageResponse {
    /// The `status` member.
    pub status: TriggerMessageStatus,
}

impl TriggerMessageResponse {
    /// Creates a `TriggerMessageResponse` from its required fields.
    #[must_use]
    pub fn new(status: TriggerMessageStatus) -> Self {
        Self { status }
    }
}

impl Validate for TriggerMessageResponse {
    fn validate_at(&self, path: &mut ValidationPath, out: &mut Violations) {
        path.push_key("status");
        Validate::validate_at(&self.status, path, out);
        path.pop();
    }
}

impl Sealed for TriggerMessageResponse {}
impl Response for TriggerMessageResponse {}

/// Spec: OCPP 1.6 · Core · CSMS → Charging Station · CALL.
#[derive(Clone, Debug, PartialEq, Serialize, Deserialize)]
pub struct UnlockConnectorRequest {
    /// The `connectorId` member.
    #[serde(rename = "connectorId")]
    pub connector_id: i32,
}

impl UnlockConnectorRequest {
    /// Creates a `UnlockConnectorRequest` from its required fields.
    #[must_use]
    pub fn new(connector_id: i32) -> Self {
        Self { connector_id }
    }
}

impl Validate for UnlockConnectorRequest {
    fn validate_at(&self, _path: &mut ValidationPath, _out: &mut Violations) {}
}

impl Sealed for UnlockConnectorRequest {}
impl Request for UnlockConnectorRequest {
    type Action = Action;
    const ACTION: Action = Action::UnlockConnector;
    const KIND: MessageKind = MessageKind::Call;
    const ORIGIN: Origin = Origin::Csms;
    type Response = UnlockConnectorResponse;
}

impl Confirmed for UnlockConnectorRequest {}

/// Spec: OCPP 1.6 · Core · CSMS → Charging Station · CALL.
#[derive(Clone, Debug, PartialEq, Serialize, Deserialize)]
pub struct UnlockConnectorResponse {
    /// The `status` member.
    pub status: UnlockStatus,
}

impl UnlockConnectorResponse {
    /// Creates a `UnlockConnectorResponse` from its required fields.
    #[must_use]
    pub fn new(status: UnlockStatus) -> Self {
        Self { status }
    }
}

impl Validate for UnlockConnectorResponse {
    fn validate_at(&self, path: &mut ValidationPath, out: &mut Violations) {
        path.push_key("status");
        Validate::validate_at(&self.status, path, out);
        path.pop();
    }
}

impl Sealed for UnlockConnectorResponse {}
impl Response for UnlockConnectorResponse {}

/// Spec: OCPP 1.6 · Firmware Management · CSMS → Charging Station · CALL.
#[derive(Clone, Debug, PartialEq, Serialize, Deserialize)]
pub struct UpdateFirmwareRequest {
    /// The `location` member.
    pub location: String,
    /// The `retrieveDate` member.
    #[serde(rename = "retrieveDate")]
    pub retrieve_date: crate::types::DateTime,
    /// The `retries` member.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub retries: Option<i32>,
    /// The `retryInterval` member.
    #[serde(rename = "retryInterval")]
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub retry_interval: Option<i32>,
}

impl UpdateFirmwareRequest {
    /// Creates a `UpdateFirmwareRequest` from its required fields.
    #[must_use]
    pub fn new(
        location: impl Into<String>,
        retrieve_date: impl Into<crate::types::DateTime>,
    ) -> Self {
        Self {
            location: location.into(),
            retrieve_date: retrieve_date.into(),
            retries: None,
            retry_interval: None,
        }
    }

    /// Sets the optional `retries` field.
    #[must_use]
    pub fn with_retries(mut self, value: i32) -> Self {
        self.retries = Some(value);
        self
    }

    /// Sets the optional `retryInterval` field.
    #[must_use]
    pub fn with_retry_interval(mut self, value: i32) -> Self {
        self.retry_interval = Some(value);
        self
    }
}

impl Validate for UpdateFirmwareRequest {
    fn validate_at(&self, _path: &mut ValidationPath, _out: &mut Violations) {}
}

impl Sealed for UpdateFirmwareRequest {}
impl Request for UpdateFirmwareRequest {
    type Action = Action;
    const ACTION: Action = Action::UpdateFirmware;
    const KIND: MessageKind = MessageKind::Call;
    const ORIGIN: Origin = Origin::Csms;
    type Response = UpdateFirmwareResponse;
}

impl Confirmed for UpdateFirmwareRequest {}

/// Spec: OCPP 1.6 · Firmware Management · CSMS → Charging Station · CALL.
#[derive(Clone, Debug, Default, PartialEq, Serialize, Deserialize)]
pub struct UpdateFirmwareResponse {}

impl UpdateFirmwareResponse {
    /// Creates an empty `UpdateFirmwareResponse`. Every field of this type is optional.
    #[must_use]
    pub fn new() -> Self {
        Self::default()
    }
}

impl Validate for UpdateFirmwareResponse {
    fn validate_at(&self, _path: &mut ValidationPath, _out: &mut Violations) {}
}

impl Sealed for UpdateFirmwareResponse {}
impl Response for UpdateFirmwareResponse {}
