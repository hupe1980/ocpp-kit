// @generated by `cargo xtask codegen` from the vendored OCPP JSON schemas.
// DO NOT EDIT — re-run the generator instead. CI fails if this file is stale.

//! Data types shared between OCPP 2.0.1 messages, generated from the official JSON schemas.

#![allow(unused_imports, clippy::doc_markdown, clippy::struct_excessive_bools)]
#![allow(
    clippy::too_many_lines,
    clippy::too_many_arguments,
    clippy::wildcard_imports
)]
#![allow(clippy::match_same_arms, clippy::fn_params_excessive_bools)]

use alloc::string::String;
use alloc::vec::Vec;

use serde::{Deserialize, Serialize};

use super::enums::*;
use crate::validate::{self, Validate, ValidationPath, Violations};

/// AC_ Charging_ Parameters
/// EV AC charging parameters.
///
/// The OCPP 2.0.1 `ACChargingParameters` data type.
#[derive(Clone, Debug, PartialEq, Serialize, Deserialize)]
pub struct ACChargingParameters {
    /// AC_ Charging_ Parameters. Energy_ Amount. Energy_ Amount
    /// Amount of energy requested (in Wh). This includes energy required for preconditioning.
    #[serde(rename = "energyAmount")]
    pub energy_amount: i32,
    /// AC_ Charging_ Parameters. EV_ Min. Current
    /// Minimum current (amps) supported by the electric vehicle (per phase).
    #[serde(rename = "evMinCurrent")]
    pub ev_min_current: i32,
    /// AC_ Charging_ Parameters. EV_ Max. Current
    /// Maximum current (amps) supported by the electric vehicle (per phase). Includes cable capacity.
    #[serde(rename = "evMaxCurrent")]
    pub ev_max_current: i32,
    /// AC_ Charging_ Parameters. EV_ Max. Voltage
    /// Maximum voltage supported by the electric vehicle
    #[serde(rename = "evMaxVoltage")]
    pub ev_max_voltage: i32,
    /// The `customData` member.
    #[serde(rename = "customData")]
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub custom_data: Option<crate::types::CustomData>,
}

impl ACChargingParameters {
    /// Creates a `ACChargingParameters` from its required fields.
    #[must_use]
    pub fn new(
        energy_amount: i32,
        ev_min_current: i32,
        ev_max_current: i32,
        ev_max_voltage: i32,
    ) -> Self {
        Self {
            energy_amount,
            ev_min_current,
            ev_max_current,
            ev_max_voltage,
            custom_data: None,
        }
    }

    /// Sets the optional `customData` field.
    #[must_use]
    pub fn with_custom_data(mut self, value: crate::types::CustomData) -> Self {
        self.custom_data = Some(value);
        self
    }
}

impl Validate for ACChargingParameters {
    fn validate_at(&self, path: &mut ValidationPath, out: &mut Violations) {
        path.push_key("customData");
        if let Some(value) = &self.custom_data {
            Validate::validate_at(value, path, out);
        }
        path.pop();
    }
}

/// APN
/// Collection of configuration data needed to make a data-connection over a cellular network.
///
/// NOTE: When asking a GSM modem to dial in, it is possible to specify which mobile operator should be used. This can be done with the mobile country code (MCC) in combination with a mobile network code (MNC). Example: If your preferred network is Vodafone Netherlands, the MCC=204 and the MNC=04 which means the key PreferredNetwork = 20404 Some modems allows to specify a preferred network, which means, if this network is not available, a different network is used. If you specify UseOnlyPreferredNetwork and this network is not available, the modem will not dial in.
///
/// The OCPP 2.0.1 `APN` data type.
#[derive(Clone, Debug, PartialEq, Serialize, Deserialize)]
pub struct APN {
    /// APN. APN. URI
    /// The Access Point Name as an URL.
    pub apn: String,
    /// The `apnAuthentication` member.
    #[serde(rename = "apnAuthentication")]
    pub apn_authentication: APNAuthentication,
    /// APN. APN. User_ Name
    /// APN username.
    #[serde(rename = "apnUserName")]
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub apn_user_name: Option<String>,
    /// APN. APN. Password
    /// APN Password.
    #[serde(rename = "apnPassword")]
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub apn_password: Option<String>,
    /// APN. SIMPIN. PIN_ Code
    /// SIM card pin code.
    #[serde(rename = "simPin")]
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub sim_pin: Option<i32>,
    /// APN. Preferred_ Network. Mobile_ Network_ ID
    /// Preferred network, written as MCC and MNC concatenated. See note.
    #[serde(rename = "preferredNetwork")]
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub preferred_network: Option<String>,
    /// APN. Use_ Only_ Preferred_ Network. Indicator
    /// Default: false. Use only the preferred Network, do
    /// not dial in when not available. See Note.
    /// Schema default: `false`.
    #[serde(rename = "useOnlyPreferredNetwork")]
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub use_only_preferred_network: Option<bool>,
    /// The `customData` member.
    #[serde(rename = "customData")]
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub custom_data: Option<crate::types::CustomData>,
}

impl APN {
    /// Creates a `APN` from its required fields.
    #[must_use]
    pub fn new(apn: impl Into<String>, apn_authentication: APNAuthentication) -> Self {
        Self {
            apn: apn.into(),
            apn_authentication,
            apn_user_name: None,
            apn_password: None,
            sim_pin: None,
            preferred_network: None,
            use_only_preferred_network: None,
            custom_data: None,
        }
    }

    /// Sets the optional `apnUserName` field.
    #[must_use]
    pub fn with_apn_user_name(mut self, value: impl Into<String>) -> Self {
        self.apn_user_name = Some(value.into());
        self
    }

    /// Sets the optional `apnPassword` field.
    #[must_use]
    pub fn with_apn_password(mut self, value: impl Into<String>) -> Self {
        self.apn_password = Some(value.into());
        self
    }

    /// Sets the optional `simPin` field.
    #[must_use]
    pub fn with_sim_pin(mut self, value: i32) -> Self {
        self.sim_pin = Some(value);
        self
    }

    /// Sets the optional `preferredNetwork` field.
    #[must_use]
    pub fn with_preferred_network(mut self, value: impl Into<String>) -> Self {
        self.preferred_network = Some(value.into());
        self
    }

    /// Sets the optional `useOnlyPreferredNetwork` field.
    #[must_use]
    pub fn with_use_only_preferred_network(mut self, value: bool) -> Self {
        self.use_only_preferred_network = Some(value);
        self
    }

    /// Sets the optional `customData` field.
    #[must_use]
    pub fn with_custom_data(mut self, value: crate::types::CustomData) -> Self {
        self.custom_data = Some(value);
        self
    }
}

impl Validate for APN {
    fn validate_at(&self, path: &mut ValidationPath, out: &mut Violations) {
        path.push_key("apn");
        validate::string(self.apn.as_str(), None, Some(512), path, out);
        path.pop();
        path.push_key("apnAuthentication");
        Validate::validate_at(&self.apn_authentication, path, out);
        path.pop();
        path.push_key("apnUserName");
        if let Some(value) = &self.apn_user_name {
            validate::string(value.as_str(), None, Some(20), path, out);
        }
        path.pop();
        path.push_key("apnPassword");
        if let Some(value) = &self.apn_password {
            validate::string(value.as_str(), None, Some(20), path, out);
        }
        path.pop();
        path.push_key("preferredNetwork");
        if let Some(value) = &self.preferred_network {
            validate::string(value.as_str(), None, Some(6), path, out);
        }
        path.pop();
        path.push_key("customData");
        if let Some(value) = &self.custom_data {
            Validate::validate_at(value, path, out);
        }
        path.pop();
    }
}

/// Contains a case insensitive identifier to use for the authorization and the type of authorization to support multiple forms of identifiers.
///
/// The OCPP 2.0.1 `AdditionalInfo` data type.
#[derive(Clone, Debug, PartialEq, Serialize, Deserialize)]
pub struct AdditionalInfo {
    /// This field specifies the additional IdToken.
    #[serde(rename = "additionalIdToken")]
    pub additional_id_token: String,
    /// This defines the type of the additionalIdToken. This is a custom type, so the implementation needs to be agreed upon by all involved parties.
    pub r#type: String,
    /// The `customData` member.
    #[serde(rename = "customData")]
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub custom_data: Option<crate::types::CustomData>,
}

impl AdditionalInfo {
    /// Creates a `AdditionalInfo` from its required fields.
    #[must_use]
    pub fn new(additional_id_token: impl Into<String>, r#type: impl Into<String>) -> Self {
        Self {
            additional_id_token: additional_id_token.into(),
            r#type: r#type.into(),
            custom_data: None,
        }
    }

    /// Sets the optional `customData` field.
    #[must_use]
    pub fn with_custom_data(mut self, value: crate::types::CustomData) -> Self {
        self.custom_data = Some(value);
        self
    }
}

impl Validate for AdditionalInfo {
    fn validate_at(&self, path: &mut ValidationPath, out: &mut Violations) {
        path.push_key("additionalIdToken");
        validate::string(self.additional_id_token.as_str(), None, Some(36), path, out);
        path.pop();
        path.push_key("type");
        validate::string(self.r#type.as_str(), None, Some(50), path, out);
        path.pop();
        path.push_key("customData");
        if let Some(value) = &self.custom_data {
            Validate::validate_at(value, path, out);
        }
        path.pop();
    }
}

/// Contains the identifier to use for authorization.
///
/// The OCPP 2.0.1 `AuthorizationData` data type.
#[derive(Clone, Debug, PartialEq, Serialize, Deserialize)]
pub struct AuthorizationData {
    /// The `idToken` member.
    #[serde(rename = "idToken")]
    pub id_token: IdToken,
    /// The `idTokenInfo` member.
    #[serde(rename = "idTokenInfo")]
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub id_token_info: Option<IdTokenInfo>,
    /// The `customData` member.
    #[serde(rename = "customData")]
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub custom_data: Option<crate::types::CustomData>,
}

impl AuthorizationData {
    /// Creates a `AuthorizationData` from its required fields.
    #[must_use]
    pub fn new(id_token: IdToken) -> Self {
        Self {
            id_token,
            id_token_info: None,
            custom_data: None,
        }
    }

    /// Sets the optional `idTokenInfo` field.
    #[must_use]
    pub fn with_id_token_info(mut self, value: IdTokenInfo) -> Self {
        self.id_token_info = Some(value);
        self
    }

    /// Sets the optional `customData` field.
    #[must_use]
    pub fn with_custom_data(mut self, value: crate::types::CustomData) -> Self {
        self.custom_data = Some(value);
        self
    }
}

impl Validate for AuthorizationData {
    fn validate_at(&self, path: &mut ValidationPath, out: &mut Violations) {
        path.push_key("idToken");
        Validate::validate_at(&self.id_token, path, out);
        path.pop();
        path.push_key("idTokenInfo");
        if let Some(value) = &self.id_token_info {
            Validate::validate_at(value, path, out);
        }
        path.pop();
        path.push_key("customData");
        if let Some(value) = &self.custom_data {
            Validate::validate_at(value, path, out);
        }
        path.pop();
    }
}

/// The OCPP 2.0.1 `CertificateHashData` data type.
#[derive(Clone, Debug, PartialEq, Serialize, Deserialize)]
pub struct CertificateHashData {
    /// The `hashAlgorithm` member.
    #[serde(rename = "hashAlgorithm")]
    pub hash_algorithm: HashAlgorithm,
    /// Hashed value of the Issuer DN (Distinguished Name).
    #[serde(rename = "issuerNameHash")]
    pub issuer_name_hash: String,
    /// Hashed value of the issuers public key
    #[serde(rename = "issuerKeyHash")]
    pub issuer_key_hash: String,
    /// The serial number of the certificate.
    #[serde(rename = "serialNumber")]
    pub serial_number: String,
    /// The `customData` member.
    #[serde(rename = "customData")]
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub custom_data: Option<crate::types::CustomData>,
}

impl CertificateHashData {
    /// Creates a `CertificateHashData` from its required fields.
    #[must_use]
    pub fn new(
        hash_algorithm: HashAlgorithm,
        issuer_name_hash: impl Into<String>,
        issuer_key_hash: impl Into<String>,
        serial_number: impl Into<String>,
    ) -> Self {
        Self {
            hash_algorithm,
            issuer_name_hash: issuer_name_hash.into(),
            issuer_key_hash: issuer_key_hash.into(),
            serial_number: serial_number.into(),
            custom_data: None,
        }
    }

    /// Sets the optional `customData` field.
    #[must_use]
    pub fn with_custom_data(mut self, value: crate::types::CustomData) -> Self {
        self.custom_data = Some(value);
        self
    }
}

impl Validate for CertificateHashData {
    fn validate_at(&self, path: &mut ValidationPath, out: &mut Violations) {
        path.push_key("hashAlgorithm");
        Validate::validate_at(&self.hash_algorithm, path, out);
        path.pop();
        path.push_key("issuerNameHash");
        validate::string(self.issuer_name_hash.as_str(), None, Some(128), path, out);
        path.pop();
        path.push_key("issuerKeyHash");
        validate::string(self.issuer_key_hash.as_str(), None, Some(128), path, out);
        path.pop();
        path.push_key("serialNumber");
        validate::string(self.serial_number.as_str(), None, Some(40), path, out);
        path.pop();
        path.push_key("customData");
        if let Some(value) = &self.custom_data {
            Validate::validate_at(value, path, out);
        }
        path.pop();
    }
}

/// The OCPP 2.0.1 `CertificateHashDataChain` data type.
#[derive(Clone, Debug, PartialEq, Serialize, Deserialize)]
pub struct CertificateHashDataChain {
    /// The `certificateHashData` member.
    #[serde(rename = "certificateHashData")]
    pub certificate_hash_data: CertificateHashData,
    /// The `certificateType` member.
    #[serde(rename = "certificateType")]
    pub certificate_type: GetCertificateIdUse,
    /// The `childCertificateHashData` member.
    #[serde(rename = "childCertificateHashData")]
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub child_certificate_hash_data: Option<Vec<CertificateHashData>>,
    /// The `customData` member.
    #[serde(rename = "customData")]
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub custom_data: Option<crate::types::CustomData>,
}

impl CertificateHashDataChain {
    /// Creates a `CertificateHashDataChain` from its required fields.
    #[must_use]
    pub fn new(
        certificate_hash_data: CertificateHashData,
        certificate_type: GetCertificateIdUse,
    ) -> Self {
        Self {
            certificate_hash_data,
            certificate_type,
            child_certificate_hash_data: None,
            custom_data: None,
        }
    }

    /// Sets the optional `childCertificateHashData` field.
    #[must_use]
    pub fn with_child_certificate_hash_data(mut self, value: Vec<CertificateHashData>) -> Self {
        self.child_certificate_hash_data = Some(value);
        self
    }

    /// Sets the optional `customData` field.
    #[must_use]
    pub fn with_custom_data(mut self, value: crate::types::CustomData) -> Self {
        self.custom_data = Some(value);
        self
    }
}

impl Validate for CertificateHashDataChain {
    fn validate_at(&self, path: &mut ValidationPath, out: &mut Violations) {
        path.push_key("certificateHashData");
        Validate::validate_at(&self.certificate_hash_data, path, out);
        path.pop();
        path.push_key("certificateType");
        Validate::validate_at(&self.certificate_type, path, out);
        path.pop();
        path.push_key("childCertificateHashData");
        if let Some(value) = &self.child_certificate_hash_data {
            validate::list_len(value.len(), Some(1), Some(4), path, out);
            for (index, item) in value.iter().enumerate() {
                path.push_index(index);
                Validate::validate_at(item, path, out);
                path.pop();
            }
        }
        path.pop();
        path.push_key("customData");
        if let Some(value) = &self.custom_data {
            Validate::validate_at(value, path, out);
        }
        path.pop();
    }
}

/// Charging_ Limit
/// urn:x-enexis:ecdm:uid:2:234489
///
/// The OCPP 2.0.1 `ChargingLimit` data type.
#[derive(Clone, Debug, PartialEq, Serialize, Deserialize)]
pub struct ChargingLimit {
    /// The `chargingLimitSource` member.
    #[serde(rename = "chargingLimitSource")]
    pub charging_limit_source: ChargingLimitSource,
    /// Charging_ Limit. Is_ Grid_ Critical. Indicator
    /// urn:x-enexis:ecdm:uid:1:570847
    /// Indicates whether the charging limit is critical for the grid.
    #[serde(rename = "isGridCritical")]
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub is_grid_critical: Option<bool>,
    /// The `customData` member.
    #[serde(rename = "customData")]
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub custom_data: Option<crate::types::CustomData>,
}

impl ChargingLimit {
    /// Creates a `ChargingLimit` from its required fields.
    #[must_use]
    pub fn new(charging_limit_source: ChargingLimitSource) -> Self {
        Self {
            charging_limit_source,
            is_grid_critical: None,
            custom_data: None,
        }
    }

    /// Sets the optional `isGridCritical` field.
    #[must_use]
    pub fn with_is_grid_critical(mut self, value: bool) -> Self {
        self.is_grid_critical = Some(value);
        self
    }

    /// Sets the optional `customData` field.
    #[must_use]
    pub fn with_custom_data(mut self, value: crate::types::CustomData) -> Self {
        self.custom_data = Some(value);
        self
    }
}

impl Validate for ChargingLimit {
    fn validate_at(&self, path: &mut ValidationPath, out: &mut Violations) {
        path.push_key("chargingLimitSource");
        Validate::validate_at(&self.charging_limit_source, path, out);
        path.pop();
        path.push_key("customData");
        if let Some(value) = &self.custom_data {
            Validate::validate_at(value, path, out);
        }
        path.pop();
    }
}

/// Charging_ Needs
///
/// The OCPP 2.0.1 `ChargingNeeds` data type.
#[derive(Clone, Debug, PartialEq, Serialize, Deserialize)]
pub struct ChargingNeeds {
    /// The `requestedEnergyTransfer` member.
    #[serde(rename = "requestedEnergyTransfer")]
    pub requested_energy_transfer: EnergyTransferMode,
    /// The `acChargingParameters` member.
    #[serde(rename = "acChargingParameters")]
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub ac_charging_parameters: Option<ACChargingParameters>,
    /// The `dcChargingParameters` member.
    #[serde(rename = "dcChargingParameters")]
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub dc_charging_parameters: Option<DCChargingParameters>,
    /// Charging_ Needs. Departure_ Time. Date_ Time
    /// Estimated departure time of the EV.
    #[serde(rename = "departureTime")]
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub departure_time: Option<crate::types::DateTime>,
    /// The `customData` member.
    #[serde(rename = "customData")]
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub custom_data: Option<crate::types::CustomData>,
}

impl ChargingNeeds {
    /// Creates a `ChargingNeeds` from its required fields.
    #[must_use]
    pub fn new(requested_energy_transfer: EnergyTransferMode) -> Self {
        Self {
            requested_energy_transfer,
            ac_charging_parameters: None,
            dc_charging_parameters: None,
            departure_time: None,
            custom_data: None,
        }
    }

    /// Sets the optional `acChargingParameters` field.
    #[must_use]
    pub fn with_ac_charging_parameters(mut self, value: ACChargingParameters) -> Self {
        self.ac_charging_parameters = Some(value);
        self
    }

    /// Sets the optional `dcChargingParameters` field.
    #[must_use]
    pub fn with_dc_charging_parameters(mut self, value: DCChargingParameters) -> Self {
        self.dc_charging_parameters = Some(value);
        self
    }

    /// Sets the optional `departureTime` field.
    #[must_use]
    pub fn with_departure_time(mut self, value: impl Into<crate::types::DateTime>) -> Self {
        self.departure_time = Some(value.into());
        self
    }

    /// Sets the optional `customData` field.
    #[must_use]
    pub fn with_custom_data(mut self, value: crate::types::CustomData) -> Self {
        self.custom_data = Some(value);
        self
    }
}

impl Validate for ChargingNeeds {
    fn validate_at(&self, path: &mut ValidationPath, out: &mut Violations) {
        path.push_key("requestedEnergyTransfer");
        Validate::validate_at(&self.requested_energy_transfer, path, out);
        path.pop();
        path.push_key("acChargingParameters");
        if let Some(value) = &self.ac_charging_parameters {
            Validate::validate_at(value, path, out);
        }
        path.pop();
        path.push_key("dcChargingParameters");
        if let Some(value) = &self.dc_charging_parameters {
            Validate::validate_at(value, path, out);
        }
        path.pop();
        path.push_key("customData");
        if let Some(value) = &self.custom_data {
            Validate::validate_at(value, path, out);
        }
        path.pop();
    }
}

/// Charging_ Profile
/// A ChargingProfile consists of ChargingSchedule, describing the amount of power or current that can be delivered per time interval.
///
/// The OCPP 2.0.1 `ChargingProfile` data type.
#[derive(Clone, Debug, PartialEq, Serialize, Deserialize)]
pub struct ChargingProfile {
    /// Identified_ Object. MRID. Numeric_ Identifier
    /// urn:x-enexis:ecdm:uid:1:569198
    /// Id of ChargingProfile.
    pub id: i32,
    /// Charging_ Profile. Stack_ Level. Counter
    /// Value determining level in hierarchy stack of profiles. Higher values have precedence over lower values. Lowest level is 0.
    #[serde(rename = "stackLevel")]
    pub stack_level: i32,
    /// The `chargingProfilePurpose` member.
    #[serde(rename = "chargingProfilePurpose")]
    pub charging_profile_purpose: ChargingProfilePurpose,
    /// The `chargingProfileKind` member.
    #[serde(rename = "chargingProfileKind")]
    pub charging_profile_kind: ChargingProfileKind,
    /// The `chargingSchedule` member.
    #[serde(rename = "chargingSchedule")]
    pub charging_schedule: Vec<ChargingSchedule>,
    /// The `recurrencyKind` member.
    #[serde(rename = "recurrencyKind")]
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub recurrency_kind: Option<RecurrencyKind>,
    /// Charging_ Profile. Valid_ From. Date_ Time
    /// Point in time at which the profile starts to be valid. If absent, the profile is valid as soon as it is received by the Charging Station.
    #[serde(rename = "validFrom")]
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub valid_from: Option<crate::types::DateTime>,
    /// Charging_ Profile. Valid_ To. Date_ Time
    /// Point in time at which the profile stops to be valid. If absent, the profile is valid until it is replaced by another profile.
    #[serde(rename = "validTo")]
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub valid_to: Option<crate::types::DateTime>,
    /// SHALL only be included if ChargingProfilePurpose is set to TxProfile. The transactionId is used to match the profile to a specific transaction.
    #[serde(rename = "transactionId")]
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub transaction_id: Option<String>,
    /// The `customData` member.
    #[serde(rename = "customData")]
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub custom_data: Option<crate::types::CustomData>,
}

impl ChargingProfile {
    /// Creates a `ChargingProfile` from its required fields.
    #[must_use]
    pub fn new(
        id: i32,
        stack_level: i32,
        charging_profile_purpose: ChargingProfilePurpose,
        charging_profile_kind: ChargingProfileKind,
        charging_schedule: Vec<ChargingSchedule>,
    ) -> Self {
        Self {
            id,
            stack_level,
            charging_profile_purpose,
            charging_profile_kind,
            charging_schedule,
            recurrency_kind: None,
            valid_from: None,
            valid_to: None,
            transaction_id: None,
            custom_data: None,
        }
    }

    /// Sets the optional `recurrencyKind` field.
    #[must_use]
    pub fn with_recurrency_kind(mut self, value: RecurrencyKind) -> Self {
        self.recurrency_kind = Some(value);
        self
    }

    /// Sets the optional `validFrom` field.
    #[must_use]
    pub fn with_valid_from(mut self, value: impl Into<crate::types::DateTime>) -> Self {
        self.valid_from = Some(value.into());
        self
    }

    /// Sets the optional `validTo` field.
    #[must_use]
    pub fn with_valid_to(mut self, value: impl Into<crate::types::DateTime>) -> Self {
        self.valid_to = Some(value.into());
        self
    }

    /// Sets the optional `transactionId` field.
    #[must_use]
    pub fn with_transaction_id(mut self, value: impl Into<String>) -> Self {
        self.transaction_id = Some(value.into());
        self
    }

    /// Sets the optional `customData` field.
    #[must_use]
    pub fn with_custom_data(mut self, value: crate::types::CustomData) -> Self {
        self.custom_data = Some(value);
        self
    }
}

impl Validate for ChargingProfile {
    fn validate_at(&self, path: &mut ValidationPath, out: &mut Violations) {
        path.push_key("chargingProfilePurpose");
        Validate::validate_at(&self.charging_profile_purpose, path, out);
        path.pop();
        path.push_key("chargingProfileKind");
        Validate::validate_at(&self.charging_profile_kind, path, out);
        path.pop();
        path.push_key("chargingSchedule");
        validate::list_len(self.charging_schedule.len(), Some(1), Some(3), path, out);
        for (index, item) in self.charging_schedule.iter().enumerate() {
            path.push_index(index);
            Validate::validate_at(item, path, out);
            path.pop();
        }
        path.pop();
        path.push_key("recurrencyKind");
        if let Some(value) = &self.recurrency_kind {
            Validate::validate_at(value, path, out);
        }
        path.pop();
        path.push_key("transactionId");
        if let Some(value) = &self.transaction_id {
            validate::string(value.as_str(), None, Some(36), path, out);
        }
        path.pop();
        path.push_key("customData");
        if let Some(value) = &self.custom_data {
            Validate::validate_at(value, path, out);
        }
        path.pop();
    }
}

/// Charging_ Profile
/// A ChargingProfile consists of ChargingSchedule, describing the amount of power or current that can be delivered per time interval.
///
/// The OCPP 2.0.1 `ChargingProfileCriterion` data type.
#[derive(Clone, Debug, Default, PartialEq, Serialize, Deserialize)]
pub struct ChargingProfileCriterion {
    /// The `chargingProfilePurpose` member.
    #[serde(rename = "chargingProfilePurpose")]
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub charging_profile_purpose: Option<ChargingProfilePurpose>,
    /// Charging_ Profile. Stack_ Level. Counter
    /// Value determining level in hierarchy stack of profiles. Higher values have precedence over lower values. Lowest level is 0.
    #[serde(rename = "stackLevel")]
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub stack_level: Option<i32>,
    /// List of all the chargingProfileIds requested. Any ChargingProfile that matches one of these profiles will be reported. If omitted, the Charging Station SHALL not filter on chargingProfileId. This field SHALL NOT contain more ids than set in &lt;&lt;configkey-charging-profile-entries,ChargingProfileEntries.maxLimit&gt;&gt;
    #[serde(rename = "chargingProfileId")]
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub charging_profile_id: Option<Vec<i32>>,
    /// For which charging limit sources, charging profiles SHALL be reported. If omitted, the Charging Station SHALL not filter on chargingLimitSource.
    #[serde(rename = "chargingLimitSource")]
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub charging_limit_source: Option<Vec<ChargingLimitSource>>,
    /// The `customData` member.
    #[serde(rename = "customData")]
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub custom_data: Option<crate::types::CustomData>,
}

impl ChargingProfileCriterion {
    /// Creates an empty `ChargingProfileCriterion`. Every field of this type is optional.
    #[must_use]
    pub fn new() -> Self {
        Self::default()
    }

    /// Sets the optional `chargingProfilePurpose` field.
    #[must_use]
    pub fn with_charging_profile_purpose(mut self, value: ChargingProfilePurpose) -> Self {
        self.charging_profile_purpose = Some(value);
        self
    }

    /// Sets the optional `stackLevel` field.
    #[must_use]
    pub fn with_stack_level(mut self, value: i32) -> Self {
        self.stack_level = Some(value);
        self
    }

    /// Sets the optional `chargingProfileId` field.
    #[must_use]
    pub fn with_charging_profile_id(mut self, value: Vec<i32>) -> Self {
        self.charging_profile_id = Some(value);
        self
    }

    /// Sets the optional `chargingLimitSource` field.
    #[must_use]
    pub fn with_charging_limit_source(mut self, value: Vec<ChargingLimitSource>) -> Self {
        self.charging_limit_source = Some(value);
        self
    }

    /// Sets the optional `customData` field.
    #[must_use]
    pub fn with_custom_data(mut self, value: crate::types::CustomData) -> Self {
        self.custom_data = Some(value);
        self
    }
}

impl Validate for ChargingProfileCriterion {
    fn validate_at(&self, path: &mut ValidationPath, out: &mut Violations) {
        path.push_key("chargingProfilePurpose");
        if let Some(value) = &self.charging_profile_purpose {
            Validate::validate_at(value, path, out);
        }
        path.pop();
        path.push_key("chargingProfileId");
        if let Some(value) = &self.charging_profile_id {
            validate::list_len(value.len(), Some(1), None, path, out);
        }
        path.pop();
        path.push_key("chargingLimitSource");
        if let Some(value) = &self.charging_limit_source {
            validate::list_len(value.len(), Some(1), Some(4), path, out);
            for (index, item) in value.iter().enumerate() {
                path.push_index(index);
                Validate::validate_at(item, path, out);
                path.pop();
            }
        }
        path.pop();
        path.push_key("customData");
        if let Some(value) = &self.custom_data {
            Validate::validate_at(value, path, out);
        }
        path.pop();
    }
}

/// Charging_ Schedule
/// Charging schedule structure defines a list of charging periods, as used in: GetCompositeSchedule.conf and ChargingProfile.
///
/// The OCPP 2.0.1 `ChargingSchedule` data type.
#[derive(Clone, Debug, PartialEq, Serialize, Deserialize)]
pub struct ChargingSchedule {
    /// Identifies the ChargingSchedule.
    pub id: i32,
    /// The `chargingRateUnit` member.
    #[serde(rename = "chargingRateUnit")]
    pub charging_rate_unit: ChargingRateUnit,
    /// The `chargingSchedulePeriod` member.
    #[serde(rename = "chargingSchedulePeriod")]
    pub charging_schedule_period: Vec<ChargingSchedulePeriod>,
    /// Charging_ Schedule. Start_ Schedule. Date_ Time
    /// Starting point of an absolute schedule. If absent the schedule will be relative to start of charging.
    #[serde(rename = "startSchedule")]
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub start_schedule: Option<crate::types::DateTime>,
    /// Charging_ Schedule. Duration. Elapsed_ Time
    /// Duration of the charging schedule in seconds. If the duration is left empty, the last period will continue indefinitely or until end of the transaction if chargingProfilePurpose = TxProfile.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub duration: Option<i32>,
    /// Charging_ Schedule. Min_ Charging_ Rate. Numeric
    /// Minimum charging rate supported by the EV. The unit of measure is defined by the chargingRateUnit. This parameter is intended to be used by a local smart charging algorithm to optimize the power allocation for in the case a charging process is inefficient at lower charging rates. Accepts at most one digit fraction (e.g. 8.1)
    #[serde(rename = "minChargingRate")]
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub min_charging_rate: Option<crate::types::Decimal>,
    /// The `salesTariff` member.
    #[serde(rename = "salesTariff")]
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub sales_tariff: Option<SalesTariff>,
    /// The `customData` member.
    #[serde(rename = "customData")]
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub custom_data: Option<crate::types::CustomData>,
}

impl ChargingSchedule {
    /// Creates a `ChargingSchedule` from its required fields.
    #[must_use]
    pub fn new(
        id: i32,
        charging_rate_unit: ChargingRateUnit,
        charging_schedule_period: Vec<ChargingSchedulePeriod>,
    ) -> Self {
        Self {
            id,
            charging_rate_unit,
            charging_schedule_period,
            start_schedule: None,
            duration: None,
            min_charging_rate: None,
            sales_tariff: None,
            custom_data: None,
        }
    }

    /// Sets the optional `startSchedule` field.
    #[must_use]
    pub fn with_start_schedule(mut self, value: impl Into<crate::types::DateTime>) -> Self {
        self.start_schedule = Some(value.into());
        self
    }

    /// Sets the optional `duration` field.
    #[must_use]
    pub fn with_duration(mut self, value: i32) -> Self {
        self.duration = Some(value);
        self
    }

    /// Sets the optional `minChargingRate` field.
    #[must_use]
    pub fn with_min_charging_rate(mut self, value: impl Into<crate::types::Decimal>) -> Self {
        self.min_charging_rate = Some(value.into());
        self
    }

    /// Sets the optional `salesTariff` field.
    #[must_use]
    pub fn with_sales_tariff(mut self, value: SalesTariff) -> Self {
        self.sales_tariff = Some(value);
        self
    }

    /// Sets the optional `customData` field.
    #[must_use]
    pub fn with_custom_data(mut self, value: crate::types::CustomData) -> Self {
        self.custom_data = Some(value);
        self
    }
}

impl Validate for ChargingSchedule {
    fn validate_at(&self, path: &mut ValidationPath, out: &mut Violations) {
        path.push_key("chargingRateUnit");
        Validate::validate_at(&self.charging_rate_unit, path, out);
        path.pop();
        path.push_key("chargingSchedulePeriod");
        validate::list_len(
            self.charging_schedule_period.len(),
            Some(1),
            Some(1024),
            path,
            out,
        );
        for (index, item) in self.charging_schedule_period.iter().enumerate() {
            path.push_index(index);
            Validate::validate_at(item, path, out);
            path.pop();
        }
        path.pop();
        path.push_key("salesTariff");
        if let Some(value) = &self.sales_tariff {
            Validate::validate_at(value, path, out);
        }
        path.pop();
        path.push_key("customData");
        if let Some(value) = &self.custom_data {
            Validate::validate_at(value, path, out);
        }
        path.pop();
    }
}

/// Charging_ Schedule_ Period
/// Charging schedule period structure defines a time period in a charging schedule.
///
/// The OCPP 2.0.1 `ChargingSchedulePeriod` data type.
#[derive(Clone, Debug, PartialEq, Serialize, Deserialize)]
pub struct ChargingSchedulePeriod {
    /// Charging_ Schedule_ Period. Start_ Period. Elapsed_ Time
    /// Start of the period, in seconds from the start of schedule. The value of StartPeriod also defines the stop time of the previous period.
    #[serde(rename = "startPeriod")]
    pub start_period: i32,
    /// Charging_ Schedule_ Period. Limit. Measure
    /// Charging rate limit during the schedule period, in the applicable chargingRateUnit, for example in Amperes (A) or Watts (W). Accepts at most one digit fraction (e.g. 8.1).
    pub limit: crate::types::Decimal,
    /// Charging_ Schedule_ Period. Number_ Phases. Counter
    /// The number of phases that can be used for charging. If a number of phases is needed, numberPhases=3 will be assumed unless another number is given.
    #[serde(rename = "numberPhases")]
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub number_phases: Option<i32>,
    /// Values: 1..3, Used if numberPhases=1 and if the EVSE is capable of switching the phase connected to the EV, i.e. ACPhaseSwitchingSupported is defined and true. It’s not allowed unless both conditions above are true. If both conditions are true, and phaseToUse is omitted, the Charging Station / EVSE will make the selection on its own.
    #[serde(rename = "phaseToUse")]
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub phase_to_use: Option<i32>,
    /// The `customData` member.
    #[serde(rename = "customData")]
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub custom_data: Option<crate::types::CustomData>,
}

impl ChargingSchedulePeriod {
    /// Creates a `ChargingSchedulePeriod` from its required fields.
    #[must_use]
    pub fn new(start_period: i32, limit: impl Into<crate::types::Decimal>) -> Self {
        Self {
            start_period,
            limit: limit.into(),
            number_phases: None,
            phase_to_use: None,
            custom_data: None,
        }
    }

    /// Sets the optional `numberPhases` field.
    #[must_use]
    pub fn with_number_phases(mut self, value: i32) -> Self {
        self.number_phases = Some(value);
        self
    }

    /// Sets the optional `phaseToUse` field.
    #[must_use]
    pub fn with_phase_to_use(mut self, value: i32) -> Self {
        self.phase_to_use = Some(value);
        self
    }

    /// Sets the optional `customData` field.
    #[must_use]
    pub fn with_custom_data(mut self, value: crate::types::CustomData) -> Self {
        self.custom_data = Some(value);
        self
    }
}

impl Validate for ChargingSchedulePeriod {
    fn validate_at(&self, path: &mut ValidationPath, out: &mut Violations) {
        path.push_key("customData");
        if let Some(value) = &self.custom_data {
            Validate::validate_at(value, path, out);
        }
        path.pop();
    }
}

/// Charge_ Point
/// The physical system where an Electrical Vehicle (EV) can be charged.
///
/// The OCPP 2.0.1 `ChargingStation` data type.
#[derive(Clone, Debug, PartialEq, Serialize, Deserialize)]
pub struct ChargingStation {
    /// Device. Model. CI20_ Text
    /// Defines the model of the device.
    pub model: String,
    /// Identifies the vendor (not necessarily in a unique manner).
    #[serde(rename = "vendorName")]
    pub vendor_name: String,
    /// Device. Serial_ Number. Serial_ Number
    /// Vendor-specific device identifier.
    #[serde(rename = "serialNumber")]
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub serial_number: Option<String>,
    /// The `modem` member.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub modem: Option<Modem>,
    /// This contains the firmware version of the Charging Station.
    #[serde(rename = "firmwareVersion")]
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub firmware_version: Option<String>,
    /// The `customData` member.
    #[serde(rename = "customData")]
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub custom_data: Option<crate::types::CustomData>,
}

impl ChargingStation {
    /// Creates a `ChargingStation` from its required fields.
    #[must_use]
    pub fn new(model: impl Into<String>, vendor_name: impl Into<String>) -> Self {
        Self {
            model: model.into(),
            vendor_name: vendor_name.into(),
            serial_number: None,
            modem: None,
            firmware_version: None,
            custom_data: None,
        }
    }

    /// Sets the optional `serialNumber` field.
    #[must_use]
    pub fn with_serial_number(mut self, value: impl Into<String>) -> Self {
        self.serial_number = Some(value.into());
        self
    }

    /// Sets the optional `modem` field.
    #[must_use]
    pub fn with_modem(mut self, value: Modem) -> Self {
        self.modem = Some(value);
        self
    }

    /// Sets the optional `firmwareVersion` field.
    #[must_use]
    pub fn with_firmware_version(mut self, value: impl Into<String>) -> Self {
        self.firmware_version = Some(value.into());
        self
    }

    /// Sets the optional `customData` field.
    #[must_use]
    pub fn with_custom_data(mut self, value: crate::types::CustomData) -> Self {
        self.custom_data = Some(value);
        self
    }
}

impl Validate for ChargingStation {
    fn validate_at(&self, path: &mut ValidationPath, out: &mut Violations) {
        path.push_key("model");
        validate::string(self.model.as_str(), None, Some(20), path, out);
        path.pop();
        path.push_key("vendorName");
        validate::string(self.vendor_name.as_str(), None, Some(50), path, out);
        path.pop();
        path.push_key("serialNumber");
        if let Some(value) = &self.serial_number {
            validate::string(value.as_str(), None, Some(25), path, out);
        }
        path.pop();
        path.push_key("modem");
        if let Some(value) = &self.modem {
            Validate::validate_at(value, path, out);
        }
        path.pop();
        path.push_key("firmwareVersion");
        if let Some(value) = &self.firmware_version {
            validate::string(value.as_str(), None, Some(50), path, out);
        }
        path.pop();
        path.push_key("customData");
        if let Some(value) = &self.custom_data {
            Validate::validate_at(value, path, out);
        }
        path.pop();
    }
}

/// Charging_ Profile
/// A ChargingProfile consists of a ChargingSchedule, describing the amount of power or current that can be delivered per time interval.
///
/// The OCPP 2.0.1 `ClearChargingProfile` data type.
#[derive(Clone, Debug, Default, PartialEq, Serialize, Deserialize)]
pub struct ClearChargingProfile {
    /// Identified_ Object. MRID. Numeric_ Identifier
    /// urn:x-enexis:ecdm:uid:1:569198
    /// Specifies the id of the EVSE for which to clear charging profiles. An evseId of zero (0) specifies the charging profile for the overall Charging Station. Absence of this parameter means the clearing applies to all charging profiles that match the other criteria in the request.
    #[serde(rename = "evseId")]
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub evse_id: Option<i32>,
    /// The `chargingProfilePurpose` member.
    #[serde(rename = "chargingProfilePurpose")]
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub charging_profile_purpose: Option<ChargingProfilePurpose>,
    /// Charging_ Profile. Stack_ Level. Counter
    /// Specifies the stackLevel for which charging profiles will be cleared, if they meet the other criteria in the request.
    #[serde(rename = "stackLevel")]
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub stack_level: Option<i32>,
    /// The `customData` member.
    #[serde(rename = "customData")]
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub custom_data: Option<crate::types::CustomData>,
}

impl ClearChargingProfile {
    /// Creates an empty `ClearChargingProfile`. Every field of this type is optional.
    #[must_use]
    pub fn new() -> Self {
        Self::default()
    }

    /// Sets the optional `evseId` field.
    #[must_use]
    pub fn with_evse_id(mut self, value: i32) -> Self {
        self.evse_id = Some(value);
        self
    }

    /// Sets the optional `chargingProfilePurpose` field.
    #[must_use]
    pub fn with_charging_profile_purpose(mut self, value: ChargingProfilePurpose) -> Self {
        self.charging_profile_purpose = Some(value);
        self
    }

    /// Sets the optional `stackLevel` field.
    #[must_use]
    pub fn with_stack_level(mut self, value: i32) -> Self {
        self.stack_level = Some(value);
        self
    }

    /// Sets the optional `customData` field.
    #[must_use]
    pub fn with_custom_data(mut self, value: crate::types::CustomData) -> Self {
        self.custom_data = Some(value);
        self
    }
}

impl Validate for ClearChargingProfile {
    fn validate_at(&self, path: &mut ValidationPath, out: &mut Violations) {
        path.push_key("chargingProfilePurpose");
        if let Some(value) = &self.charging_profile_purpose {
            Validate::validate_at(value, path, out);
        }
        path.pop();
        path.push_key("customData");
        if let Some(value) = &self.custom_data {
            Validate::validate_at(value, path, out);
        }
        path.pop();
    }
}

/// The OCPP 2.0.1 `ClearMonitoringResult` data type.
#[derive(Clone, Debug, PartialEq, Serialize, Deserialize)]
pub struct ClearMonitoringResult {
    /// The `status` member.
    pub status: ClearMonitoringStatus,
    /// Id of the monitor of which a clear was requested.
    pub id: i32,
    /// The `statusInfo` member.
    #[serde(rename = "statusInfo")]
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub status_info: Option<StatusInfo>,
    /// The `customData` member.
    #[serde(rename = "customData")]
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub custom_data: Option<crate::types::CustomData>,
}

impl ClearMonitoringResult {
    /// Creates a `ClearMonitoringResult` from its required fields.
    #[must_use]
    pub fn new(status: ClearMonitoringStatus, id: i32) -> Self {
        Self {
            status,
            id,
            status_info: None,
            custom_data: None,
        }
    }

    /// Sets the optional `statusInfo` field.
    #[must_use]
    pub fn with_status_info(mut self, value: StatusInfo) -> Self {
        self.status_info = Some(value);
        self
    }

    /// Sets the optional `customData` field.
    #[must_use]
    pub fn with_custom_data(mut self, value: crate::types::CustomData) -> Self {
        self.custom_data = Some(value);
        self
    }
}

impl Validate for ClearMonitoringResult {
    fn validate_at(&self, path: &mut ValidationPath, out: &mut Violations) {
        path.push_key("status");
        Validate::validate_at(&self.status, path, out);
        path.pop();
        path.push_key("statusInfo");
        if let Some(value) = &self.status_info {
            Validate::validate_at(value, path, out);
        }
        path.pop();
        path.push_key("customData");
        if let Some(value) = &self.custom_data {
            Validate::validate_at(value, path, out);
        }
        path.pop();
    }
}

/// A physical or logical component
///
/// The OCPP 2.0.1 `Component` data type.
#[derive(Clone, Debug, PartialEq, Serialize, Deserialize)]
pub struct Component {
    /// Name of the component. Name should be taken from the list of standardized component names whenever possible. Case Insensitive. strongly advised to use Camel Case.
    pub name: String,
    /// The `evse` member.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub evse: Option<EVSE>,
    /// Name of instance in case the component exists as multiple instances. Case Insensitive. strongly advised to use Camel Case.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub instance: Option<String>,
    /// The `customData` member.
    #[serde(rename = "customData")]
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub custom_data: Option<crate::types::CustomData>,
}

impl Component {
    /// Creates a `Component` from its required fields.
    #[must_use]
    pub fn new(name: impl Into<String>) -> Self {
        Self {
            name: name.into(),
            evse: None,
            instance: None,
            custom_data: None,
        }
    }

    /// Sets the optional `evse` field.
    #[must_use]
    pub fn with_evse(mut self, value: EVSE) -> Self {
        self.evse = Some(value);
        self
    }

    /// Sets the optional `instance` field.
    #[must_use]
    pub fn with_instance(mut self, value: impl Into<String>) -> Self {
        self.instance = Some(value.into());
        self
    }

    /// Sets the optional `customData` field.
    #[must_use]
    pub fn with_custom_data(mut self, value: crate::types::CustomData) -> Self {
        self.custom_data = Some(value);
        self
    }
}

impl Validate for Component {
    fn validate_at(&self, path: &mut ValidationPath, out: &mut Violations) {
        path.push_key("name");
        validate::string(self.name.as_str(), None, Some(50), path, out);
        path.pop();
        path.push_key("evse");
        if let Some(value) = &self.evse {
            Validate::validate_at(value, path, out);
        }
        path.pop();
        path.push_key("instance");
        if let Some(value) = &self.instance {
            validate::string(value.as_str(), None, Some(50), path, out);
        }
        path.pop();
        path.push_key("customData");
        if let Some(value) = &self.custom_data {
            Validate::validate_at(value, path, out);
        }
        path.pop();
    }
}

/// Class to report components, variables and variable attributes and characteristics.
///
/// The OCPP 2.0.1 `ComponentVariable` data type.
#[derive(Clone, Debug, PartialEq, Serialize, Deserialize)]
pub struct ComponentVariable {
    /// The `component` member.
    pub component: Component,
    /// The `variable` member.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub variable: Option<Variable>,
    /// The `customData` member.
    #[serde(rename = "customData")]
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub custom_data: Option<crate::types::CustomData>,
}

impl ComponentVariable {
    /// Creates a `ComponentVariable` from its required fields.
    #[must_use]
    pub fn new(component: Component) -> Self {
        Self {
            component,
            variable: None,
            custom_data: None,
        }
    }

    /// Sets the optional `variable` field.
    #[must_use]
    pub fn with_variable(mut self, value: Variable) -> Self {
        self.variable = Some(value);
        self
    }

    /// Sets the optional `customData` field.
    #[must_use]
    pub fn with_custom_data(mut self, value: crate::types::CustomData) -> Self {
        self.custom_data = Some(value);
        self
    }
}

impl Validate for ComponentVariable {
    fn validate_at(&self, path: &mut ValidationPath, out: &mut Violations) {
        path.push_key("component");
        Validate::validate_at(&self.component, path, out);
        path.pop();
        path.push_key("variable");
        if let Some(value) = &self.variable {
            Validate::validate_at(value, path, out);
        }
        path.pop();
        path.push_key("customData");
        if let Some(value) = &self.custom_data {
            Validate::validate_at(value, path, out);
        }
        path.pop();
    }
}

/// Composite_ Schedule
///
/// The OCPP 2.0.1 `CompositeSchedule` data type.
#[derive(Clone, Debug, PartialEq, Serialize, Deserialize)]
pub struct CompositeSchedule {
    /// The `chargingSchedulePeriod` member.
    #[serde(rename = "chargingSchedulePeriod")]
    pub charging_schedule_period: Vec<ChargingSchedulePeriod>,
    /// The ID of the EVSE for which the
    /// schedule is requested. When evseid=0, the
    /// Charging Station calculated the expected
    /// consumption for the grid connection.
    #[serde(rename = "evseId")]
    pub evse_id: i32,
    /// Duration of the schedule in seconds.
    pub duration: i32,
    /// Composite_ Schedule. Start. Date_ Time
    /// Date and time at which the schedule becomes active. All time measurements within the schedule are relative to this timestamp.
    #[serde(rename = "scheduleStart")]
    pub schedule_start: crate::types::DateTime,
    /// The `chargingRateUnit` member.
    #[serde(rename = "chargingRateUnit")]
    pub charging_rate_unit: ChargingRateUnit,
    /// The `customData` member.
    #[serde(rename = "customData")]
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub custom_data: Option<crate::types::CustomData>,
}

impl CompositeSchedule {
    /// Creates a `CompositeSchedule` from its required fields.
    #[must_use]
    pub fn new(
        charging_schedule_period: Vec<ChargingSchedulePeriod>,
        evse_id: i32,
        duration: i32,
        schedule_start: impl Into<crate::types::DateTime>,
        charging_rate_unit: ChargingRateUnit,
    ) -> Self {
        Self {
            charging_schedule_period,
            evse_id,
            duration,
            schedule_start: schedule_start.into(),
            charging_rate_unit,
            custom_data: None,
        }
    }

    /// Sets the optional `customData` field.
    #[must_use]
    pub fn with_custom_data(mut self, value: crate::types::CustomData) -> Self {
        self.custom_data = Some(value);
        self
    }
}

impl Validate for CompositeSchedule {
    fn validate_at(&self, path: &mut ValidationPath, out: &mut Violations) {
        path.push_key("chargingSchedulePeriod");
        validate::list_len(
            self.charging_schedule_period.len(),
            Some(1),
            None,
            path,
            out,
        );
        for (index, item) in self.charging_schedule_period.iter().enumerate() {
            path.push_index(index);
            Validate::validate_at(item, path, out);
            path.pop();
        }
        path.pop();
        path.push_key("chargingRateUnit");
        Validate::validate_at(&self.charging_rate_unit, path, out);
        path.pop();
        path.push_key("customData");
        if let Some(value) = &self.custom_data {
            Validate::validate_at(value, path, out);
        }
        path.pop();
    }
}

/// Consumption_ Cost
///
/// The OCPP 2.0.1 `ConsumptionCost` data type.
#[derive(Clone, Debug, PartialEq, Serialize, Deserialize)]
pub struct ConsumptionCost {
    /// Consumption_ Cost. Start_ Value. Numeric
    /// The lowest level of consumption that defines the starting point of this consumption block. The block interval extends to the start of the next interval.
    #[serde(rename = "startValue")]
    pub start_value: crate::types::Decimal,
    /// The `cost` member.
    pub cost: Vec<Cost>,
    /// The `customData` member.
    #[serde(rename = "customData")]
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub custom_data: Option<crate::types::CustomData>,
}

impl ConsumptionCost {
    /// Creates a `ConsumptionCost` from its required fields.
    #[must_use]
    pub fn new(start_value: impl Into<crate::types::Decimal>, cost: Vec<Cost>) -> Self {
        Self {
            start_value: start_value.into(),
            cost,
            custom_data: None,
        }
    }

    /// Sets the optional `customData` field.
    #[must_use]
    pub fn with_custom_data(mut self, value: crate::types::CustomData) -> Self {
        self.custom_data = Some(value);
        self
    }
}

impl Validate for ConsumptionCost {
    fn validate_at(&self, path: &mut ValidationPath, out: &mut Violations) {
        path.push_key("cost");
        validate::list_len(self.cost.len(), Some(1), Some(3), path, out);
        for (index, item) in self.cost.iter().enumerate() {
            path.push_index(index);
            Validate::validate_at(item, path, out);
            path.pop();
        }
        path.pop();
        path.push_key("customData");
        if let Some(value) = &self.custom_data {
            Validate::validate_at(value, path, out);
        }
        path.pop();
    }
}

/// Cost
///
/// The OCPP 2.0.1 `Cost` data type.
#[derive(Clone, Debug, PartialEq, Serialize, Deserialize)]
pub struct Cost {
    /// The `costKind` member.
    #[serde(rename = "costKind")]
    pub cost_kind: CostKind,
    /// Cost. Amount. Amount
    /// The estimated or actual cost per kWh
    pub amount: i32,
    /// Cost. Amount_ Multiplier. Integer
    /// Values: -3..3, The amountMultiplier defines the exponent to base 10 (dec). The final value is determined by: amount * 10 ^ amountMultiplier
    #[serde(rename = "amountMultiplier")]
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub amount_multiplier: Option<i32>,
    /// The `customData` member.
    #[serde(rename = "customData")]
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub custom_data: Option<crate::types::CustomData>,
}

impl Cost {
    /// Creates a `Cost` from its required fields.
    #[must_use]
    pub fn new(cost_kind: CostKind, amount: i32) -> Self {
        Self {
            cost_kind,
            amount,
            amount_multiplier: None,
            custom_data: None,
        }
    }

    /// Sets the optional `amountMultiplier` field.
    #[must_use]
    pub fn with_amount_multiplier(mut self, value: i32) -> Self {
        self.amount_multiplier = Some(value);
        self
    }

    /// Sets the optional `customData` field.
    #[must_use]
    pub fn with_custom_data(mut self, value: crate::types::CustomData) -> Self {
        self.custom_data = Some(value);
        self
    }
}

impl Validate for Cost {
    fn validate_at(&self, path: &mut ValidationPath, out: &mut Violations) {
        path.push_key("costKind");
        Validate::validate_at(&self.cost_kind, path, out);
        path.pop();
        path.push_key("customData");
        if let Some(value) = &self.custom_data {
            Validate::validate_at(value, path, out);
        }
        path.pop();
    }
}

/// DC_ Charging_ Parameters
/// EV DC charging parameters
///
/// The OCPP 2.0.1 `DCChargingParameters` data type.
#[derive(Clone, Debug, PartialEq, Serialize, Deserialize)]
pub struct DCChargingParameters {
    /// DC_ Charging_ Parameters. EV_ Max. Current
    /// Maximum current (amps) supported by the electric vehicle. Includes cable capacity.
    #[serde(rename = "evMaxCurrent")]
    pub ev_max_current: i32,
    /// DC_ Charging_ Parameters. EV_ Max. Voltage
    /// Maximum voltage supported by the electric vehicle
    #[serde(rename = "evMaxVoltage")]
    pub ev_max_voltage: i32,
    /// DC_ Charging_ Parameters. Energy_ Amount. Energy_ Amount
    /// Amount of energy requested (in Wh). This inludes energy required for preconditioning.
    #[serde(rename = "energyAmount")]
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub energy_amount: Option<i32>,
    /// DC_ Charging_ Parameters. EV_ Max. Power
    /// Maximum power (in W) supported by the electric vehicle. Required for DC charging.
    #[serde(rename = "evMaxPower")]
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub ev_max_power: Option<i32>,
    /// DC_ Charging_ Parameters. State_ Of_ Charge. Numeric
    /// Energy available in the battery (in percent of the battery capacity)
    #[serde(rename = "stateOfCharge")]
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub state_of_charge: Option<i32>,
    /// DC_ Charging_ Parameters. EV_ Energy_ Capacity. Numeric
    /// Capacity of the electric vehicle battery (in Wh)
    #[serde(rename = "evEnergyCapacity")]
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub ev_energy_capacity: Option<i32>,
    /// DC_ Charging_ Parameters. Full_ SOC. Percentage
    /// Percentage of SoC at which the EV considers the battery fully charged. (possible values: 0 - 100)
    #[serde(rename = "fullSoC")]
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub full_so_c: Option<i32>,
    /// DC_ Charging_ Parameters. Bulk_ SOC. Percentage
    /// Percentage of SoC at which the EV considers a fast charging process to end. (possible values: 0 - 100)
    #[serde(rename = "bulkSoC")]
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub bulk_so_c: Option<i32>,
    /// The `customData` member.
    #[serde(rename = "customData")]
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub custom_data: Option<crate::types::CustomData>,
}

impl DCChargingParameters {
    /// Creates a `DCChargingParameters` from its required fields.
    #[must_use]
    pub fn new(ev_max_current: i32, ev_max_voltage: i32) -> Self {
        Self {
            ev_max_current,
            ev_max_voltage,
            energy_amount: None,
            ev_max_power: None,
            state_of_charge: None,
            ev_energy_capacity: None,
            full_so_c: None,
            bulk_so_c: None,
            custom_data: None,
        }
    }

    /// Sets the optional `energyAmount` field.
    #[must_use]
    pub fn with_energy_amount(mut self, value: i32) -> Self {
        self.energy_amount = Some(value);
        self
    }

    /// Sets the optional `evMaxPower` field.
    #[must_use]
    pub fn with_ev_max_power(mut self, value: i32) -> Self {
        self.ev_max_power = Some(value);
        self
    }

    /// Sets the optional `stateOfCharge` field.
    #[must_use]
    pub fn with_state_of_charge(mut self, value: i32) -> Self {
        self.state_of_charge = Some(value);
        self
    }

    /// Sets the optional `evEnergyCapacity` field.
    #[must_use]
    pub fn with_ev_energy_capacity(mut self, value: i32) -> Self {
        self.ev_energy_capacity = Some(value);
        self
    }

    /// Sets the optional `fullSoC` field.
    #[must_use]
    pub fn with_full_so_c(mut self, value: i32) -> Self {
        self.full_so_c = Some(value);
        self
    }

    /// Sets the optional `bulkSoC` field.
    #[must_use]
    pub fn with_bulk_so_c(mut self, value: i32) -> Self {
        self.bulk_so_c = Some(value);
        self
    }

    /// Sets the optional `customData` field.
    #[must_use]
    pub fn with_custom_data(mut self, value: crate::types::CustomData) -> Self {
        self.custom_data = Some(value);
        self
    }
}

impl Validate for DCChargingParameters {
    fn validate_at(&self, path: &mut ValidationPath, out: &mut Violations) {
        path.push_key("stateOfCharge");
        if let Some(value) = &self.state_of_charge {
            validate::int_range(i64::from(*value), Some(0), Some(100), path, out);
        }
        path.pop();
        path.push_key("fullSoC");
        if let Some(value) = &self.full_so_c {
            validate::int_range(i64::from(*value), Some(0), Some(100), path, out);
        }
        path.pop();
        path.push_key("bulkSoC");
        if let Some(value) = &self.bulk_so_c {
            validate::int_range(i64::from(*value), Some(0), Some(100), path, out);
        }
        path.pop();
        path.push_key("customData");
        if let Some(value) = &self.custom_data {
            Validate::validate_at(value, path, out);
        }
        path.pop();
    }
}

/// EVSE
/// Electric Vehicle Supply Equipment
///
/// The OCPP 2.0.1 `EVSE` data type.
#[derive(Clone, Debug, PartialEq, Serialize, Deserialize)]
pub struct EVSE {
    /// Identified_ Object. MRID. Numeric_ Identifier
    /// urn:x-enexis:ecdm:uid:1:569198
    /// EVSE Identifier. This contains a number (&gt; 0) designating an EVSE of the Charging Station.
    pub id: i32,
    /// An id to designate a specific connector (on an EVSE) by connector index number.
    #[serde(rename = "connectorId")]
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub connector_id: Option<i32>,
    /// The `customData` member.
    #[serde(rename = "customData")]
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub custom_data: Option<crate::types::CustomData>,
}

impl EVSE {
    /// Creates a `EVSE` from its required fields.
    #[must_use]
    pub fn new(id: i32) -> Self {
        Self {
            id,
            connector_id: None,
            custom_data: None,
        }
    }

    /// Sets the optional `connectorId` field.
    #[must_use]
    pub fn with_connector_id(mut self, value: i32) -> Self {
        self.connector_id = Some(value);
        self
    }

    /// Sets the optional `customData` field.
    #[must_use]
    pub fn with_custom_data(mut self, value: crate::types::CustomData) -> Self {
        self.custom_data = Some(value);
        self
    }
}

impl Validate for EVSE {
    fn validate_at(&self, path: &mut ValidationPath, out: &mut Violations) {
        path.push_key("customData");
        if let Some(value) = &self.custom_data {
            Validate::validate_at(value, path, out);
        }
        path.pop();
    }
}

/// Class to report an event notification for a component-variable.
///
/// The OCPP 2.0.1 `EventData` data type.
#[derive(Clone, Debug, PartialEq, Serialize, Deserialize)]
pub struct EventData {
    /// Identifies the event. This field can be referred to as a cause by other events.
    #[serde(rename = "eventId")]
    pub event_id: i32,
    /// Timestamp of the moment the report was generated.
    pub timestamp: crate::types::DateTime,
    /// The `trigger` member.
    pub trigger: EventTrigger,
    /// Actual value (_attributeType_ Actual) of the variable.
    ///
    /// The Configuration Variable &lt;&lt;configkey-reporting-value-size,ReportingValueSize&gt;&gt; can be used to limit GetVariableResult.attributeValue, VariableAttribute.value and EventData.actualValue. The max size of these values will always remain equal.
    #[serde(rename = "actualValue")]
    pub actual_value: String,
    /// The `component` member.
    pub component: Component,
    /// The `eventNotificationType` member.
    #[serde(rename = "eventNotificationType")]
    pub event_notification_type: EventNotification,
    /// The `variable` member.
    pub variable: Variable,
    /// Refers to the Id of an event that is considered to be the cause for this event.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub cause: Option<i32>,
    /// Technical (error) code as reported by component.
    #[serde(rename = "techCode")]
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub tech_code: Option<String>,
    /// Technical detail information as reported by component.
    #[serde(rename = "techInfo")]
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub tech_info: Option<String>,
    /// _Cleared_ is set to true to report the clearing of a monitored situation, i.e. a 'return to normal'.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub cleared: Option<bool>,
    /// If an event notification is linked to a specific transaction, this field can be used to specify its transactionId.
    #[serde(rename = "transactionId")]
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub transaction_id: Option<String>,
    /// Identifies the VariableMonitoring which triggered the event.
    #[serde(rename = "variableMonitoringId")]
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub variable_monitoring_id: Option<i32>,
    /// The `customData` member.
    #[serde(rename = "customData")]
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub custom_data: Option<crate::types::CustomData>,
}

impl EventData {
    /// Creates a `EventData` from its required fields.
    #[must_use]
    pub fn new(
        event_id: i32,
        timestamp: impl Into<crate::types::DateTime>,
        trigger: EventTrigger,
        actual_value: impl Into<String>,
        component: Component,
        event_notification_type: EventNotification,
        variable: Variable,
    ) -> Self {
        Self {
            event_id,
            timestamp: timestamp.into(),
            trigger,
            actual_value: actual_value.into(),
            component,
            event_notification_type,
            variable,
            cause: None,
            tech_code: None,
            tech_info: None,
            cleared: None,
            transaction_id: None,
            variable_monitoring_id: None,
            custom_data: None,
        }
    }

    /// Sets the optional `cause` field.
    #[must_use]
    pub fn with_cause(mut self, value: i32) -> Self {
        self.cause = Some(value);
        self
    }

    /// Sets the optional `techCode` field.
    #[must_use]
    pub fn with_tech_code(mut self, value: impl Into<String>) -> Self {
        self.tech_code = Some(value.into());
        self
    }

    /// Sets the optional `techInfo` field.
    #[must_use]
    pub fn with_tech_info(mut self, value: impl Into<String>) -> Self {
        self.tech_info = Some(value.into());
        self
    }

    /// Sets the optional `cleared` field.
    #[must_use]
    pub fn with_cleared(mut self, value: bool) -> Self {
        self.cleared = Some(value);
        self
    }

    /// Sets the optional `transactionId` field.
    #[must_use]
    pub fn with_transaction_id(mut self, value: impl Into<String>) -> Self {
        self.transaction_id = Some(value.into());
        self
    }

    /// Sets the optional `variableMonitoringId` field.
    #[must_use]
    pub fn with_variable_monitoring_id(mut self, value: i32) -> Self {
        self.variable_monitoring_id = Some(value);
        self
    }

    /// Sets the optional `customData` field.
    #[must_use]
    pub fn with_custom_data(mut self, value: crate::types::CustomData) -> Self {
        self.custom_data = Some(value);
        self
    }
}

impl Validate for EventData {
    fn validate_at(&self, path: &mut ValidationPath, out: &mut Violations) {
        path.push_key("trigger");
        Validate::validate_at(&self.trigger, path, out);
        path.pop();
        path.push_key("actualValue");
        validate::string(self.actual_value.as_str(), None, Some(2500), path, out);
        path.pop();
        path.push_key("component");
        Validate::validate_at(&self.component, path, out);
        path.pop();
        path.push_key("eventNotificationType");
        Validate::validate_at(&self.event_notification_type, path, out);
        path.pop();
        path.push_key("variable");
        Validate::validate_at(&self.variable, path, out);
        path.pop();
        path.push_key("techCode");
        if let Some(value) = &self.tech_code {
            validate::string(value.as_str(), None, Some(50), path, out);
        }
        path.pop();
        path.push_key("techInfo");
        if let Some(value) = &self.tech_info {
            validate::string(value.as_str(), None, Some(500), path, out);
        }
        path.pop();
        path.push_key("transactionId");
        if let Some(value) = &self.transaction_id {
            validate::string(value.as_str(), None, Some(36), path, out);
        }
        path.pop();
        path.push_key("customData");
        if let Some(value) = &self.custom_data {
            Validate::validate_at(value, path, out);
        }
        path.pop();
    }
}

/// Firmware
/// urn:x-enexis:ecdm:uid:2:233291
/// Represents a copy of the firmware that can be loaded/updated on the Charging Station.
///
/// The OCPP 2.0.1 `Firmware` data type.
#[derive(Clone, Debug, PartialEq, Serialize, Deserialize)]
pub struct Firmware {
    /// Firmware. Location. URI
    /// urn:x-enexis:ecdm:uid:1:569460
    /// URI defining the origin of the firmware.
    pub location: String,
    /// Firmware. Retrieve. Date_ Time
    /// urn:x-enexis:ecdm:uid:1:569461
    /// Date and time at which the firmware shall be retrieved.
    #[serde(rename = "retrieveDateTime")]
    pub retrieve_date_time: crate::types::DateTime,
    /// Firmware. Install. Date_ Time
    /// urn:x-enexis:ecdm:uid:1:569462
    /// Date and time at which the firmware shall be installed.
    #[serde(rename = "installDateTime")]
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub install_date_time: Option<crate::types::DateTime>,
    /// Certificate with which the firmware was signed.
    /// PEM encoded X.509 certificate.
    #[serde(rename = "signingCertificate")]
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub signing_certificate: Option<String>,
    /// Firmware. Signature. Signature
    /// urn:x-enexis:ecdm:uid:1:569464
    /// Base64 encoded firmware signature.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub signature: Option<String>,
    /// The `customData` member.
    #[serde(rename = "customData")]
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub custom_data: Option<crate::types::CustomData>,
}

impl Firmware {
    /// Creates a `Firmware` from its required fields.
    #[must_use]
    pub fn new(
        location: impl Into<String>,
        retrieve_date_time: impl Into<crate::types::DateTime>,
    ) -> Self {
        Self {
            location: location.into(),
            retrieve_date_time: retrieve_date_time.into(),
            install_date_time: None,
            signing_certificate: None,
            signature: None,
            custom_data: None,
        }
    }

    /// Sets the optional `installDateTime` field.
    #[must_use]
    pub fn with_install_date_time(mut self, value: impl Into<crate::types::DateTime>) -> Self {
        self.install_date_time = Some(value.into());
        self
    }

    /// Sets the optional `signingCertificate` field.
    #[must_use]
    pub fn with_signing_certificate(mut self, value: impl Into<String>) -> Self {
        self.signing_certificate = Some(value.into());
        self
    }

    /// Sets the optional `signature` field.
    #[must_use]
    pub fn with_signature(mut self, value: impl Into<String>) -> Self {
        self.signature = Some(value.into());
        self
    }

    /// Sets the optional `customData` field.
    #[must_use]
    pub fn with_custom_data(mut self, value: crate::types::CustomData) -> Self {
        self.custom_data = Some(value);
        self
    }
}

impl Validate for Firmware {
    fn validate_at(&self, path: &mut ValidationPath, out: &mut Violations) {
        path.push_key("location");
        validate::string(self.location.as_str(), None, Some(512), path, out);
        path.pop();
        path.push_key("signingCertificate");
        if let Some(value) = &self.signing_certificate {
            validate::string(value.as_str(), None, Some(5500), path, out);
        }
        path.pop();
        path.push_key("signature");
        if let Some(value) = &self.signature {
            validate::string(value.as_str(), None, Some(800), path, out);
        }
        path.pop();
        path.push_key("customData");
        if let Some(value) = &self.custom_data {
            Validate::validate_at(value, path, out);
        }
        path.pop();
    }
}

/// Class to hold parameters for GetVariables request.
///
/// The OCPP 2.0.1 `GetVariableData` data type.
#[derive(Clone, Debug, PartialEq, Serialize, Deserialize)]
pub struct GetVariableData {
    /// The `component` member.
    pub component: Component,
    /// The `variable` member.
    pub variable: Variable,
    /// The `attributeType` member.
    #[serde(rename = "attributeType")]
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub attribute_type: Option<Attribute>,
    /// The `customData` member.
    #[serde(rename = "customData")]
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub custom_data: Option<crate::types::CustomData>,
}

impl GetVariableData {
    /// Creates a `GetVariableData` from its required fields.
    #[must_use]
    pub fn new(component: Component, variable: Variable) -> Self {
        Self {
            component,
            variable,
            attribute_type: None,
            custom_data: None,
        }
    }

    /// Sets the optional `attributeType` field.
    #[must_use]
    pub fn with_attribute_type(mut self, value: Attribute) -> Self {
        self.attribute_type = Some(value);
        self
    }

    /// Sets the optional `customData` field.
    #[must_use]
    pub fn with_custom_data(mut self, value: crate::types::CustomData) -> Self {
        self.custom_data = Some(value);
        self
    }
}

impl Validate for GetVariableData {
    fn validate_at(&self, path: &mut ValidationPath, out: &mut Violations) {
        path.push_key("component");
        Validate::validate_at(&self.component, path, out);
        path.pop();
        path.push_key("variable");
        Validate::validate_at(&self.variable, path, out);
        path.pop();
        path.push_key("attributeType");
        if let Some(value) = &self.attribute_type {
            Validate::validate_at(value, path, out);
        }
        path.pop();
        path.push_key("customData");
        if let Some(value) = &self.custom_data {
            Validate::validate_at(value, path, out);
        }
        path.pop();
    }
}

/// Class to hold results of GetVariables request.
///
/// The OCPP 2.0.1 `GetVariableResult` data type.
#[derive(Clone, Debug, PartialEq, Serialize, Deserialize)]
pub struct GetVariableResult {
    /// The `attributeStatus` member.
    #[serde(rename = "attributeStatus")]
    pub attribute_status: GetVariableStatus,
    /// The `component` member.
    pub component: Component,
    /// The `variable` member.
    pub variable: Variable,
    /// The `attributeStatusInfo` member.
    #[serde(rename = "attributeStatusInfo")]
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub attribute_status_info: Option<StatusInfo>,
    /// The `attributeType` member.
    #[serde(rename = "attributeType")]
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub attribute_type: Option<Attribute>,
    /// Value of requested attribute type of component-variable. This field can only be empty when the given status is NOT accepted.
    ///
    /// The Configuration Variable &lt;&lt;configkey-reporting-value-size,ReportingValueSize&gt;&gt; can be used to limit GetVariableResult.attributeValue, VariableAttribute.value and EventData.actualValue. The max size of these values will always remain equal.
    #[serde(rename = "attributeValue")]
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub attribute_value: Option<String>,
    /// The `customData` member.
    #[serde(rename = "customData")]
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub custom_data: Option<crate::types::CustomData>,
}

impl GetVariableResult {
    /// Creates a `GetVariableResult` from its required fields.
    #[must_use]
    pub fn new(
        attribute_status: GetVariableStatus,
        component: Component,
        variable: Variable,
    ) -> Self {
        Self {
            attribute_status,
            component,
            variable,
            attribute_status_info: None,
            attribute_type: None,
            attribute_value: None,
            custom_data: None,
        }
    }

    /// Sets the optional `attributeStatusInfo` field.
    #[must_use]
    pub fn with_attribute_status_info(mut self, value: StatusInfo) -> Self {
        self.attribute_status_info = Some(value);
        self
    }

    /// Sets the optional `attributeType` field.
    #[must_use]
    pub fn with_attribute_type(mut self, value: Attribute) -> Self {
        self.attribute_type = Some(value);
        self
    }

    /// Sets the optional `attributeValue` field.
    #[must_use]
    pub fn with_attribute_value(mut self, value: impl Into<String>) -> Self {
        self.attribute_value = Some(value.into());
        self
    }

    /// Sets the optional `customData` field.
    #[must_use]
    pub fn with_custom_data(mut self, value: crate::types::CustomData) -> Self {
        self.custom_data = Some(value);
        self
    }
}

impl Validate for GetVariableResult {
    fn validate_at(&self, path: &mut ValidationPath, out: &mut Violations) {
        path.push_key("attributeStatus");
        Validate::validate_at(&self.attribute_status, path, out);
        path.pop();
        path.push_key("component");
        Validate::validate_at(&self.component, path, out);
        path.pop();
        path.push_key("variable");
        Validate::validate_at(&self.variable, path, out);
        path.pop();
        path.push_key("attributeStatusInfo");
        if let Some(value) = &self.attribute_status_info {
            Validate::validate_at(value, path, out);
        }
        path.pop();
        path.push_key("attributeType");
        if let Some(value) = &self.attribute_type {
            Validate::validate_at(value, path, out);
        }
        path.pop();
        path.push_key("attributeValue");
        if let Some(value) = &self.attribute_value {
            validate::string(value.as_str(), None, Some(2500), path, out);
        }
        path.pop();
        path.push_key("customData");
        if let Some(value) = &self.custom_data {
            Validate::validate_at(value, path, out);
        }
        path.pop();
    }
}

/// Contains a case insensitive identifier to use for the authorization and the type of authorization to support multiple forms of identifiers.
///
/// The OCPP 2.0.1 `IdToken` data type.
#[derive(Clone, Debug, PartialEq, Serialize, Deserialize)]
pub struct IdToken {
    /// IdToken is case insensitive. Might hold the hidden id of an RFID tag, but can for example also contain a UUID.
    #[serde(rename = "idToken")]
    pub id_token: String,
    /// The `type` member.
    pub r#type: IdTokenEnum,
    /// The `additionalInfo` member.
    #[serde(rename = "additionalInfo")]
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub additional_info: Option<Vec<AdditionalInfo>>,
    /// The `customData` member.
    #[serde(rename = "customData")]
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub custom_data: Option<crate::types::CustomData>,
}

impl IdToken {
    /// Creates a `IdToken` from its required fields.
    #[must_use]
    pub fn new(id_token: impl Into<String>, r#type: IdTokenEnum) -> Self {
        Self {
            id_token: id_token.into(),
            r#type,
            additional_info: None,
            custom_data: None,
        }
    }

    /// Sets the optional `additionalInfo` field.
    #[must_use]
    pub fn with_additional_info(mut self, value: Vec<AdditionalInfo>) -> Self {
        self.additional_info = Some(value);
        self
    }

    /// Sets the optional `customData` field.
    #[must_use]
    pub fn with_custom_data(mut self, value: crate::types::CustomData) -> Self {
        self.custom_data = Some(value);
        self
    }
}

impl Validate for IdToken {
    fn validate_at(&self, path: &mut ValidationPath, out: &mut Violations) {
        path.push_key("idToken");
        validate::string(self.id_token.as_str(), None, Some(36), path, out);
        path.pop();
        path.push_key("type");
        Validate::validate_at(&self.r#type, path, out);
        path.pop();
        path.push_key("additionalInfo");
        if let Some(value) = &self.additional_info {
            validate::list_len(value.len(), Some(1), None, path, out);
            for (index, item) in value.iter().enumerate() {
                path.push_index(index);
                Validate::validate_at(item, path, out);
                path.pop();
            }
        }
        path.pop();
        path.push_key("customData");
        if let Some(value) = &self.custom_data {
            Validate::validate_at(value, path, out);
        }
        path.pop();
    }
}

/// ID_ Token
/// Contains status information about an identifier.
/// It is advised to not stop charging for a token that expires during charging, as ExpiryDate is only used for caching purposes. If ExpiryDate is not given, the status has no end date.
///
/// The OCPP 2.0.1 `IdTokenInfo` data type.
#[derive(Clone, Debug, PartialEq, Serialize, Deserialize)]
pub struct IdTokenInfo {
    /// The `status` member.
    pub status: AuthorizationStatus,
    /// ID_ Token. Expiry. Date_ Time
    /// Date and Time after which the token must be considered invalid.
    #[serde(rename = "cacheExpiryDateTime")]
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub cache_expiry_date_time: Option<crate::types::DateTime>,
    /// Priority from a business point of view. Default priority is 0, The range is from -9 to 9. Higher values indicate a higher priority. The chargingPriority in &lt;&lt;transactioneventresponse,TransactionEventResponse&gt;&gt; overrules this one.
    #[serde(rename = "chargingPriority")]
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub charging_priority: Option<i32>,
    /// ID_ Token. Language1. Language_ Code
    /// Preferred user interface language of identifier user. Contains a language code as defined in &lt;&lt;ref-RFC5646,\[RFC5646\]&gt;&gt;.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub language1: Option<String>,
    /// Only used when the IdToken is only valid for one or more specific EVSEs, not for the entire Charging Station.
    #[serde(rename = "evseId")]
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub evse_id: Option<Vec<i32>>,
    /// The `groupIdToken` member.
    #[serde(rename = "groupIdToken")]
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub group_id_token: Option<IdToken>,
    /// ID_ Token. Language2. Language_ Code
    /// Second preferred user interface language of identifier user. Don’t use when language1 is omitted, has to be different from language1. Contains a language code as defined in &lt;&lt;ref-RFC5646,\[RFC5646\]&gt;&gt;.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub language2: Option<String>,
    /// The `personalMessage` member.
    #[serde(rename = "personalMessage")]
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub personal_message: Option<MessageContent>,
    /// The `customData` member.
    #[serde(rename = "customData")]
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub custom_data: Option<crate::types::CustomData>,
}

impl IdTokenInfo {
    /// Creates a `IdTokenInfo` from its required fields.
    #[must_use]
    pub fn new(status: AuthorizationStatus) -> Self {
        Self {
            status,
            cache_expiry_date_time: None,
            charging_priority: None,
            language1: None,
            evse_id: None,
            group_id_token: None,
            language2: None,
            personal_message: None,
            custom_data: None,
        }
    }

    /// Sets the optional `cacheExpiryDateTime` field.
    #[must_use]
    pub fn with_cache_expiry_date_time(mut self, value: impl Into<crate::types::DateTime>) -> Self {
        self.cache_expiry_date_time = Some(value.into());
        self
    }

    /// Sets the optional `chargingPriority` field.
    #[must_use]
    pub fn with_charging_priority(mut self, value: i32) -> Self {
        self.charging_priority = Some(value);
        self
    }

    /// Sets the optional `language1` field.
    #[must_use]
    pub fn with_language1(mut self, value: impl Into<String>) -> Self {
        self.language1 = Some(value.into());
        self
    }

    /// Sets the optional `evseId` field.
    #[must_use]
    pub fn with_evse_id(mut self, value: Vec<i32>) -> Self {
        self.evse_id = Some(value);
        self
    }

    /// Sets the optional `groupIdToken` field.
    #[must_use]
    pub fn with_group_id_token(mut self, value: IdToken) -> Self {
        self.group_id_token = Some(value);
        self
    }

    /// Sets the optional `language2` field.
    #[must_use]
    pub fn with_language2(mut self, value: impl Into<String>) -> Self {
        self.language2 = Some(value.into());
        self
    }

    /// Sets the optional `personalMessage` field.
    #[must_use]
    pub fn with_personal_message(mut self, value: MessageContent) -> Self {
        self.personal_message = Some(value);
        self
    }

    /// Sets the optional `customData` field.
    #[must_use]
    pub fn with_custom_data(mut self, value: crate::types::CustomData) -> Self {
        self.custom_data = Some(value);
        self
    }
}

impl Validate for IdTokenInfo {
    fn validate_at(&self, path: &mut ValidationPath, out: &mut Violations) {
        path.push_key("status");
        Validate::validate_at(&self.status, path, out);
        path.pop();
        path.push_key("language1");
        if let Some(value) = &self.language1 {
            validate::string(value.as_str(), None, Some(8), path, out);
        }
        path.pop();
        path.push_key("evseId");
        if let Some(value) = &self.evse_id {
            validate::list_len(value.len(), Some(1), None, path, out);
        }
        path.pop();
        path.push_key("groupIdToken");
        if let Some(value) = &self.group_id_token {
            Validate::validate_at(value, path, out);
        }
        path.pop();
        path.push_key("language2");
        if let Some(value) = &self.language2 {
            validate::string(value.as_str(), None, Some(8), path, out);
        }
        path.pop();
        path.push_key("personalMessage");
        if let Some(value) = &self.personal_message {
            Validate::validate_at(value, path, out);
        }
        path.pop();
        path.push_key("customData");
        if let Some(value) = &self.custom_data {
            Validate::validate_at(value, path, out);
        }
        path.pop();
    }
}

/// Log
/// urn:x-enexis:ecdm:uid:2:233373
/// Generic class for the configuration of logging entries.
///
/// The OCPP 2.0.1 `LogParameters` data type.
#[derive(Clone, Debug, PartialEq, Serialize, Deserialize)]
pub struct LogParameters {
    /// Log. Remote_ Location. URI
    /// urn:x-enexis:ecdm:uid:1:569484
    /// The URL of the location at the remote system where the log should be stored.
    #[serde(rename = "remoteLocation")]
    pub remote_location: String,
    /// Log. Oldest_ Timestamp. Date_ Time
    /// urn:x-enexis:ecdm:uid:1:569477
    /// This contains the date and time of the oldest logging information to include in the diagnostics.
    #[serde(rename = "oldestTimestamp")]
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub oldest_timestamp: Option<crate::types::DateTime>,
    /// Log. Latest_ Timestamp. Date_ Time
    /// urn:x-enexis:ecdm:uid:1:569482
    /// This contains the date and time of the latest logging information to include in the diagnostics.
    #[serde(rename = "latestTimestamp")]
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub latest_timestamp: Option<crate::types::DateTime>,
    /// The `customData` member.
    #[serde(rename = "customData")]
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub custom_data: Option<crate::types::CustomData>,
}

impl LogParameters {
    /// Creates a `LogParameters` from its required fields.
    #[must_use]
    pub fn new(remote_location: impl Into<String>) -> Self {
        Self {
            remote_location: remote_location.into(),
            oldest_timestamp: None,
            latest_timestamp: None,
            custom_data: None,
        }
    }

    /// Sets the optional `oldestTimestamp` field.
    #[must_use]
    pub fn with_oldest_timestamp(mut self, value: impl Into<crate::types::DateTime>) -> Self {
        self.oldest_timestamp = Some(value.into());
        self
    }

    /// Sets the optional `latestTimestamp` field.
    #[must_use]
    pub fn with_latest_timestamp(mut self, value: impl Into<crate::types::DateTime>) -> Self {
        self.latest_timestamp = Some(value.into());
        self
    }

    /// Sets the optional `customData` field.
    #[must_use]
    pub fn with_custom_data(mut self, value: crate::types::CustomData) -> Self {
        self.custom_data = Some(value);
        self
    }
}

impl Validate for LogParameters {
    fn validate_at(&self, path: &mut ValidationPath, out: &mut Violations) {
        path.push_key("remoteLocation");
        validate::string(self.remote_location.as_str(), None, Some(512), path, out);
        path.pop();
        path.push_key("customData");
        if let Some(value) = &self.custom_data {
            Validate::validate_at(value, path, out);
        }
        path.pop();
    }
}

/// Message_ Content
/// urn:x-enexis:ecdm:uid:2:234490
/// Contains message details, for a message to be displayed on a Charging Station.
///
/// The OCPP 2.0.1 `MessageContent` data type.
#[derive(Clone, Debug, PartialEq, Serialize, Deserialize)]
pub struct MessageContent {
    /// The `format` member.
    pub format: MessageFormat,
    /// Message_ Content. Content. Message
    /// urn:x-enexis:ecdm:uid:1:570852
    /// Message contents.
    pub content: String,
    /// Message_ Content. Language. Language_ Code
    /// urn:x-enexis:ecdm:uid:1:570849
    /// Message language identifier. Contains a language code as defined in &lt;&lt;ref-RFC5646,\[RFC5646\]&gt;&gt;.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub language: Option<String>,
    /// The `customData` member.
    #[serde(rename = "customData")]
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub custom_data: Option<crate::types::CustomData>,
}

impl MessageContent {
    /// Creates a `MessageContent` from its required fields.
    #[must_use]
    pub fn new(format: MessageFormat, content: impl Into<String>) -> Self {
        Self {
            format,
            content: content.into(),
            language: None,
            custom_data: None,
        }
    }

    /// Sets the optional `language` field.
    #[must_use]
    pub fn with_language(mut self, value: impl Into<String>) -> Self {
        self.language = Some(value.into());
        self
    }

    /// Sets the optional `customData` field.
    #[must_use]
    pub fn with_custom_data(mut self, value: crate::types::CustomData) -> Self {
        self.custom_data = Some(value);
        self
    }
}

impl Validate for MessageContent {
    fn validate_at(&self, path: &mut ValidationPath, out: &mut Violations) {
        path.push_key("format");
        Validate::validate_at(&self.format, path, out);
        path.pop();
        path.push_key("content");
        validate::string(self.content.as_str(), None, Some(512), path, out);
        path.pop();
        path.push_key("language");
        if let Some(value) = &self.language {
            validate::string(value.as_str(), None, Some(8), path, out);
        }
        path.pop();
        path.push_key("customData");
        if let Some(value) = &self.custom_data {
            Validate::validate_at(value, path, out);
        }
        path.pop();
    }
}

/// Message_ Info
/// urn:x-enexis:ecdm:uid:2:233264
/// Contains message details, for a message to be displayed on a Charging Station.
///
/// The OCPP 2.0.1 `MessageInfo` data type.
#[derive(Clone, Debug, PartialEq, Serialize, Deserialize)]
pub struct MessageInfo {
    /// Identified_ Object. MRID. Numeric_ Identifier
    /// urn:x-enexis:ecdm:uid:1:569198
    /// Master resource identifier, unique within an exchange context. It is defined within the OCPP context as a positive Integer value (greater or equal to zero).
    pub id: i32,
    /// The `priority` member.
    pub priority: MessagePriority,
    /// The `message` member.
    pub message: MessageContent,
    /// The `display` member.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub display: Option<Component>,
    /// The `state` member.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub state: Option<MessageState>,
    /// Message_ Info. Start. Date_ Time
    /// urn:x-enexis:ecdm:uid:1:569256
    /// From what date-time should this message be shown. If omitted: directly.
    #[serde(rename = "startDateTime")]
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub start_date_time: Option<crate::types::DateTime>,
    /// Message_ Info. End. Date_ Time
    /// urn:x-enexis:ecdm:uid:1:569257
    /// Until what date-time should this message be shown, after this date/time this message SHALL be removed.
    #[serde(rename = "endDateTime")]
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub end_date_time: Option<crate::types::DateTime>,
    /// During which transaction shall this message be shown.
    /// Message SHALL be removed by the Charging Station after transaction has
    /// ended.
    #[serde(rename = "transactionId")]
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub transaction_id: Option<String>,
    /// The `customData` member.
    #[serde(rename = "customData")]
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub custom_data: Option<crate::types::CustomData>,
}

impl MessageInfo {
    /// Creates a `MessageInfo` from its required fields.
    #[must_use]
    pub fn new(id: i32, priority: MessagePriority, message: MessageContent) -> Self {
        Self {
            id,
            priority,
            message,
            display: None,
            state: None,
            start_date_time: None,
            end_date_time: None,
            transaction_id: None,
            custom_data: None,
        }
    }

    /// Sets the optional `display` field.
    #[must_use]
    pub fn with_display(mut self, value: Component) -> Self {
        self.display = Some(value);
        self
    }

    /// Sets the optional `state` field.
    #[must_use]
    pub fn with_state(mut self, value: MessageState) -> Self {
        self.state = Some(value);
        self
    }

    /// Sets the optional `startDateTime` field.
    #[must_use]
    pub fn with_start_date_time(mut self, value: impl Into<crate::types::DateTime>) -> Self {
        self.start_date_time = Some(value.into());
        self
    }

    /// Sets the optional `endDateTime` field.
    #[must_use]
    pub fn with_end_date_time(mut self, value: impl Into<crate::types::DateTime>) -> Self {
        self.end_date_time = Some(value.into());
        self
    }

    /// Sets the optional `transactionId` field.
    #[must_use]
    pub fn with_transaction_id(mut self, value: impl Into<String>) -> Self {
        self.transaction_id = Some(value.into());
        self
    }

    /// Sets the optional `customData` field.
    #[must_use]
    pub fn with_custom_data(mut self, value: crate::types::CustomData) -> Self {
        self.custom_data = Some(value);
        self
    }
}

impl Validate for MessageInfo {
    fn validate_at(&self, path: &mut ValidationPath, out: &mut Violations) {
        path.push_key("priority");
        Validate::validate_at(&self.priority, path, out);
        path.pop();
        path.push_key("message");
        Validate::validate_at(&self.message, path, out);
        path.pop();
        path.push_key("display");
        if let Some(value) = &self.display {
            Validate::validate_at(value, path, out);
        }
        path.pop();
        path.push_key("state");
        if let Some(value) = &self.state {
            Validate::validate_at(value, path, out);
        }
        path.pop();
        path.push_key("transactionId");
        if let Some(value) = &self.transaction_id {
            validate::string(value.as_str(), None, Some(36), path, out);
        }
        path.pop();
        path.push_key("customData");
        if let Some(value) = &self.custom_data {
            Validate::validate_at(value, path, out);
        }
        path.pop();
    }
}

/// Meter_ Value
/// Collection of one or more sampled values in MeterValuesRequest and TransactionEvent. All sampled values in a MeterValue are sampled at the same point in time.
///
/// The OCPP 2.0.1 `MeterValue` data type.
#[derive(Clone, Debug, PartialEq, Serialize, Deserialize)]
pub struct MeterValue {
    /// The `sampledValue` member.
    #[serde(rename = "sampledValue")]
    pub sampled_value: Vec<SampledValue>,
    /// Meter_ Value. Timestamp. Date_ Time
    /// Timestamp for measured value(s).
    pub timestamp: crate::types::DateTime,
    /// The `customData` member.
    #[serde(rename = "customData")]
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub custom_data: Option<crate::types::CustomData>,
}

impl MeterValue {
    /// Creates a `MeterValue` from its required fields.
    #[must_use]
    pub fn new(
        sampled_value: Vec<SampledValue>,
        timestamp: impl Into<crate::types::DateTime>,
    ) -> Self {
        Self {
            sampled_value,
            timestamp: timestamp.into(),
            custom_data: None,
        }
    }

    /// Sets the optional `customData` field.
    #[must_use]
    pub fn with_custom_data(mut self, value: crate::types::CustomData) -> Self {
        self.custom_data = Some(value);
        self
    }
}

impl Validate for MeterValue {
    fn validate_at(&self, path: &mut ValidationPath, out: &mut Violations) {
        path.push_key("sampledValue");
        validate::list_len(self.sampled_value.len(), Some(1), None, path, out);
        for (index, item) in self.sampled_value.iter().enumerate() {
            path.push_index(index);
            Validate::validate_at(item, path, out);
            path.pop();
        }
        path.pop();
        path.push_key("customData");
        if let Some(value) = &self.custom_data {
            Validate::validate_at(value, path, out);
        }
        path.pop();
    }
}

/// Wireless_ Communication_ Module
/// Defines parameters required for initiating and maintaining wireless communication with other devices.
///
/// The OCPP 2.0.1 `Modem` data type.
#[derive(Clone, Debug, Default, PartialEq, Serialize, Deserialize)]
pub struct Modem {
    /// Wireless_ Communication_ Module. ICCID. CI20_ Text
    /// This contains the ICCID of the modem’s SIM card.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub iccid: Option<String>,
    /// Wireless_ Communication_ Module. IMSI. CI20_ Text
    /// This contains the IMSI of the modem’s SIM card.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub imsi: Option<String>,
    /// The `customData` member.
    #[serde(rename = "customData")]
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub custom_data: Option<crate::types::CustomData>,
}

impl Modem {
    /// Creates an empty `Modem`. Every field of this type is optional.
    #[must_use]
    pub fn new() -> Self {
        Self::default()
    }

    /// Sets the optional `iccid` field.
    #[must_use]
    pub fn with_iccid(mut self, value: impl Into<String>) -> Self {
        self.iccid = Some(value.into());
        self
    }

    /// Sets the optional `imsi` field.
    #[must_use]
    pub fn with_imsi(mut self, value: impl Into<String>) -> Self {
        self.imsi = Some(value.into());
        self
    }

    /// Sets the optional `customData` field.
    #[must_use]
    pub fn with_custom_data(mut self, value: crate::types::CustomData) -> Self {
        self.custom_data = Some(value);
        self
    }
}

impl Validate for Modem {
    fn validate_at(&self, path: &mut ValidationPath, out: &mut Violations) {
        path.push_key("iccid");
        if let Some(value) = &self.iccid {
            validate::string(value.as_str(), None, Some(20), path, out);
        }
        path.pop();
        path.push_key("imsi");
        if let Some(value) = &self.imsi {
            validate::string(value.as_str(), None, Some(20), path, out);
        }
        path.pop();
        path.push_key("customData");
        if let Some(value) = &self.custom_data {
            Validate::validate_at(value, path, out);
        }
        path.pop();
    }
}

/// Class to hold parameters of SetVariableMonitoring request.
///
/// The OCPP 2.0.1 `MonitoringData` data type.
#[derive(Clone, Debug, PartialEq, Serialize, Deserialize)]
pub struct MonitoringData {
    /// The `component` member.
    pub component: Component,
    /// The `variable` member.
    pub variable: Variable,
    /// The `variableMonitoring` member.
    #[serde(rename = "variableMonitoring")]
    pub variable_monitoring: Vec<VariableMonitoring>,
    /// The `customData` member.
    #[serde(rename = "customData")]
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub custom_data: Option<crate::types::CustomData>,
}

impl MonitoringData {
    /// Creates a `MonitoringData` from its required fields.
    #[must_use]
    pub fn new(
        component: Component,
        variable: Variable,
        variable_monitoring: Vec<VariableMonitoring>,
    ) -> Self {
        Self {
            component,
            variable,
            variable_monitoring,
            custom_data: None,
        }
    }

    /// Sets the optional `customData` field.
    #[must_use]
    pub fn with_custom_data(mut self, value: crate::types::CustomData) -> Self {
        self.custom_data = Some(value);
        self
    }
}

impl Validate for MonitoringData {
    fn validate_at(&self, path: &mut ValidationPath, out: &mut Violations) {
        path.push_key("component");
        Validate::validate_at(&self.component, path, out);
        path.pop();
        path.push_key("variable");
        Validate::validate_at(&self.variable, path, out);
        path.pop();
        path.push_key("variableMonitoring");
        validate::list_len(self.variable_monitoring.len(), Some(1), None, path, out);
        for (index, item) in self.variable_monitoring.iter().enumerate() {
            path.push_index(index);
            Validate::validate_at(item, path, out);
            path.pop();
        }
        path.pop();
        path.push_key("customData");
        if let Some(value) = &self.custom_data {
            Validate::validate_at(value, path, out);
        }
        path.pop();
    }
}

/// Communication_ Function
/// The NetworkConnectionProfile defines the functional and technical parameters of a communication link.
///
/// The OCPP 2.0.1 `NetworkConnectionProfile` data type.
#[derive(Clone, Debug, PartialEq, Serialize, Deserialize)]
pub struct NetworkConnectionProfile {
    /// The `ocppVersion` member.
    #[serde(rename = "ocppVersion")]
    pub ocpp_version: OCPPVersion,
    /// The `ocppTransport` member.
    #[serde(rename = "ocppTransport")]
    pub ocpp_transport: OCPPTransport,
    /// Communication_ Function. OCPP_ Central_ System_ URL. URI
    /// URL of the CSMS(s) that this Charging Station  communicates with.
    #[serde(rename = "ocppCsmsUrl")]
    pub ocpp_csms_url: String,
    /// Duration in seconds before a message send by the Charging Station via this network connection times-out.
    /// The best setting depends on the underlying network and response times of the CSMS.
    /// If you are looking for a some guideline: use 30 seconds as a starting point.
    #[serde(rename = "messageTimeout")]
    pub message_timeout: i32,
    /// This field specifies the security profile used when connecting to the CSMS with this NetworkConnectionProfile.
    #[serde(rename = "securityProfile")]
    pub security_profile: i32,
    /// The `ocppInterface` member.
    #[serde(rename = "ocppInterface")]
    pub ocpp_interface: OCPPInterface,
    /// The `apn` member.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub apn: Option<APN>,
    /// The `vpn` member.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub vpn: Option<VPN>,
    /// The `customData` member.
    #[serde(rename = "customData")]
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub custom_data: Option<crate::types::CustomData>,
}

impl NetworkConnectionProfile {
    /// Creates a `NetworkConnectionProfile` from its required fields.
    #[must_use]
    pub fn new(
        ocpp_version: OCPPVersion,
        ocpp_transport: OCPPTransport,
        ocpp_csms_url: impl Into<String>,
        message_timeout: i32,
        security_profile: i32,
        ocpp_interface: OCPPInterface,
    ) -> Self {
        Self {
            ocpp_version,
            ocpp_transport,
            ocpp_csms_url: ocpp_csms_url.into(),
            message_timeout,
            security_profile,
            ocpp_interface,
            apn: None,
            vpn: None,
            custom_data: None,
        }
    }

    /// Sets the optional `apn` field.
    #[must_use]
    pub fn with_apn(mut self, value: APN) -> Self {
        self.apn = Some(value);
        self
    }

    /// Sets the optional `vpn` field.
    #[must_use]
    pub fn with_vpn(mut self, value: VPN) -> Self {
        self.vpn = Some(value);
        self
    }

    /// Sets the optional `customData` field.
    #[must_use]
    pub fn with_custom_data(mut self, value: crate::types::CustomData) -> Self {
        self.custom_data = Some(value);
        self
    }
}

impl Validate for NetworkConnectionProfile {
    fn validate_at(&self, path: &mut ValidationPath, out: &mut Violations) {
        path.push_key("ocppVersion");
        Validate::validate_at(&self.ocpp_version, path, out);
        path.pop();
        path.push_key("ocppTransport");
        Validate::validate_at(&self.ocpp_transport, path, out);
        path.pop();
        path.push_key("ocppCsmsUrl");
        validate::string(self.ocpp_csms_url.as_str(), None, Some(512), path, out);
        path.pop();
        path.push_key("ocppInterface");
        Validate::validate_at(&self.ocpp_interface, path, out);
        path.pop();
        path.push_key("apn");
        if let Some(value) = &self.apn {
            Validate::validate_at(value, path, out);
        }
        path.pop();
        path.push_key("vpn");
        if let Some(value) = &self.vpn {
            Validate::validate_at(value, path, out);
        }
        path.pop();
        path.push_key("customData");
        if let Some(value) = &self.custom_data {
            Validate::validate_at(value, path, out);
        }
        path.pop();
    }
}

/// The OCPP 2.0.1 `OCSPRequestData` data type.
#[derive(Clone, Debug, PartialEq, Serialize, Deserialize)]
pub struct OCSPRequestData {
    /// The `hashAlgorithm` member.
    #[serde(rename = "hashAlgorithm")]
    pub hash_algorithm: HashAlgorithm,
    /// Hashed value of the Issuer DN (Distinguished Name).
    #[serde(rename = "issuerNameHash")]
    pub issuer_name_hash: String,
    /// Hashed value of the issuers public key
    #[serde(rename = "issuerKeyHash")]
    pub issuer_key_hash: String,
    /// The serial number of the certificate.
    #[serde(rename = "serialNumber")]
    pub serial_number: String,
    /// This contains the responder URL (Case insensitive).
    #[serde(rename = "responderURL")]
    pub responder_url: String,
    /// The `customData` member.
    #[serde(rename = "customData")]
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub custom_data: Option<crate::types::CustomData>,
}

impl OCSPRequestData {
    /// Creates a `OCSPRequestData` from its required fields.
    #[must_use]
    pub fn new(
        hash_algorithm: HashAlgorithm,
        issuer_name_hash: impl Into<String>,
        issuer_key_hash: impl Into<String>,
        serial_number: impl Into<String>,
        responder_url: impl Into<String>,
    ) -> Self {
        Self {
            hash_algorithm,
            issuer_name_hash: issuer_name_hash.into(),
            issuer_key_hash: issuer_key_hash.into(),
            serial_number: serial_number.into(),
            responder_url: responder_url.into(),
            custom_data: None,
        }
    }

    /// Sets the optional `customData` field.
    #[must_use]
    pub fn with_custom_data(mut self, value: crate::types::CustomData) -> Self {
        self.custom_data = Some(value);
        self
    }
}

impl Validate for OCSPRequestData {
    fn validate_at(&self, path: &mut ValidationPath, out: &mut Violations) {
        path.push_key("hashAlgorithm");
        Validate::validate_at(&self.hash_algorithm, path, out);
        path.pop();
        path.push_key("issuerNameHash");
        validate::string(self.issuer_name_hash.as_str(), None, Some(128), path, out);
        path.pop();
        path.push_key("issuerKeyHash");
        validate::string(self.issuer_key_hash.as_str(), None, Some(128), path, out);
        path.pop();
        path.push_key("serialNumber");
        validate::string(self.serial_number.as_str(), None, Some(40), path, out);
        path.pop();
        path.push_key("responderURL");
        validate::string(self.responder_url.as_str(), None, Some(512), path, out);
        path.pop();
        path.push_key("customData");
        if let Some(value) = &self.custom_data {
            Validate::validate_at(value, path, out);
        }
        path.pop();
    }
}

/// Relative_ Timer_ Interval
///
/// The OCPP 2.0.1 `RelativeTimeInterval` data type.
#[derive(Clone, Debug, PartialEq, Serialize, Deserialize)]
pub struct RelativeTimeInterval {
    /// Relative_ Timer_ Interval. Start. Elapsed_ Time
    /// Start of the interval, in seconds from NOW.
    pub start: i32,
    /// Relative_ Timer_ Interval. Duration. Elapsed_ Time
    /// Duration of the interval, in seconds.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub duration: Option<i32>,
    /// The `customData` member.
    #[serde(rename = "customData")]
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub custom_data: Option<crate::types::CustomData>,
}

impl RelativeTimeInterval {
    /// Creates a `RelativeTimeInterval` from its required fields.
    #[must_use]
    pub fn new(start: i32) -> Self {
        Self {
            start,
            duration: None,
            custom_data: None,
        }
    }

    /// Sets the optional `duration` field.
    #[must_use]
    pub fn with_duration(mut self, value: i32) -> Self {
        self.duration = Some(value);
        self
    }

    /// Sets the optional `customData` field.
    #[must_use]
    pub fn with_custom_data(mut self, value: crate::types::CustomData) -> Self {
        self.custom_data = Some(value);
        self
    }
}

impl Validate for RelativeTimeInterval {
    fn validate_at(&self, path: &mut ValidationPath, out: &mut Violations) {
        path.push_key("customData");
        if let Some(value) = &self.custom_data {
            Validate::validate_at(value, path, out);
        }
        path.pop();
    }
}

/// Class to report components, variables and variable attributes and characteristics.
///
/// The OCPP 2.0.1 `ReportData` data type.
#[derive(Clone, Debug, PartialEq, Serialize, Deserialize)]
pub struct ReportData {
    /// The `component` member.
    pub component: Component,
    /// The `variable` member.
    pub variable: Variable,
    /// The `variableAttribute` member.
    #[serde(rename = "variableAttribute")]
    pub variable_attribute: Vec<VariableAttribute>,
    /// The `variableCharacteristics` member.
    #[serde(rename = "variableCharacteristics")]
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub variable_characteristics: Option<VariableCharacteristics>,
    /// The `customData` member.
    #[serde(rename = "customData")]
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub custom_data: Option<crate::types::CustomData>,
}

impl ReportData {
    /// Creates a `ReportData` from its required fields.
    #[must_use]
    pub fn new(
        component: Component,
        variable: Variable,
        variable_attribute: Vec<VariableAttribute>,
    ) -> Self {
        Self {
            component,
            variable,
            variable_attribute,
            variable_characteristics: None,
            custom_data: None,
        }
    }

    /// Sets the optional `variableCharacteristics` field.
    #[must_use]
    pub fn with_variable_characteristics(mut self, value: VariableCharacteristics) -> Self {
        self.variable_characteristics = Some(value);
        self
    }

    /// Sets the optional `customData` field.
    #[must_use]
    pub fn with_custom_data(mut self, value: crate::types::CustomData) -> Self {
        self.custom_data = Some(value);
        self
    }
}

impl Validate for ReportData {
    fn validate_at(&self, path: &mut ValidationPath, out: &mut Violations) {
        path.push_key("component");
        Validate::validate_at(&self.component, path, out);
        path.pop();
        path.push_key("variable");
        Validate::validate_at(&self.variable, path, out);
        path.pop();
        path.push_key("variableAttribute");
        validate::list_len(self.variable_attribute.len(), Some(1), Some(4), path, out);
        for (index, item) in self.variable_attribute.iter().enumerate() {
            path.push_index(index);
            Validate::validate_at(item, path, out);
            path.pop();
        }
        path.pop();
        path.push_key("variableCharacteristics");
        if let Some(value) = &self.variable_characteristics {
            Validate::validate_at(value, path, out);
        }
        path.pop();
        path.push_key("customData");
        if let Some(value) = &self.custom_data {
            Validate::validate_at(value, path, out);
        }
        path.pop();
    }
}

/// Sales_ Tariff
/// NOTE: This dataType is based on dataTypes from &lt;&lt;ref-ISOIEC15118-2,ISO 15118-2&gt;&gt;.
///
/// The OCPP 2.0.1 `SalesTariff` data type.
#[derive(Clone, Debug, PartialEq, Serialize, Deserialize)]
pub struct SalesTariff {
    /// Identified_ Object. MRID. Numeric_ Identifier
    /// urn:x-enexis:ecdm:uid:1:569198
    /// SalesTariff identifier used to identify one sales tariff. An SAID remains a unique identifier for one schedule throughout a charging session.
    pub id: i32,
    /// The `salesTariffEntry` member.
    #[serde(rename = "salesTariffEntry")]
    pub sales_tariff_entry: Vec<SalesTariffEntry>,
    /// Sales_ Tariff. Sales. Tariff_ Description
    /// A human readable title/short description of the sales tariff e.g. for HMI display purposes.
    #[serde(rename = "salesTariffDescription")]
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub sales_tariff_description: Option<String>,
    /// Sales_ Tariff. Num_ E_ Price_ Levels. Counter
    /// Defines the overall number of distinct price levels used across all provided SalesTariff elements.
    #[serde(rename = "numEPriceLevels")]
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub num_e_price_levels: Option<i32>,
    /// The `customData` member.
    #[serde(rename = "customData")]
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub custom_data: Option<crate::types::CustomData>,
}

impl SalesTariff {
    /// Creates a `SalesTariff` from its required fields.
    #[must_use]
    pub fn new(id: i32, sales_tariff_entry: Vec<SalesTariffEntry>) -> Self {
        Self {
            id,
            sales_tariff_entry,
            sales_tariff_description: None,
            num_e_price_levels: None,
            custom_data: None,
        }
    }

    /// Sets the optional `salesTariffDescription` field.
    #[must_use]
    pub fn with_sales_tariff_description(mut self, value: impl Into<String>) -> Self {
        self.sales_tariff_description = Some(value.into());
        self
    }

    /// Sets the optional `numEPriceLevels` field.
    #[must_use]
    pub fn with_num_e_price_levels(mut self, value: i32) -> Self {
        self.num_e_price_levels = Some(value);
        self
    }

    /// Sets the optional `customData` field.
    #[must_use]
    pub fn with_custom_data(mut self, value: crate::types::CustomData) -> Self {
        self.custom_data = Some(value);
        self
    }
}

impl Validate for SalesTariff {
    fn validate_at(&self, path: &mut ValidationPath, out: &mut Violations) {
        path.push_key("salesTariffEntry");
        validate::list_len(
            self.sales_tariff_entry.len(),
            Some(1),
            Some(1024),
            path,
            out,
        );
        for (index, item) in self.sales_tariff_entry.iter().enumerate() {
            path.push_index(index);
            Validate::validate_at(item, path, out);
            path.pop();
        }
        path.pop();
        path.push_key("salesTariffDescription");
        if let Some(value) = &self.sales_tariff_description {
            validate::string(value.as_str(), None, Some(32), path, out);
        }
        path.pop();
        path.push_key("customData");
        if let Some(value) = &self.custom_data {
            Validate::validate_at(value, path, out);
        }
        path.pop();
    }
}

/// Sales_ Tariff_ Entry
///
/// The OCPP 2.0.1 `SalesTariffEntry` data type.
#[derive(Clone, Debug, PartialEq, Serialize, Deserialize)]
pub struct SalesTariffEntry {
    /// The `relativeTimeInterval` member.
    #[serde(rename = "relativeTimeInterval")]
    pub relative_time_interval: RelativeTimeInterval,
    /// Sales_ Tariff_ Entry. E_ Price_ Level. Unsigned_ Integer
    /// Defines the price level of this SalesTariffEntry (referring to NumEPriceLevels). Small values for the EPriceLevel represent a cheaper TariffEntry. Large values for the EPriceLevel represent a more expensive TariffEntry.
    #[serde(rename = "ePriceLevel")]
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub e_price_level: Option<i32>,
    /// The `consumptionCost` member.
    #[serde(rename = "consumptionCost")]
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub consumption_cost: Option<Vec<ConsumptionCost>>,
    /// The `customData` member.
    #[serde(rename = "customData")]
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub custom_data: Option<crate::types::CustomData>,
}

impl SalesTariffEntry {
    /// Creates a `SalesTariffEntry` from its required fields.
    #[must_use]
    pub fn new(relative_time_interval: RelativeTimeInterval) -> Self {
        Self {
            relative_time_interval,
            e_price_level: None,
            consumption_cost: None,
            custom_data: None,
        }
    }

    /// Sets the optional `ePriceLevel` field.
    #[must_use]
    pub fn with_e_price_level(mut self, value: i32) -> Self {
        self.e_price_level = Some(value);
        self
    }

    /// Sets the optional `consumptionCost` field.
    #[must_use]
    pub fn with_consumption_cost(mut self, value: Vec<ConsumptionCost>) -> Self {
        self.consumption_cost = Some(value);
        self
    }

    /// Sets the optional `customData` field.
    #[must_use]
    pub fn with_custom_data(mut self, value: crate::types::CustomData) -> Self {
        self.custom_data = Some(value);
        self
    }
}

impl Validate for SalesTariffEntry {
    fn validate_at(&self, path: &mut ValidationPath, out: &mut Violations) {
        path.push_key("relativeTimeInterval");
        Validate::validate_at(&self.relative_time_interval, path, out);
        path.pop();
        path.push_key("ePriceLevel");
        if let Some(value) = &self.e_price_level {
            validate::int_range(i64::from(*value), Some(0), None, path, out);
        }
        path.pop();
        path.push_key("consumptionCost");
        if let Some(value) = &self.consumption_cost {
            validate::list_len(value.len(), Some(1), Some(3), path, out);
            for (index, item) in value.iter().enumerate() {
                path.push_index(index);
                Validate::validate_at(item, path, out);
                path.pop();
            }
        }
        path.pop();
        path.push_key("customData");
        if let Some(value) = &self.custom_data {
            Validate::validate_at(value, path, out);
        }
        path.pop();
    }
}

/// Sampled_ Value
/// Single sampled value in MeterValues. Each value can be accompanied by optional fields.
///
/// To save on mobile data usage, default values of all of the optional fields are such that. The value without any additional fields will be interpreted, as a register reading of active import energy in Wh (Watt-hour) units.
///
/// The OCPP 2.0.1 `SampledValue` data type.
#[derive(Clone, Debug, PartialEq, Serialize, Deserialize)]
pub struct SampledValue {
    /// Sampled_ Value. Value. Measure
    /// Indicates the measured value.
    pub value: crate::types::Decimal,
    /// The `context` member.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub context: Option<ReadingContext>,
    /// The `measurand` member.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub measurand: Option<Measurand>,
    /// The `phase` member.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub phase: Option<Phase>,
    /// The `location` member.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub location: Option<Location>,
    /// The `signedMeterValue` member.
    #[serde(rename = "signedMeterValue")]
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub signed_meter_value: Option<SignedMeterValue>,
    /// The `unitOfMeasure` member.
    #[serde(rename = "unitOfMeasure")]
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub unit_of_measure: Option<UnitOfMeasure>,
    /// The `customData` member.
    #[serde(rename = "customData")]
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub custom_data: Option<crate::types::CustomData>,
}

impl SampledValue {
    /// Creates a `SampledValue` from its required fields.
    #[must_use]
    pub fn new(value: impl Into<crate::types::Decimal>) -> Self {
        Self {
            value: value.into(),
            context: None,
            measurand: None,
            phase: None,
            location: None,
            signed_meter_value: None,
            unit_of_measure: None,
            custom_data: None,
        }
    }

    /// Sets the optional `context` field.
    #[must_use]
    pub fn with_context(mut self, value: ReadingContext) -> Self {
        self.context = Some(value);
        self
    }

    /// Sets the optional `measurand` field.
    #[must_use]
    pub fn with_measurand(mut self, value: Measurand) -> Self {
        self.measurand = Some(value);
        self
    }

    /// Sets the optional `phase` field.
    #[must_use]
    pub fn with_phase(mut self, value: Phase) -> Self {
        self.phase = Some(value);
        self
    }

    /// Sets the optional `location` field.
    #[must_use]
    pub fn with_location(mut self, value: Location) -> Self {
        self.location = Some(value);
        self
    }

    /// Sets the optional `signedMeterValue` field.
    #[must_use]
    pub fn with_signed_meter_value(mut self, value: SignedMeterValue) -> Self {
        self.signed_meter_value = Some(value);
        self
    }

    /// Sets the optional `unitOfMeasure` field.
    #[must_use]
    pub fn with_unit_of_measure(mut self, value: UnitOfMeasure) -> Self {
        self.unit_of_measure = Some(value);
        self
    }

    /// Sets the optional `customData` field.
    #[must_use]
    pub fn with_custom_data(mut self, value: crate::types::CustomData) -> Self {
        self.custom_data = Some(value);
        self
    }
}

impl Validate for SampledValue {
    fn validate_at(&self, path: &mut ValidationPath, out: &mut Violations) {
        path.push_key("context");
        if let Some(value) = &self.context {
            Validate::validate_at(value, path, out);
        }
        path.pop();
        path.push_key("measurand");
        if let Some(value) = &self.measurand {
            Validate::validate_at(value, path, out);
        }
        path.pop();
        path.push_key("phase");
        if let Some(value) = &self.phase {
            Validate::validate_at(value, path, out);
        }
        path.pop();
        path.push_key("location");
        if let Some(value) = &self.location {
            Validate::validate_at(value, path, out);
        }
        path.pop();
        path.push_key("signedMeterValue");
        if let Some(value) = &self.signed_meter_value {
            Validate::validate_at(value, path, out);
        }
        path.pop();
        path.push_key("unitOfMeasure");
        if let Some(value) = &self.unit_of_measure {
            Validate::validate_at(value, path, out);
        }
        path.pop();
        path.push_key("customData");
        if let Some(value) = &self.custom_data {
            Validate::validate_at(value, path, out);
        }
        path.pop();
    }
}

/// Class to hold parameters of SetVariableMonitoring request.
///
/// The OCPP 2.0.1 `SetMonitoringData` data type.
#[derive(Clone, Debug, PartialEq, Serialize, Deserialize)]
pub struct SetMonitoringData {
    /// Value for threshold or delta monitoring.
    /// For Periodic or PeriodicClockAligned this is the interval in seconds.
    pub value: crate::types::Decimal,
    /// The `type` member.
    pub r#type: Monitor,
    /// The severity that will be assigned to an event that is triggered by this monitor. The severity range is 0-9, with 0 as the highest and 9 as the lowest severity level.
    ///
    /// The severity levels have the following meaning: +
    /// *0-Danger* +
    /// Indicates lives are potentially in danger. Urgent attention is needed and action should be taken immediately. +
    /// *1-Hardware Failure* +
    /// Indicates that the Charging Station is unable to continue regular operations due to Hardware issues. Action is required. +
    /// *2-System Failure* +
    /// Indicates that the Charging Station is unable to continue regular operations due to software or minor hardware issues. Action is required. +
    /// *3-Critical* +
    /// Indicates a critical error. Action is required. +
    /// *4-Error* +
    /// Indicates a non-urgent error. Action is required. +
    /// *5-Alert* +
    /// Indicates an alert event. Default severity for any type of monitoring event.  +
    /// *6-Warning* +
    /// Indicates a warning event. Action may be required. +
    /// *7-Notice* +
    /// Indicates an unusual event. No immediate action is required. +
    /// *8-Informational* +
    /// Indicates a regular operational event. May be used for reporting, measuring throughput, etc. No action is required. +
    /// *9-Debug* +
    /// Indicates information useful to developers for debugging, not useful during operations.
    pub severity: i32,
    /// The `component` member.
    pub component: Component,
    /// The `variable` member.
    pub variable: Variable,
    /// An id SHALL only be given to replace an existing monitor. The Charging Station handles the generation of id's for new monitors.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub id: Option<i32>,
    /// Monitor only active when a transaction is ongoing on a component relevant to this transaction. Default = false.
    /// Schema default: `false`.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub transaction: Option<bool>,
    /// The `customData` member.
    #[serde(rename = "customData")]
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub custom_data: Option<crate::types::CustomData>,
}

impl SetMonitoringData {
    /// Creates a `SetMonitoringData` from its required fields.
    #[must_use]
    pub fn new(
        value: impl Into<crate::types::Decimal>,
        r#type: Monitor,
        severity: i32,
        component: Component,
        variable: Variable,
    ) -> Self {
        Self {
            value: value.into(),
            r#type,
            severity,
            component,
            variable,
            id: None,
            transaction: None,
            custom_data: None,
        }
    }

    /// Sets the optional `id` field.
    #[must_use]
    pub fn with_id(mut self, value: i32) -> Self {
        self.id = Some(value);
        self
    }

    /// Sets the optional `transaction` field.
    #[must_use]
    pub fn with_transaction(mut self, value: bool) -> Self {
        self.transaction = Some(value);
        self
    }

    /// Sets the optional `customData` field.
    #[must_use]
    pub fn with_custom_data(mut self, value: crate::types::CustomData) -> Self {
        self.custom_data = Some(value);
        self
    }
}

impl Validate for SetMonitoringData {
    fn validate_at(&self, path: &mut ValidationPath, out: &mut Violations) {
        path.push_key("type");
        Validate::validate_at(&self.r#type, path, out);
        path.pop();
        path.push_key("component");
        Validate::validate_at(&self.component, path, out);
        path.pop();
        path.push_key("variable");
        Validate::validate_at(&self.variable, path, out);
        path.pop();
        path.push_key("customData");
        if let Some(value) = &self.custom_data {
            Validate::validate_at(value, path, out);
        }
        path.pop();
    }
}

/// Class to hold result of SetVariableMonitoring request.
///
/// The OCPP 2.0.1 `SetMonitoringResult` data type.
#[derive(Clone, Debug, PartialEq, Serialize, Deserialize)]
pub struct SetMonitoringResult {
    /// The `status` member.
    pub status: SetMonitoringStatus,
    /// The `type` member.
    pub r#type: Monitor,
    /// The `component` member.
    pub component: Component,
    /// The `variable` member.
    pub variable: Variable,
    /// The severity that will be assigned to an event that is triggered by this monitor. The severity range is 0-9, with 0 as the highest and 9 as the lowest severity level.
    ///
    /// The severity levels have the following meaning: +
    /// *0-Danger* +
    /// Indicates lives are potentially in danger. Urgent attention is needed and action should be taken immediately. +
    /// *1-Hardware Failure* +
    /// Indicates that the Charging Station is unable to continue regular operations due to Hardware issues. Action is required. +
    /// *2-System Failure* +
    /// Indicates that the Charging Station is unable to continue regular operations due to software or minor hardware issues. Action is required. +
    /// *3-Critical* +
    /// Indicates a critical error. Action is required. +
    /// *4-Error* +
    /// Indicates a non-urgent error. Action is required. +
    /// *5-Alert* +
    /// Indicates an alert event. Default severity for any type of monitoring event.  +
    /// *6-Warning* +
    /// Indicates a warning event. Action may be required. +
    /// *7-Notice* +
    /// Indicates an unusual event. No immediate action is required. +
    /// *8-Informational* +
    /// Indicates a regular operational event. May be used for reporting, measuring throughput, etc. No action is required. +
    /// *9-Debug* +
    /// Indicates information useful to developers for debugging, not useful during operations.
    pub severity: i32,
    /// Id given to the VariableMonitor by the Charging Station. The Id is only returned when status is accepted. Installed VariableMonitors should have unique id's but the id's of removed Installed monitors should have unique id's but the id's of removed monitors MAY be reused.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub id: Option<i32>,
    /// The `statusInfo` member.
    #[serde(rename = "statusInfo")]
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub status_info: Option<StatusInfo>,
    /// The `customData` member.
    #[serde(rename = "customData")]
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub custom_data: Option<crate::types::CustomData>,
}

impl SetMonitoringResult {
    /// Creates a `SetMonitoringResult` from its required fields.
    #[must_use]
    pub fn new(
        status: SetMonitoringStatus,
        r#type: Monitor,
        component: Component,
        variable: Variable,
        severity: i32,
    ) -> Self {
        Self {
            status,
            r#type,
            component,
            variable,
            severity,
            id: None,
            status_info: None,
            custom_data: None,
        }
    }

    /// Sets the optional `id` field.
    #[must_use]
    pub fn with_id(mut self, value: i32) -> Self {
        self.id = Some(value);
        self
    }

    /// Sets the optional `statusInfo` field.
    #[must_use]
    pub fn with_status_info(mut self, value: StatusInfo) -> Self {
        self.status_info = Some(value);
        self
    }

    /// Sets the optional `customData` field.
    #[must_use]
    pub fn with_custom_data(mut self, value: crate::types::CustomData) -> Self {
        self.custom_data = Some(value);
        self
    }
}

impl Validate for SetMonitoringResult {
    fn validate_at(&self, path: &mut ValidationPath, out: &mut Violations) {
        path.push_key("status");
        Validate::validate_at(&self.status, path, out);
        path.pop();
        path.push_key("type");
        Validate::validate_at(&self.r#type, path, out);
        path.pop();
        path.push_key("component");
        Validate::validate_at(&self.component, path, out);
        path.pop();
        path.push_key("variable");
        Validate::validate_at(&self.variable, path, out);
        path.pop();
        path.push_key("statusInfo");
        if let Some(value) = &self.status_info {
            Validate::validate_at(value, path, out);
        }
        path.pop();
        path.push_key("customData");
        if let Some(value) = &self.custom_data {
            Validate::validate_at(value, path, out);
        }
        path.pop();
    }
}

/// The OCPP 2.0.1 `SetVariableData` data type.
#[derive(Clone, Debug, PartialEq, Serialize, Deserialize)]
pub struct SetVariableData {
    /// Value to be assigned to attribute of variable.
    ///
    /// The Configuration Variable &lt;&lt;configkey-configuration-value-size,ConfigurationValueSize&gt;&gt; can be used to limit SetVariableData.attributeValue and VariableCharacteristics.valueList. The max size of these values will always remain equal.
    #[serde(rename = "attributeValue")]
    pub attribute_value: String,
    /// The `component` member.
    pub component: Component,
    /// The `variable` member.
    pub variable: Variable,
    /// The `attributeType` member.
    #[serde(rename = "attributeType")]
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub attribute_type: Option<Attribute>,
    /// The `customData` member.
    #[serde(rename = "customData")]
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub custom_data: Option<crate::types::CustomData>,
}

impl SetVariableData {
    /// Creates a `SetVariableData` from its required fields.
    #[must_use]
    pub fn new(
        attribute_value: impl Into<String>,
        component: Component,
        variable: Variable,
    ) -> Self {
        Self {
            attribute_value: attribute_value.into(),
            component,
            variable,
            attribute_type: None,
            custom_data: None,
        }
    }

    /// Sets the optional `attributeType` field.
    #[must_use]
    pub fn with_attribute_type(mut self, value: Attribute) -> Self {
        self.attribute_type = Some(value);
        self
    }

    /// Sets the optional `customData` field.
    #[must_use]
    pub fn with_custom_data(mut self, value: crate::types::CustomData) -> Self {
        self.custom_data = Some(value);
        self
    }
}

impl Validate for SetVariableData {
    fn validate_at(&self, path: &mut ValidationPath, out: &mut Violations) {
        path.push_key("attributeValue");
        validate::string(self.attribute_value.as_str(), None, Some(1000), path, out);
        path.pop();
        path.push_key("component");
        Validate::validate_at(&self.component, path, out);
        path.pop();
        path.push_key("variable");
        Validate::validate_at(&self.variable, path, out);
        path.pop();
        path.push_key("attributeType");
        if let Some(value) = &self.attribute_type {
            Validate::validate_at(value, path, out);
        }
        path.pop();
        path.push_key("customData");
        if let Some(value) = &self.custom_data {
            Validate::validate_at(value, path, out);
        }
        path.pop();
    }
}

/// The OCPP 2.0.1 `SetVariableResult` data type.
#[derive(Clone, Debug, PartialEq, Serialize, Deserialize)]
pub struct SetVariableResult {
    /// The `attributeStatus` member.
    #[serde(rename = "attributeStatus")]
    pub attribute_status: SetVariableStatus,
    /// The `component` member.
    pub component: Component,
    /// The `variable` member.
    pub variable: Variable,
    /// The `attributeType` member.
    #[serde(rename = "attributeType")]
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub attribute_type: Option<Attribute>,
    /// The `attributeStatusInfo` member.
    #[serde(rename = "attributeStatusInfo")]
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub attribute_status_info: Option<StatusInfo>,
    /// The `customData` member.
    #[serde(rename = "customData")]
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub custom_data: Option<crate::types::CustomData>,
}

impl SetVariableResult {
    /// Creates a `SetVariableResult` from its required fields.
    #[must_use]
    pub fn new(
        attribute_status: SetVariableStatus,
        component: Component,
        variable: Variable,
    ) -> Self {
        Self {
            attribute_status,
            component,
            variable,
            attribute_type: None,
            attribute_status_info: None,
            custom_data: None,
        }
    }

    /// Sets the optional `attributeType` field.
    #[must_use]
    pub fn with_attribute_type(mut self, value: Attribute) -> Self {
        self.attribute_type = Some(value);
        self
    }

    /// Sets the optional `attributeStatusInfo` field.
    #[must_use]
    pub fn with_attribute_status_info(mut self, value: StatusInfo) -> Self {
        self.attribute_status_info = Some(value);
        self
    }

    /// Sets the optional `customData` field.
    #[must_use]
    pub fn with_custom_data(mut self, value: crate::types::CustomData) -> Self {
        self.custom_data = Some(value);
        self
    }
}

impl Validate for SetVariableResult {
    fn validate_at(&self, path: &mut ValidationPath, out: &mut Violations) {
        path.push_key("attributeStatus");
        Validate::validate_at(&self.attribute_status, path, out);
        path.pop();
        path.push_key("component");
        Validate::validate_at(&self.component, path, out);
        path.pop();
        path.push_key("variable");
        Validate::validate_at(&self.variable, path, out);
        path.pop();
        path.push_key("attributeType");
        if let Some(value) = &self.attribute_type {
            Validate::validate_at(value, path, out);
        }
        path.pop();
        path.push_key("attributeStatusInfo");
        if let Some(value) = &self.attribute_status_info {
            Validate::validate_at(value, path, out);
        }
        path.pop();
        path.push_key("customData");
        if let Some(value) = &self.custom_data {
            Validate::validate_at(value, path, out);
        }
        path.pop();
    }
}

/// Represent a signed version of the meter value.
///
/// The OCPP 2.0.1 `SignedMeterValue` data type.
#[derive(Clone, Debug, PartialEq, Serialize, Deserialize)]
pub struct SignedMeterValue {
    /// Base64 encoded, contains the signed data which might contain more then just the meter value. It can contain information like timestamps, reference to a customer etc.
    #[serde(rename = "signedMeterData")]
    pub signed_meter_data: String,
    /// Method used to create the digital signature.
    #[serde(rename = "signingMethod")]
    pub signing_method: String,
    /// Method used to encode the meter values before applying the digital signature algorithm.
    #[serde(rename = "encodingMethod")]
    pub encoding_method: String,
    /// Base64 encoded, sending depends on configuration variable _PublicKeyWithSignedMeterValue_.
    #[serde(rename = "publicKey")]
    pub public_key: String,
    /// The `customData` member.
    #[serde(rename = "customData")]
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub custom_data: Option<crate::types::CustomData>,
}

impl SignedMeterValue {
    /// Creates a `SignedMeterValue` from its required fields.
    #[must_use]
    pub fn new(
        signed_meter_data: impl Into<String>,
        signing_method: impl Into<String>,
        encoding_method: impl Into<String>,
        public_key: impl Into<String>,
    ) -> Self {
        Self {
            signed_meter_data: signed_meter_data.into(),
            signing_method: signing_method.into(),
            encoding_method: encoding_method.into(),
            public_key: public_key.into(),
            custom_data: None,
        }
    }

    /// Sets the optional `customData` field.
    #[must_use]
    pub fn with_custom_data(mut self, value: crate::types::CustomData) -> Self {
        self.custom_data = Some(value);
        self
    }
}

impl Validate for SignedMeterValue {
    fn validate_at(&self, path: &mut ValidationPath, out: &mut Violations) {
        path.push_key("signedMeterData");
        validate::string(self.signed_meter_data.as_str(), None, Some(2500), path, out);
        path.pop();
        path.push_key("signingMethod");
        validate::string(self.signing_method.as_str(), None, Some(50), path, out);
        path.pop();
        path.push_key("encodingMethod");
        validate::string(self.encoding_method.as_str(), None, Some(50), path, out);
        path.pop();
        path.push_key("publicKey");
        validate::string(self.public_key.as_str(), None, Some(2500), path, out);
        path.pop();
        path.push_key("customData");
        if let Some(value) = &self.custom_data {
            Validate::validate_at(value, path, out);
        }
        path.pop();
    }
}

/// Element providing more information about the status.
///
/// The OCPP 2.0.1 `StatusInfo` data type.
#[derive(Clone, Debug, PartialEq, Serialize, Deserialize)]
pub struct StatusInfo {
    /// A predefined code for the reason why the status is returned in this response. The string is case-insensitive.
    #[serde(rename = "reasonCode")]
    pub reason_code: String,
    /// Additional text to provide detailed information.
    #[serde(rename = "additionalInfo")]
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub additional_info: Option<String>,
    /// The `customData` member.
    #[serde(rename = "customData")]
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub custom_data: Option<crate::types::CustomData>,
}

impl StatusInfo {
    /// Creates a `StatusInfo` from its required fields.
    #[must_use]
    pub fn new(reason_code: impl Into<String>) -> Self {
        Self {
            reason_code: reason_code.into(),
            additional_info: None,
            custom_data: None,
        }
    }

    /// Sets the optional `additionalInfo` field.
    #[must_use]
    pub fn with_additional_info(mut self, value: impl Into<String>) -> Self {
        self.additional_info = Some(value.into());
        self
    }

    /// Sets the optional `customData` field.
    #[must_use]
    pub fn with_custom_data(mut self, value: crate::types::CustomData) -> Self {
        self.custom_data = Some(value);
        self
    }
}

impl Validate for StatusInfo {
    fn validate_at(&self, path: &mut ValidationPath, out: &mut Violations) {
        path.push_key("reasonCode");
        validate::string(self.reason_code.as_str(), None, Some(20), path, out);
        path.pop();
        path.push_key("additionalInfo");
        if let Some(value) = &self.additional_info {
            validate::string(value.as_str(), None, Some(512), path, out);
        }
        path.pop();
        path.push_key("customData");
        if let Some(value) = &self.custom_data {
            Validate::validate_at(value, path, out);
        }
        path.pop();
    }
}

/// Transaction
///
/// The OCPP 2.0.1 `Transaction` data type.
#[derive(Clone, Debug, PartialEq, Serialize, Deserialize)]
pub struct Transaction {
    /// This contains the Id of the transaction.
    #[serde(rename = "transactionId")]
    pub transaction_id: String,
    /// The `chargingState` member.
    #[serde(rename = "chargingState")]
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub charging_state: Option<ChargingState>,
    /// Transaction. Time_ Spent_ Charging. Elapsed_ Time
    /// Contains the total time that energy flowed from EVSE to EV during the transaction (in seconds). Note that timeSpentCharging is smaller or equal to the duration of the transaction.
    #[serde(rename = "timeSpentCharging")]
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub time_spent_charging: Option<i32>,
    /// The `stoppedReason` member.
    #[serde(rename = "stoppedReason")]
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub stopped_reason: Option<Reason>,
    /// The ID given to remote start request (&lt;&lt;requeststarttransactionrequest, RequestStartTransactionRequest&gt;&gt;. This enables to CSMS to match the started transaction to the given start request.
    #[serde(rename = "remoteStartId")]
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub remote_start_id: Option<i32>,
    /// The `customData` member.
    #[serde(rename = "customData")]
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub custom_data: Option<crate::types::CustomData>,
}

impl Transaction {
    /// Creates a `Transaction` from its required fields.
    #[must_use]
    pub fn new(transaction_id: impl Into<String>) -> Self {
        Self {
            transaction_id: transaction_id.into(),
            charging_state: None,
            time_spent_charging: None,
            stopped_reason: None,
            remote_start_id: None,
            custom_data: None,
        }
    }

    /// Sets the optional `chargingState` field.
    #[must_use]
    pub fn with_charging_state(mut self, value: ChargingState) -> Self {
        self.charging_state = Some(value);
        self
    }

    /// Sets the optional `timeSpentCharging` field.
    #[must_use]
    pub fn with_time_spent_charging(mut self, value: i32) -> Self {
        self.time_spent_charging = Some(value);
        self
    }

    /// Sets the optional `stoppedReason` field.
    #[must_use]
    pub fn with_stopped_reason(mut self, value: Reason) -> Self {
        self.stopped_reason = Some(value);
        self
    }

    /// Sets the optional `remoteStartId` field.
    #[must_use]
    pub fn with_remote_start_id(mut self, value: i32) -> Self {
        self.remote_start_id = Some(value);
        self
    }

    /// Sets the optional `customData` field.
    #[must_use]
    pub fn with_custom_data(mut self, value: crate::types::CustomData) -> Self {
        self.custom_data = Some(value);
        self
    }
}

impl Validate for Transaction {
    fn validate_at(&self, path: &mut ValidationPath, out: &mut Violations) {
        path.push_key("transactionId");
        validate::string(self.transaction_id.as_str(), None, Some(36), path, out);
        path.pop();
        path.push_key("chargingState");
        if let Some(value) = &self.charging_state {
            Validate::validate_at(value, path, out);
        }
        path.pop();
        path.push_key("stoppedReason");
        if let Some(value) = &self.stopped_reason {
            Validate::validate_at(value, path, out);
        }
        path.pop();
        path.push_key("customData");
        if let Some(value) = &self.custom_data {
            Validate::validate_at(value, path, out);
        }
        path.pop();
    }
}

/// Represents a UnitOfMeasure with a multiplier
///
/// The OCPP 2.0.1 `UnitOfMeasure` data type.
#[derive(Clone, Debug, Default, PartialEq, Serialize, Deserialize)]
pub struct UnitOfMeasure {
    /// Unit of the value. Default = "Wh" if the (default) measurand is an "Energy" type.
    /// This field SHALL use a value from the list Standardized Units of Measurements in Part 2 Appendices.
    /// If an applicable unit is available in that list, otherwise a "custom" unit might be used.
    /// Schema default: `"Wh"`.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub unit: Option<String>,
    /// Multiplier, this value represents the exponent to base 10. I.e. multiplier 3 means 10 raised to the 3rd power. Default is 0.
    /// Schema default: `0`.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub multiplier: Option<i32>,
    /// The `customData` member.
    #[serde(rename = "customData")]
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub custom_data: Option<crate::types::CustomData>,
}

impl UnitOfMeasure {
    /// Creates an empty `UnitOfMeasure`. Every field of this type is optional.
    #[must_use]
    pub fn new() -> Self {
        Self::default()
    }

    /// Sets the optional `unit` field.
    #[must_use]
    pub fn with_unit(mut self, value: impl Into<String>) -> Self {
        self.unit = Some(value.into());
        self
    }

    /// Sets the optional `multiplier` field.
    #[must_use]
    pub fn with_multiplier(mut self, value: i32) -> Self {
        self.multiplier = Some(value);
        self
    }

    /// Sets the optional `customData` field.
    #[must_use]
    pub fn with_custom_data(mut self, value: crate::types::CustomData) -> Self {
        self.custom_data = Some(value);
        self
    }
}

impl Validate for UnitOfMeasure {
    fn validate_at(&self, path: &mut ValidationPath, out: &mut Violations) {
        path.push_key("unit");
        if let Some(value) = &self.unit {
            validate::string(value.as_str(), None, Some(20), path, out);
        }
        path.pop();
        path.push_key("customData");
        if let Some(value) = &self.custom_data {
            Validate::validate_at(value, path, out);
        }
        path.pop();
    }
}

/// VPN
/// VPN Configuration settings
///
/// The OCPP 2.0.1 `VPN` data type.
#[derive(Clone, Debug, PartialEq, Serialize, Deserialize)]
pub struct VPN {
    /// VPN. Server. URI
    /// VPN Server Address
    pub server: String,
    /// VPN. User. User_ Name
    /// VPN User
    pub user: String,
    /// VPN. Password. Password
    /// VPN Password.
    pub password: String,
    /// VPN. Key. VPN_ Key
    /// VPN shared secret.
    pub key: String,
    /// The `type` member.
    pub r#type: VPNEnum,
    /// VPN. Group. Group_ Name
    /// VPN group.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub group: Option<String>,
    /// The `customData` member.
    #[serde(rename = "customData")]
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub custom_data: Option<crate::types::CustomData>,
}

impl VPN {
    /// Creates a `VPN` from its required fields.
    #[must_use]
    pub fn new(
        server: impl Into<String>,
        user: impl Into<String>,
        password: impl Into<String>,
        key: impl Into<String>,
        r#type: VPNEnum,
    ) -> Self {
        Self {
            server: server.into(),
            user: user.into(),
            password: password.into(),
            key: key.into(),
            r#type,
            group: None,
            custom_data: None,
        }
    }

    /// Sets the optional `group` field.
    #[must_use]
    pub fn with_group(mut self, value: impl Into<String>) -> Self {
        self.group = Some(value.into());
        self
    }

    /// Sets the optional `customData` field.
    #[must_use]
    pub fn with_custom_data(mut self, value: crate::types::CustomData) -> Self {
        self.custom_data = Some(value);
        self
    }
}

impl Validate for VPN {
    fn validate_at(&self, path: &mut ValidationPath, out: &mut Violations) {
        path.push_key("server");
        validate::string(self.server.as_str(), None, Some(512), path, out);
        path.pop();
        path.push_key("user");
        validate::string(self.user.as_str(), None, Some(20), path, out);
        path.pop();
        path.push_key("password");
        validate::string(self.password.as_str(), None, Some(20), path, out);
        path.pop();
        path.push_key("key");
        validate::string(self.key.as_str(), None, Some(255), path, out);
        path.pop();
        path.push_key("type");
        Validate::validate_at(&self.r#type, path, out);
        path.pop();
        path.push_key("group");
        if let Some(value) = &self.group {
            validate::string(value.as_str(), None, Some(20), path, out);
        }
        path.pop();
        path.push_key("customData");
        if let Some(value) = &self.custom_data {
            Validate::validate_at(value, path, out);
        }
        path.pop();
    }
}

/// Reference key to a component-variable.
///
/// The OCPP 2.0.1 `Variable` data type.
#[derive(Clone, Debug, PartialEq, Serialize, Deserialize)]
pub struct Variable {
    /// Name of the variable. Name should be taken from the list of standardized variable names whenever possible. Case Insensitive. strongly advised to use Camel Case.
    pub name: String,
    /// Name of instance in case the variable exists as multiple instances. Case Insensitive. strongly advised to use Camel Case.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub instance: Option<String>,
    /// The `customData` member.
    #[serde(rename = "customData")]
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub custom_data: Option<crate::types::CustomData>,
}

impl Variable {
    /// Creates a `Variable` from its required fields.
    #[must_use]
    pub fn new(name: impl Into<String>) -> Self {
        Self {
            name: name.into(),
            instance: None,
            custom_data: None,
        }
    }

    /// Sets the optional `instance` field.
    #[must_use]
    pub fn with_instance(mut self, value: impl Into<String>) -> Self {
        self.instance = Some(value.into());
        self
    }

    /// Sets the optional `customData` field.
    #[must_use]
    pub fn with_custom_data(mut self, value: crate::types::CustomData) -> Self {
        self.custom_data = Some(value);
        self
    }
}

impl Validate for Variable {
    fn validate_at(&self, path: &mut ValidationPath, out: &mut Violations) {
        path.push_key("name");
        validate::string(self.name.as_str(), None, Some(50), path, out);
        path.pop();
        path.push_key("instance");
        if let Some(value) = &self.instance {
            validate::string(value.as_str(), None, Some(50), path, out);
        }
        path.pop();
        path.push_key("customData");
        if let Some(value) = &self.custom_data {
            Validate::validate_at(value, path, out);
        }
        path.pop();
    }
}

/// Attribute data of a variable.
///
/// The OCPP 2.0.1 `VariableAttribute` data type.
#[derive(Clone, Debug, Default, PartialEq, Serialize, Deserialize)]
pub struct VariableAttribute {
    /// The `type` member.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub r#type: Option<Attribute>,
    /// Value of the attribute. May only be omitted when mutability is set to 'WriteOnly'.
    ///
    /// The Configuration Variable &lt;&lt;configkey-reporting-value-size,ReportingValueSize&gt;&gt; can be used to limit GetVariableResult.attributeValue, VariableAttribute.value and EventData.actualValue. The max size of these values will always remain equal.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub value: Option<String>,
    /// The `mutability` member.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub mutability: Option<Mutability>,
    /// If true, value will be persistent across system reboots or power down. Default when omitted is false.
    /// Schema default: `false`.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub persistent: Option<bool>,
    /// If true, value that will never be changed by the Charging Station at runtime. Default when omitted is false.
    /// Schema default: `false`.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub constant: Option<bool>,
    /// The `customData` member.
    #[serde(rename = "customData")]
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub custom_data: Option<crate::types::CustomData>,
}

impl VariableAttribute {
    /// Creates an empty `VariableAttribute`. Every field of this type is optional.
    #[must_use]
    pub fn new() -> Self {
        Self::default()
    }

    /// Sets the optional `type` field.
    #[must_use]
    pub fn with_type(mut self, value: Attribute) -> Self {
        self.r#type = Some(value);
        self
    }

    /// Sets the optional `value` field.
    #[must_use]
    pub fn with_value(mut self, value: impl Into<String>) -> Self {
        self.value = Some(value.into());
        self
    }

    /// Sets the optional `mutability` field.
    #[must_use]
    pub fn with_mutability(mut self, value: Mutability) -> Self {
        self.mutability = Some(value);
        self
    }

    /// Sets the optional `persistent` field.
    #[must_use]
    pub fn with_persistent(mut self, value: bool) -> Self {
        self.persistent = Some(value);
        self
    }

    /// Sets the optional `constant` field.
    #[must_use]
    pub fn with_constant(mut self, value: bool) -> Self {
        self.constant = Some(value);
        self
    }

    /// Sets the optional `customData` field.
    #[must_use]
    pub fn with_custom_data(mut self, value: crate::types::CustomData) -> Self {
        self.custom_data = Some(value);
        self
    }
}

impl Validate for VariableAttribute {
    fn validate_at(&self, path: &mut ValidationPath, out: &mut Violations) {
        path.push_key("type");
        if let Some(value) = &self.r#type {
            Validate::validate_at(value, path, out);
        }
        path.pop();
        path.push_key("value");
        if let Some(value) = &self.value {
            validate::string(value.as_str(), None, Some(2500), path, out);
        }
        path.pop();
        path.push_key("mutability");
        if let Some(value) = &self.mutability {
            Validate::validate_at(value, path, out);
        }
        path.pop();
        path.push_key("customData");
        if let Some(value) = &self.custom_data {
            Validate::validate_at(value, path, out);
        }
        path.pop();
    }
}

/// Fixed read-only parameters of a variable.
///
/// The OCPP 2.0.1 `VariableCharacteristics` data type.
#[derive(Clone, Debug, PartialEq, Serialize, Deserialize)]
pub struct VariableCharacteristics {
    /// The `dataType` member.
    #[serde(rename = "dataType")]
    pub data_type: Data,
    /// Flag indicating if this variable supports monitoring.
    #[serde(rename = "supportsMonitoring")]
    pub supports_monitoring: bool,
    /// Unit of the variable. When the transmitted value has a unit, this field SHALL be included.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub unit: Option<String>,
    /// Minimum possible value of this variable.
    #[serde(rename = "minLimit")]
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub min_limit: Option<crate::types::Decimal>,
    /// Maximum possible value of this variable. When the datatype of this Variable is String, OptionList, SequenceList or MemberList, this field defines the maximum length of the (CSV) string.
    #[serde(rename = "maxLimit")]
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub max_limit: Option<crate::types::Decimal>,
    /// Allowed values when variable is Option/Member/SequenceList.
    ///
    /// * OptionList: The (Actual) Variable value must be a single value from the reported (CSV) enumeration list.
    ///
    /// * MemberList: The (Actual) Variable value  may be an (unordered) (sub-)set of the reported (CSV) valid values list.
    ///
    /// * SequenceList: The (Actual) Variable value  may be an ordered (priority, etc)  (sub-)set of the reported (CSV) valid values.
    ///
    /// This is a comma separated list.
    ///
    /// The Configuration Variable &lt;&lt;configkey-configuration-value-size,ConfigurationValueSize&gt;&gt; can be used to limit SetVariableData.attributeValue and VariableCharacteristics.valueList. The max size of these values will always remain equal.
    #[serde(rename = "valuesList")]
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub values_list: Option<String>,
    /// The `customData` member.
    #[serde(rename = "customData")]
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub custom_data: Option<crate::types::CustomData>,
}

impl VariableCharacteristics {
    /// Creates a `VariableCharacteristics` from its required fields.
    #[must_use]
    pub fn new(data_type: Data, supports_monitoring: bool) -> Self {
        Self {
            data_type,
            supports_monitoring,
            unit: None,
            min_limit: None,
            max_limit: None,
            values_list: None,
            custom_data: None,
        }
    }

    /// Sets the optional `unit` field.
    #[must_use]
    pub fn with_unit(mut self, value: impl Into<String>) -> Self {
        self.unit = Some(value.into());
        self
    }

    /// Sets the optional `minLimit` field.
    #[must_use]
    pub fn with_min_limit(mut self, value: impl Into<crate::types::Decimal>) -> Self {
        self.min_limit = Some(value.into());
        self
    }

    /// Sets the optional `maxLimit` field.
    #[must_use]
    pub fn with_max_limit(mut self, value: impl Into<crate::types::Decimal>) -> Self {
        self.max_limit = Some(value.into());
        self
    }

    /// Sets the optional `valuesList` field.
    #[must_use]
    pub fn with_values_list(mut self, value: impl Into<String>) -> Self {
        self.values_list = Some(value.into());
        self
    }

    /// Sets the optional `customData` field.
    #[must_use]
    pub fn with_custom_data(mut self, value: crate::types::CustomData) -> Self {
        self.custom_data = Some(value);
        self
    }
}

impl Validate for VariableCharacteristics {
    fn validate_at(&self, path: &mut ValidationPath, out: &mut Violations) {
        path.push_key("dataType");
        Validate::validate_at(&self.data_type, path, out);
        path.pop();
        path.push_key("unit");
        if let Some(value) = &self.unit {
            validate::string(value.as_str(), None, Some(16), path, out);
        }
        path.pop();
        path.push_key("valuesList");
        if let Some(value) = &self.values_list {
            validate::string(value.as_str(), None, Some(1000), path, out);
        }
        path.pop();
        path.push_key("customData");
        if let Some(value) = &self.custom_data {
            Validate::validate_at(value, path, out);
        }
        path.pop();
    }
}

/// A monitoring setting for a variable.
///
/// The OCPP 2.0.1 `VariableMonitoring` data type.
#[derive(Clone, Debug, PartialEq, Serialize, Deserialize)]
pub struct VariableMonitoring {
    /// Identifies the monitor.
    pub id: i32,
    /// Monitor only active when a transaction is ongoing on a component relevant to this transaction.
    pub transaction: bool,
    /// Value for threshold or delta monitoring.
    /// For Periodic or PeriodicClockAligned this is the interval in seconds.
    pub value: crate::types::Decimal,
    /// The `type` member.
    pub r#type: Monitor,
    /// The severity that will be assigned to an event that is triggered by this monitor. The severity range is 0-9, with 0 as the highest and 9 as the lowest severity level.
    ///
    /// The severity levels have the following meaning: +
    /// *0-Danger* +
    /// Indicates lives are potentially in danger. Urgent attention is needed and action should be taken immediately. +
    /// *1-Hardware Failure* +
    /// Indicates that the Charging Station is unable to continue regular operations due to Hardware issues. Action is required. +
    /// *2-System Failure* +
    /// Indicates that the Charging Station is unable to continue regular operations due to software or minor hardware issues. Action is required. +
    /// *3-Critical* +
    /// Indicates a critical error. Action is required. +
    /// *4-Error* +
    /// Indicates a non-urgent error. Action is required. +
    /// *5-Alert* +
    /// Indicates an alert event. Default severity for any type of monitoring event.  +
    /// *6-Warning* +
    /// Indicates a warning event. Action may be required. +
    /// *7-Notice* +
    /// Indicates an unusual event. No immediate action is required. +
    /// *8-Informational* +
    /// Indicates a regular operational event. May be used for reporting, measuring throughput, etc. No action is required. +
    /// *9-Debug* +
    /// Indicates information useful to developers for debugging, not useful during operations.
    pub severity: i32,
    /// The `customData` member.
    #[serde(rename = "customData")]
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub custom_data: Option<crate::types::CustomData>,
}

impl VariableMonitoring {
    /// Creates a `VariableMonitoring` from its required fields.
    #[must_use]
    pub fn new(
        id: i32,
        transaction: bool,
        value: impl Into<crate::types::Decimal>,
        r#type: Monitor,
        severity: i32,
    ) -> Self {
        Self {
            id,
            transaction,
            value: value.into(),
            r#type,
            severity,
            custom_data: None,
        }
    }

    /// Sets the optional `customData` field.
    #[must_use]
    pub fn with_custom_data(mut self, value: crate::types::CustomData) -> Self {
        self.custom_data = Some(value);
        self
    }
}

impl Validate for VariableMonitoring {
    fn validate_at(&self, path: &mut ValidationPath, out: &mut Violations) {
        path.push_key("type");
        Validate::validate_at(&self.r#type, path, out);
        path.pop();
        path.push_key("customData");
        if let Some(value) = &self.custom_data {
            Validate::validate_at(value, path, out);
        }
        path.pop();
    }
}
